# Phase 99B — Transpiler seeds for status icon regions + 0x006138 hardware poll

## Context

Phase 94/95 found that TI-84 CE status-bar icon renderers live in ROM regions that are NOT transpiled (0x06xxxx, 0xAFxxxx, 0xB1xxxx, 0xBCxxxx). Seven jump-table slots point into those regions:

| JT slot | target addr | region note |
|---|---|---|
| 616 | 0x06306a | 0x06xxxx (maybe already transpiled — verify) |
| 645 | 0x0af966 | 0xAFxxxx (likely missing) |
| 647 | 0x0afd2d | 0xAFxxxx (likely missing) |
| 648 | 0x0afd41 | 0xAFxxxx (likely missing) |
| 666 | 0x0b15a0 | 0xB1xxxx (likely missing) |
| 675 | 0x056ab2 | 0x56xxxx (maybe already transpiled — verify) |
| 696 | 0x0bcffa | 0xBCxxxx (likely missing) |

Additionally, Phase 98C found the cold boot path terminates at `lastPc=0x006138` (a hardware poll loop). **0x006138 is NOT in PRELIFTED_BLOCKS.** Seeding it will let the walker lift it plus its fall-through.

See `.phase98b-jt-targets.md` for the original JT decoding notes.

## Task

1. **Verify each target is missing from the current lifted blocks** before seeding. Load `TI-84_Plus_CE/ROM.transpiled.js` via a small Node script and check `PRELIFTED_BLOCKS` for each of the 8 addresses (0x06306a, 0x0af966, 0x0afd2d, 0x0afd41, 0x0b15a0, 0x056ab2, 0x0bcffa, 0x006138). Log which are already present vs missing — only seed the missing ones.
2. **Find the seeds list** in `C:/Users/rober/Downloads/Projects/school/follow-alongs/scripts/transpile-ti84-rom.mjs`. It's an array of entry point addresses (search for "seeds" or "entryPoints" or similar). There's also an on-disk additional-seeds file (`TI-84_Plus_CE/new-seeds*.txt`) that the walker may consume — check which mechanism is the one actually used.
3. **Append the missing targets** using whatever mechanism the walker reads. Add a comment block naming the seeds as "Phase 99B: status icon JT targets + 0x006138 hardware poll".
4. **Regenerate the lifted blocks**: `cd C:/Users/rober/Downloads/Projects/school/follow-alongs && node scripts/transpile-ti84-rom.mjs 2>&1 | tee phase99b-transpile.log`. The walker post-Phase 27 fix runs in ~1.9s per walk; expect the full retranspile to take a few minutes. Output should land in `TI-84_Plus_CE/ROM.transpiled.js`.
5. **Regenerate the .gz**: `gzip -k -f TI-84_Plus_CE/ROM.transpiled.js` (produces `TI-84_Plus_CE/ROM.transpiled.js.gz`). Both files must be rewritten.
6. **Verify**: re-load `ROM.transpiled.js` in a small Node script and confirm that each previously-missing seed address is now present in `PRELIFTED_BLOCKS`. Log the new total block count and new total bytes.
7. **Don't touch `ROM.transpiled.report.json`** — the transpiler script should regenerate it as part of its normal run.

## Verification probe

Write `TI-84_Plus_CE/probe-phase99b-verify.mjs`:

```js
// Load ROM.transpiled.js, assert each new seed is in PRELIFTED_BLOCKS,
// then run runFrom on each new seed with maxSteps=5000 and report
// {address, steps, term, lastPc, drawn} — shows whether the new blocks
// actually execute. Use the harness pattern from probe-phase98c-followup.mjs.
```

Run it and capture `phase99b-verify.log`.

## Deliverables

1. Modified `scripts/transpile-ti84-rom.mjs` (or appropriate seeds file) with Phase 99B seeds added.
2. Regenerated `TI-84_Plus_CE/ROM.transpiled.js` and `TI-84_Plus_CE/ROM.transpiled.js.gz` (both files — not committed since the .js is gitignored, but the .gz is committed).
3. `phase99b-transpile.log` — output of the retranspile run (root dir).
4. New `TI-84_Plus_CE/probe-phase99b-verify.mjs` + `phase99b-verify.log`.
5. `TI-84_Plus_CE/phase99b-report.md` with: (a) which targets were already present (skipped), (b) which were newly seeded, (c) new block count / bytes delta, (d) per-seed runFrom results (steps, term, lastPc, drawn), (e) any seeds that failed to produce valid blocks.

## Important constraints

- The `.js` file is ~175MB and gitignored — do NOT try to commit it. Only the `.gz` is committed.
- If the transpile run takes > 15 minutes, something is wrong — stop and report. The walker post-fix should complete in a few minutes.
- If a seed address is already present in `PRELIFTED_BLOCKS`, skip it — don't duplicate.
- Do NOT modify `TI-84_Plus_CE/browser-shell.html`, `font-decoder.mjs`, or `peripherals.js` — those are separate tasks.
- If `scripts/transpile-ti84-rom.mjs` accepts a CLI arg to add seeds (like `--seed 0x006138`), prefer that over editing the file.
- The existing `new-seeds.txt`, `new-seeds-round2.txt`, `new-seeds-round3.txt` files in `TI-84_Plus_CE/` are **stale and unused** per the CONTINUATION_PROMPT_CODEX.md perf-bug note — do not append to them.
