# TI-84 Trainer v2 — Multi-Agent Review Findings

Source: workflow `ti84-trainer-improvement-review` (39 agents, 2026-06-11).
Every confirmed finding survived an adversarial refutation attempt against the actual code.

Totals: 32 confirmed, 2 refuted, 14 low-severity/unverified.

## Confirmed findings (32)

### 1. [high] (student-ux) Recall mode shows the answer on screen the whole time, so the SRS never tests recall

Track 2's whole value is the guided->recall promotion: SM-2 intervals are computed from recall errors/hints. But in recall mode the step narration — which explicitly names the key, e.g. 'Press [STAT] to open the statistics menu' — is rendered unconditionally in BOTH the left walkthrough panel and the calculator narration bar. The mock-LCD footer additionally prints 'Expect [key]'. And every wrong press replies 'Blocked. The next key is [X]' (1 error), which is strictly cheaper than the Hint button (1 hint + 1 error, 2 hints = quality 0). A student in 'recall' just reads the instruction and presses the named key, earning quality 5 forever; the trainer's spaced-repetition data (and the new gradebook score = quality/5) measures nothing. This is the single biggest pedagogy defect for the Sept cohort.

**Where:** ti84-trainer-v2/app.js:2754 (copy = step?.narration, no mode gate), app.js:3111 + 3166 (narration bar), app.js:3415 ('Expect ' footer), app.js:1936 (wrong-key reveals next key), app.js:2444-2445 (hint costs more than a wrong press); narration text: ti84-procedures-data.json one-var-stats step 1

**Suggested fix:** In recall mode, replace narration with a neutral prompt ('Step 3 of 7 — what comes next?') in both the panel and the narration bar, suppress the 'Expect [key]' footer, and make wrongFeedback in recall mode say only 'Not that key' (the commonErrors coaching can stay — it explains why, not what). Reveal the key only via the Hint button so the hint/error economy works.

**Effort:** M

**Verification verdict:** {'isReal': True, 'note': "Confirmed at every cited line. (1) app.js:2754 renderWalkthroughPanel sets copy = step?.narration with no mode gate (only data-setup/result-review phase overrides); the 'Recall' badge at 2780 renders directly above the leaked narration. (2) app.js:3111/3166 renderCalculatorColumn puts the same ungated narration in the calculator narration bar. (3) app.js:3415 mock-LCD footer prints 'Expect [key]' for every step (mock mode only, as the finding scopes; narration bar still leaks in real-emulator mode). (4) app.js:1936 wrongFeedback fallback reveals 'The next key is [X]'; pressButton (2390-2395) charges recall mode only 1 error for it. (5) showHint (2444-2445) charges 1 hint + 1 error; recallQuality (2070-2088) gives 1 error = quality 4 vs 1 hint = quality 3 vs 2 hints = quality 0 + demotion — wrong-press is strictly cheaper than the hint, confirming the inverted economy. (6) ti84-procedures-data.json one-var-stats step 1 narration is literally 'Press [STAT] to open the statistics menu.' Impact confirmed: applyTrack2Outcome (2139-2140) feeds recallQuality into sm2(), and line 2192 exports score = quality/5 to the gradebook, so a narration-reading student earns quality 5 forever and the SRS/gradebook data measures nothing. Intent corroboration: keypad key-glow IS mode-gated (2896-2899: guided || hintVisible) and Hint is recall-only (3173), proving recall was meant to hide guidance — the text channels (panel, narration bar, mock footer, physical-mode card at 3054) defeat it. Severity high stands. Minor nuance: some narrations don't name the exact key ('Move to the CALC tab'), but most are directive enough, and the mock footer names the key for every step anyway."}

### 2. [high] (student-ux) Physical-calculator mode (the DEFAULT) is pure honor-system but records perfect scores to the gradebook

physicalMode defaults to true for every new student. In physical mode the step card always shows 'Press [key]' (even in recall mode), and 'I did it' (physicalAdvance) just advances — errors and hints are never counted. So recallQuality(0,0)=5 every time, SM-2 intervals balloon, and recordTrainerAttempt writes score = 5/5 = 1.0 to the roster ledger (source 'trainer') for clicking a button N times without touching any calculator. Guided completions similarly record a fixed 0.8 (lastQuality hardcoded to 4) regardless of performance. The day-old recording feature therefore produces data the teacher can't trust, and students discover the cheapest path is to spam 'I did it'.

**Where:** ti84-trainer-v2/app.js:750 + 765 (physicalMode default true), app.js:2485-2506 (physicalAdvance: no error/hint tracking), app.js:3050-3052 ('Press ${keyLabel}' always shown), app.js:2070-2088 (recallQuality(0,0)=5), app.js:2125-2129 (guided lastQuality=4 fixed), app.js:2177-2193 (score: quality/5 to gradebook)

**Suggested fix:** Tag recorded attempts with mode ('physical' vs 'emulator') and either cap physical-mode score (e.g. 0.6 'practiced') or record it as participation-only; require the answer-verification step (typing the computed result) to earn full credit in physical mode — that's the only observable evidence the procedure was actually run. Also reconsider the default: boot into emulator mode when no preference is saved.

**Effort:** M

**Verification verdict:** {'isReal': True, 'note': "Confirmed by reading ti84-trainer-v2/app.js. All cited lines hold: physicalMode defaults true (750/765, commit a976af6); physicalAdvance (2485-2506) advances with no error/hint tracking and the physical card has no hint button, so the only errors/hints increment sites (2391, 2444-5) are unreachable in physical mode; the physical step card always shows 'Press ${keyLabel}' regardless of guided/recall mode (3037-3052) and even reveals parameter values to type (2961-2969); recallQuality(0,0)=5 (2070-2088) so SM-2 intervals balloon; guided hardcodes lastQuality=4 (2125-2129); recordTrainerAttempt (2163-2194, commit f18c2ea = most recent) writes score=quality/5 (1.0 recall / 0.8 guided) to the roster ledger for signed-in students, and the payload's mode field is walkthrough.mode (guided/recall) — physical-vs-emulator is NOT recorded, so the teacher cannot filter it. One correction to the description: 'clicking a button N times' is slightly overstated — 22 procedures have VERIFICATION_FIELDS (506-572) and the finish-review handler (3497-3503) blocks completion until the student types matching result values, in physical mode too. But this gate is toothless for scoring: a failed Check reveals 'Expected: <value>' inline (2705) so the answer can be copied in on the second try, failed checks never increment errors (quality stays 5), and several procedures (histogram, scatterplot, boxplot, residual-plot, matrix-entry, two-propztest/zint, skill drills) have no verification and finish with one ungated click. Severity high is fair: default-on mode produces untrustworthy 1.0/0.8 gradebook scores indistinguishable from real emulator performance. The suggestion to tag attempts with physical/emulator mode is well-founded (currently absent); the answer-verification suggestion should be amended to also stop leaking the expected value on failed checks and/or feed verification failures into quality."}

### 3. [high] (student-ux) On phones (<=600px) the banner is display:none, so ALL mistake/hint/status feedback is invisible

Every piece of textual feedback — wrong-key coaching from commonErrors ('Use [STAT] directly. [2ND] only activates yellow secondary functions.'), 'Blocked...' messages, Hint text ('Hint: press [stat]'), data-setup/pause/resume status, error messages from the bridge — is written only to app.banner, and the mobile stylesheet hides .banner-row entirely. A phone student who presses a wrong key sees only a 220ms red flash with no explanation; pressing Hint still charges hints+errors (and can demote to guided / tank the recorded quality) while the hint text never appears anywhere. The richest teaching content in the data files (commonErrors feedback) is unreachable for mobile users.

**Where:** ti84-trainer-v2/style.css:1090-1092 (.banner-row display:none in @media 600px), app.js:2395 (wrong feedback -> app.banner only), app.js:2444-2453 (showHint charges penalty, writes banner only), app.js:1939-1950 (flashButton 220ms is the only mobile cue)

**Suggested fix:** On mobile, route app.banner into the sticky narration bar (replace the narration headline for ~3s, or stack a second line) or add a toast. At minimum, never charge a hint when the hint text cannot be displayed.

**Effort:** M

**Verification verdict:** {'isReal': True, 'note': "Confirmed core finding. Verified: (1) style.css:1090-1092 hides .banner-row at <=600px (mirrored standalone.html:1097), matching isMobileViewport()'s 600px (app.js:502-504); (2) app.banner's ONLY render sink is .banner-row (app.js:3350-3352, full-grep verified) so every banner message — commonErrors wrong-key coaching via wrongFeedback (app.js:2395, 1921-1937), 'Blocked...' fallback, hint sentences (2451-2453), and bridge error messages — is invisible on phones; (3) wrong press on mobile yields only the 220ms flashButton red flash (1939-1950); (4) showHint charges hints+errors (2444-2446) and hints>=2 → recallQuality 0 (2070-2073) + demotion to guided (2146-2148), so the SM-2 impact claim holds. Two corrections soften sub-claims: (a) Hint is NOT useless on mobile — hintVisible drives showAssist (2897-2899) which highlights the correct key ('suggested' class) and dims the rest (renderKeyButton 2855-2870, guidedSuggestions 1894-1918), so the hint's actionable content IS visible; only the text/sample-value is hidden — the 'never charge a hint when it cannot be displayed' suggestion mischaracterizes this. (b) data-setup/result-review/pause status is partly duplicated in the mobile-visible sticky narration bar (app.js:3114-3120, 3174-3178; style.css:1159-1167). Net severity: still real and high-impact for mistake coaching and error messages (the richest teaching content in the data files is unreachable on mobile), slightly lower than stated for hints/status. Fix suggestion of routing banner into the narration bar or a toast remains the right shape."}

### 4. [high] (student-ux) Mobile + default physical mode is a dead end: no way to reach Options or the emulator

The only exits from physical mode are (a) the 'Options' button in the window titlebar and (b) the 'Real TI-84' toggle inside the emulator's narration bar. On <=600px the titlebar is display:none, and renderPhysicalColumn (the column shown in physical mode) contains neither button. Since physicalMode defaults to true, a brand-new phone/small-iframe student is permanently locked in honor-system mode and never discovers the ROM emulator — the app's centerpiece. Even on desktop, the emulator is hidden behind a titlebar 'Options' click for every first-time user.

**Where:** ti84-trainer-v2/style.css:1081-1083 (.window-titlebar display:none at 600px), app.js:3074-3093 (renderPhysicalColumn has no toggle/options button), app.js:3095-3097 (early return skips narration-bar toggle), app.js:750 (default physicalMode: true)

**Suggested fix:** Add the mode toggle (and Options) to the physical panel header — it already has a status-pills area — and/or show a first-run chooser: 'Do you have a TI-84 with you? [Yes, use my calculator] [No, use the on-screen calculator]'. That doubles as onboarding.

**Effort:** S

**Verification verdict:** {'isReal': True, 'note': "Confirmed all four citations by reading ti84-trainer-v2/style.css and app.js, then adversarially searched for any alternate escape hatch. style.css:1081-1083 hides .window-titlebar (display:none) at <=600px with no later override; app.js:750/765 default physicalMode=true (and coerce non-false persisted values to true); renderPhysicalColumn (3074-3093) renders only a status pill + back/advance buttons; the early return at 3095-3097 means the 'Real TI-84' toggle (3171) only renders in emulator mode. Exhaustive data-action enumeration shows toggle-physical-mode exists only at 3171 (emulator-only) and 3250 (Options dialog), open-options-dialog only at 3347 (the hidden titlebar), optionsDialogOpen is set true nowhere else, and handleKeydown has no shortcut. The ROM/Firmware dialog reachable in physical mode has no mode toggle; standalone.html mirrors the same code. Minor correction: the finding's exit (b) is not actually an exit — the narration-bar toggle never renders in physical mode, so the Options dialog is the SOLE exit on any viewport, making the mobile dead-end even stricter than claimed. Severity high/student-ux is plausible: default-on physical mode + hidden titlebar means phone/small-iframe students can never reach the emulator."}

### 5. [high] (student-ux) 2-PropZTest and 2-PropZInt are fully authored but silently dropped from the trainer

two-propztest and two-propzint (Unit 6, each with complete 16-step walkthroughs and authored result screens) are filtered out because ti84-pattern-recognition-data.json has no patternSignatures/canonicalProblems/distractorSets for them — PROCEDURES keeps only procedures with a signature. Comparing two proportions is one of the most-tested AP inference procedures; students preparing with this tool will simply never see it, with no indication anything is missing. The known native-suite failure ('expects 27 but data has 29', verify-all-procedures.test.js:241) is the same half-finished integration.

**Where:** ti84-trainer-v2/app.js:206-208 (signature filter), ti84-procedures-data.json (two-propztest/two-propzint: 16 steps each, unit 6), ti84-pattern-recognition-data.json (27 signatures vs 29 procedures), ti84-trainer-v2/native/tests/verify-all-procedures.test.js:240-241

**Suggested fix:** Author patternSignatures, 2-3 canonical problems, and distractor sets for both procedures (mirror one-propztest/one-propzint entries), add them to VERIFICATION_FIELDS and computeExpected (stat-math has the building blocks), and update the native test to 29.

**Effort:** M

**Verification verdict:** {'isReal': True, 'note': "Confirmed end-to-end. (1) app.js:206-208 filters proceduresData.procedures by patternsData.patternSignatures[id] with no warning. (2) Parsed both JSONs: 29 procedures vs 27 patternSignatures/canonicalProblems/distractorSets; the exact missing IDs are two-propztest and two-propzint, with zero two-prop entries in any pattern-data section. (3) Both procedures are fully authored: unit 6, 16 steps each, wizard+result screens (two-propztest-wizard/-result, two-propzint-wizard/-result) all defined in proceduresData.screens. (4) No alternate student path: filteredProcedures() (app.js:1134) and PROCEDURE_BY_ID (app.js:209) both derive from the filtered PROCEDURES, so walkthrough, SRS scheduling, and pattern-recognition modes all exclude them; only generated/state-machine.js and the native test read the unfiltered list. (5) Reproduced the native test failure live: vitest in ti84-trainer-v2/native fails verify-all-procedures.test.js:241 with 'expected 29 to be 27'. Minor nuance: the native emulator suite DOES walk all 29 procedures (loop at line 109), so the native calculator side covers them — the gap is purely the trainer app + pattern-recognition data, exactly as the finding describes. Severity (student-ux/high) is plausible: 2-prop inference is a heavily tested AP Stats Unit 6 procedure and the drop is invisible to students."}

### 6. [high] (content) 2-PropZTest and 2-PropZInt exist in the data but are silently unreachable in the trainer

app.js builds its procedure list as `PROCEDURES = proceduresData.procedures.filter((procedure) => patternsData.patternSignatures[procedure.id])`. The two procedures added in commit 7fe8726 (two-propztest at ti84-procedures-data.json:5202, two-propzint at :5391) were never backfilled into ti84-pattern-recognition-data.json — they are missing from patternSignatures, canonicalProblems, AND distractorSets (verified programmatically: all three structures have exactly the 27 original keys). Net effect: students can never see, practice, or ledger-record the two-proportion procedures in the trainer, so Unit 6 shows only 2 of its 4 procedures (1-prop only). The classic AP confusion '1-prop vs 2-prop z' also cannot be drilled — the 22-pair confusionMatrix has one-sample-vs-two-sample pairs for means (t-test-stats vs two-samp-ttest) but none for proportions. The generated bundle confirms this ships to students: generated/data-procedures.js and standalone.html contain 'two-propztest' but generated/data-patterns.js does not. 2-prop z tests/intervals are core U6 content and a guaranteed exam topic.

**Where:** ti84-trainer-v2/app.js:206-208; ti84-pattern-recognition-data.json:7 (procedureCount: 27); ti84-procedures-data.json:5202,5391

**Suggested fix:** Author patternSignatures, ~3 canonicalProblems each, and distractorSets for two-propztest/two-propzint (mirror the one-propztest entries at the same dataType/parameterType axes, sampleStructure: 'two-sample'), add a one-propztest<->two-propztest confusion pair, bump meta.procedureCount, and re-run build.mjs. Alternatively (quick stopgap), make the walkthrough track use all procedures and only gate the pattern-question track on signature presence.

**Effort:** M

**Verification verdict:** {'isReal': True, 'note': 'Confirmed every citation. app.js:206-208 filters PROCEDURES by patternsData.patternSignatures[procedure.id], and that filtered list is the sole source for all trainer tracks (unit dropdown :473, procedure list :1134, walkthroughs, pattern questions, SRS memory) — no bypass path exists. ti84-procedures-data.json has 29 procedures (two-propztest at ~5202, two-propzint at ~5391, both unit 6); ti84-pattern-recognition-data.json has exactly 27 keys in patternSignatures, canonicalProblems, and distractorSets (meta.procedureCount: 27), missing precisely those two IDs. Commit 7fe8726 added the procedures + rebuilt data-procedures.js/standalone.html but never touched the patterns file. Shipped bundle verified: two-propztest present in generated/data-procedures.js and standalone.html, absent from generated/data-patterns.js. Unit 6 exposes only 2 of 4 procedures in the trainer. Confusion matrix has 22 pairs with means one-vs-two-sample pairs but no proportion equivalent. Severity is fair (high/Major): 2-prop z test/interval are core, exam-guaranteed U6 content. One mitigating nuance: the same commit shipped two-prop walkthroughs to the separate study-guide surface (data/ti84-procedures.js), so the content is not entirely absent from the product — but within the trainer (the drill/pattern-recognition surface) the finding is fully accurate, and the omission is silent (no build warning).'}

### 7. [high] (content) Wizard choice fields (Inpt Data/Stats, alternative hypothesis, invNorm Tail, Pooled) are scripted without the ENTER commit

On the real TI-84 family OS, a choice row's highlighted option is only committed by pressing ENTER — arrowing onto an option and then pressing DOWN leaves the previous selection active (TI's own guides say 'highlight Stats and press [enter]'). The step data scripts these rows as cursor moves only: t-test-stats step 5 is a single RIGHT with narration 'Switch Inpt from Data to Stats' (ti84-procedures-data.json:5637, repeated at :6021 and :6343 for the 2-samp variants), the alternative row is 'RIGHT ... until the desired relation is highlighted' followed directly by DOWN, and invNorm's Tail row is RIGHT [repeatable] then DOWN (:3549, :4863). This pattern recurs across roughly 10 wizard procedures (t-test stats/data, 2-samp t test/int Pooled row, 1/2-prop z tests, invnorm, invnorm-sampling). If the real OS requires ENTER, the trainer is rehearsing a keystroke habit that on a physical exam-day calculator silently runs the test with the default alternative (≠) or default Data mode — wrong p-value, no error shown. It would also make the ROM-backed mode visibly diverge: the WebCEmu firmware screen would still show 'Data' fields while the trainer narrates 'Enter the sample mean' (the walkthrough validates the student's keypress against step.key at app.js:2387; the emulator just renders). The native simulation suite passes because it implements the same selection-follows-cursor assumption — it cannot catch this class of divergence from real firmware.

**Where:** ti84-procedures-data.json:5637,6021,6343,3549,4863; ti84-trainer-v2/app.js:2387

**Suggested fix:** Verify on the real WebCEmu ROM (load T-Test, press RIGHT then DOWN, observe whether Stats fields appear). If ENTER is required as classic OS behavior indicates, insert an ENTER confirmation step after every choice-field selection across the affected procedures, update the native form-engine to match, and re-run the verification suite. This is exactly the kind of step the trainer exists to drill — forgetting ENTER on the alternative is a top real-world student error.

**Effort:** M

**Verification verdict:** {'isReal': True, 'note': "Confirmed. All cited lines verified: t-test-stats step 5 (ti84-procedures-data.json:5633-5648) scripts Inpt Data->Stats as a single RIGHT then DOWN, no ENTER — and its commonErrors actively mark ENTER as WRONG; same pattern at :6016-6033 (TInterval — finding mislabeled this one as a '2-samp variant', it is the one-sample TInterval; behavior claim still holds) and :6338-6351 (2-SampTTest). invNorm Tail rows at :3544-3559 and :4858-4873 are RIGHT [repeatable] then DOWN. Alternative rows (:5728-5743) and Pooled rows (:6471-6483, :6695-6707) follow the same no-ENTER pattern. app.js:2383-2387 validates the student keypress against step.key and forwards it verbatim; bridge.js:711 sends raw keypad coords to the emulator with no ENTER injection, so ROM-backed mode would genuinely diverge. native/form-engine.js implements the same selection-follows-cursor assumption (choice fields are cycle fields; explicit Inpt Data/Stats toggle on arrow at ~line 460), so the native suite cannot catch it — as claimed. Smoking gun: the repo's own 'select-alternative' micro-skill (json:2787-2804) INCLUDES a step-2 ENTER commit ('Confirm the highlighted alternative if the editor requires it'), proving internal inconsistency and author awareness. Real-OS requirement (highlight + ENTER to commit choice rows; arrowing away leaves prior selection) matches TI's documented dialog-editor behavior. Severity correction: arguably worse than stated — several steps' commonErrors feedback penalizes the CORRECT real-hardware keystroke (ENTER on a choice row), actively training the error. Caveat: empirical WebCEmu ROM confirmation not run here (ROM is user-supplied); the suggested ROM smoke test remains the right first fix step."}

### 8. [high] (architecture) Two-prop procedures shipped in data with zero native-module support — and the native suite has been permanently red since

Commit 7fe8726 added two-propztest and two-propzint to ti84-procedures-data.json (now 29 procedures) but the native calculator simulation was never extended: native/menu-tables.js MENU_TARGETS for stat-tests-menu maps indexes 1,3,4,7,9,10,12,13,15,16 only — no index 5 (2-PropZTest) or 11 (2-PropZInt); native/field-tables.js, form-engine.js, and stat-math.js have no two-prop entries at all (grep for 'twoProp|two-prop' in native/ matches only the two menu label strings). Consequences for a student in simplified/mock mode: pressing '6' on the TESTS menu advances the data-driven route but the native screen never enters the wizard, typed parameters go nowhere, and the result screen's '{value}' placeholders (two-propztest-result lines in the JSON) can never be filled because native getComputedValues() knows nothing about two-prop. app.js computeExpected (lines 584-708) also has no two-propztest/two-propzint cases and VERIFICATION_FIELDS (app.js:506) has no entries, so answer verification silently degrades. Meanwhile native/tests/verify-all-procedures.test.js:241 pins procedures.length to 27, so the suite fails on every run (confirmed: 359 passed | 1 failed), training everyone to ignore red — while its actual per-step assertions are soft ('expect(true).toBe(true)' at ~line 197 and 'Always pass — this is diagnostic' at line 231), so the two-prop gap produces no real failure signal anywhere.

**Where:** C:/Users/rober/Downloads/Projects/school/follow-alongs/ti84-trainer-v2/native/menu-tables.js:267-276; native/tests/verify-all-procedures.test.js:241; app.js:584

**Suggested fix:** Either (a) add native support: twoPropZTest/twoPropZInt to stat-math.js, wizard field tables, and MENU_TARGETS entries 5/11, plus computeExpected cases and VERIFICATION_FIELDS rows; or (b) explicitly mark the two procedures as 'physical/ROM mode only' in data and gate them out of mock mode. Either way, change the 27-pin to derive from the data (or update to 29) so the suite is green-by-default, and harden at least the final-screen assertion from diagnostic warn to a real expect for procedures that claim native support.

**Effort:** M

**Verification verdict:** {'isReal': True, 'note': "CONFIRMED with one impact correction. Verified by direct reads + live test run: (1) ti84-procedures-data.json has 29 procedures incl. two-propztest/two-propzint, added in 7fe8726 (2026-04-15); (2) native/menu-tables.js:266-277 stat-tests-menu table (actual name MENU_ACTIONS, not MENU_TARGETS as cited) maps indexes 1,3,4,7,9,10,12,13,15,16 only — no 5/11; field-tables.js/form-engine.js/stat-math.js have zero two-prop support; ti84-native.js:478 silently ignores unmapped menu selections, so '6'/'B' are dead keys; (3) ran the native suite: exactly '1 failed | 359 passed', failing at verify-all-procedures.test.js:241 ('expected 29 to be 27'); the pin predates the data commit (adeb806 2026-04-08), so the suite has been permanently red ~2 months; soft assertions confirmed at lines 196 and 231-232; (4) app.js computeExpected (584-707) and VERIFICATION_FIELDS (506-572) have no two-prop cases. CORRECTION to claimed impact: the described student scenario (walkthrough advances while native screen stalls, typed params lost, verification degrades) is NOT reachable — app.js:206-208 filters PROCEDURES by patternSignatures, and generated/data-patterns.js has exactly 27 keys (no two-prop), so the v2 trainer never offers these procedures at all. Real impact = (a) suite red-by-default since 4/15 masking all future regressions (verified, the severe part), (b) two-prop silently absent from the v2 trainer despite shipping in its data (content gap, plus dead TESTS-menu items 6/B in free navigation), (c) v1 ti84_trainer.html is stale (zero two-prop refs); only the study-guide static walkthroughs actually deliver two-prop. Severity stays high for the test-hygiene/architecture claim; the runtime-breakage detail should be downgraded from 'broken wizard' to 'silently unreachable'. Suggestion (derive the pin from data or update to 29; add patternSignatures + native support or gate two-prop out explicitly) is sound."}

### 9. [high] (architecture) standalone.html (the artifact students actually load) is rebuilt by a manual, unguarded step — and history shows it has already gone stale once

The Desk embeds ti84 via APP_REGISTRY -> 'ti84-trainer-v2/standalone.html' (ap_stats_roadmap_square_mode.html:12934) and start-here.html:956 links it too, while a dev iterates against index.html which loads live files. The bridge between them is `node ti84-trainer-v2/build.mjs` — there is no npm script for it (root package.json scripts = test/test:watch/test:coverage only), no CI/test that the committed standalone.html matches sources, and no pre-commit hook. Git history already contains the failure mode: commit 2cc6f7d ('fix: keypad hover jitter + letter key (A-H) menu selection') changed app.js without rebuilding standalone.html, so prod served the un-fixed trainer until the next rebuild. I verified the build IS deterministic (no timestamps; today's rebuild left generated/* byte-identical, and standalone.html currently contains app.js/bridge.js/style.css/all 9 native files verbatim), so a drift test is trivial: a root vitest test can re-run the template logic in-memory and string-compare against the committed standalone.html.

**Where:** C:/Users/rober/Downloads/Projects/school/follow-alongs/ti84-trainer-v2/build.mjs:135; package.json:6-10; git 2cc6f7d (app.js changed, standalone.html not)

**Suggested fix:** Add `"build:ti84": "node ti84-trainer-v2/build.mjs"` to package.json and a root test (tests/ti84-standalone-sync.test.js) that imports build.mjs's assembly (refactor it to export a buildStandalone() string function, with the CLI calling it) and asserts the committed standalone.html equals the fresh output. That converts 'forgot to rebuild' from silent prod staleness into a red test.

**Effort:** S

**Verification verdict:** {'isReal': True, 'note': "Confirmed all citations. (1) Desk APP_REGISTRY (ap_stats_roadmap_square_mode.html:12934) and start-here.html:956 both point students at ti84-trainer-v2/standalone.html, while index.html loads live app.js/bridge.js/native/* — so standalone.html is the prod artifact. (2) build.mjs:135 writes it; the only invocation path is manual `node ti84-trainer-v2/build.mjs`: package.json scripts are test-only (lines 6-10), no ti84 package.json, no husky/non-sample git hooks, no .github/workflows, and the lone test mentioning standalone.html (tests/phase4-structure.test.js:245) is just a link-string marker check, not a freshness guard. (3) The historical staleness is real and I verified it more precisely than the finding: at 2cc6f7d, app.js contained the `physicalKey` fix but the committed standalone.html did NOT (string-checked both blobs at that commit); the fix only reached standalone.html at 92b6658, 3 commits later. (4) Determinism verified: I ran the build against a clean tree and git status stayed empty — byte-identical output, so the suggested drift test is feasible. One severity correction: the observed staleness window was same-day (2cc6f7d → 92b6658, both 2026-04-08), so the single known incident was brief — 'high' is at the generous end (medium-high is defensible), but the structural claim (manual, unguarded, silently-failing rebuild step for the student-facing artifact) is fully accurate. Minor caveat for the suggested test: guard against CRLF-normalizing checkouts causing false drift failures (compare normalized or pin .gitattributes)."}

### 10. [high] (integration) No end-to-end confirmation the trainer ledger write is live; every failure mode is silent (migration 0016 is USER-RUN and unverified)

The wiring chain is correct in shape — recordTrainerAttempt builds itemId 'TI84-<kebab-id>' (procedure ids are already [a-z0-9-], so the charset matches the ledger/fetchPrior prefix contract), unit 'U<n>' (matches the DN2b 'U4' convention unitKeyOf expects), score quality/5, attempt 1 — and nothing can throw into the practice loop (record() never rejects, guards use optional chaining). But the write is fire-and-forget with zero feedback at every layer: app.js ignores record()'s resolved {ok}, gradebook-client only console.warns on server ok:false, and if the teacher has not run migration 0016 the server 503s 'source trainer not provisioned' on every single write — 100% of practice evidence dropped with no student-visible, teacher-visible, or test-visible signal. The sibling commit 8e97f83 proves the deploy-day session DID hit a not-provisioned trainer_state (PGRST205 observed live), so 0016's run status is genuinely uncertain. There is also no client-side test covering recordTrainerAttempt (root tests/ has no reference to it), so a refactor could kill it silently forever — exactly the bug class f18c2ea itself fixed twice ('silently dead since it shipped; live DB confirms 0 rows ever').

**Where:** ti84-trainer-v2/app.js:2163-2194; gradebook-client.js:126-132; roster-server/ledger.js:74-77; roster-server/migrations/0016_item_ledger_trainer_source.sql:13-14

**Suggested fix:** Do a one-time live smoke: sign in on the deployed site, complete one procedure, confirm a source='trainer' row via GET /teacher/student/:sid/recent (or SQL). Then add a tiny UI hook: recordTrainerAttempt already gets a Promise that resolves {ok} — .then() it into a small 'Saved to your gradebook / Not saved' line on the session-result card. Add one jsdom test pinning the record() payload shape.

**Effort:** S

**Verification verdict:** {'isReal': True, 'note': 'Confirmed. All four citations hold exactly: app.js:2163-2194 discards record()\'s promise (fire-and-forget, line 2181); gradebook-client.js:126-132 only console.warns on server ok:false; ledger.js:74-77 returns 503 "source \'trainer\' not provisioned" on check_violation; migration 0016 is explicitly USER-RUN and pre-migration Postgres rejects every trainer row. Supporting claims verified: commit 8e97f83 (2026-06-10) explicitly records a live post-deploy PGRST205 not-provisioned trainer_state (proving trainer migrations were unrun at deploy; 0016 has no run-confirmation anywhere — no .md mentions it, memory lists 0011-0015 as user-run but not 0016); f18c2ea\'s message verbatim contains the "silently dead since it shipped; live DB confirms 0 rows ever" precedent; recordTrainerAttempt has zero test coverage (only link-presence checks mention the trainer in root tests/). The student-visible no-identity nudge never fires on this path because the trainer skips record() when signed out, so the 503/network failure path is console.warn-only — invisible to students and teachers. Minor severity correction: 0016\'s header documents trainer rows as grade-inert by omission (lesson-grade.js does not consume them), so the silent drop loses practice evidence but cannot affect any current grade — severity arguably medium-high rather than high, but the mechanism and impact are real. Suggested smoke test + {ok}-driven UI hook + payload-shape jsdom test are all apt.'}

### 11. [medium] (student-ux) First-run screen is developer jargon with zero onboarding

A new student's first screen says: 'Track 1 asks for the correct procedure. Track 2 validates each key with the native state machine while the calculator panel boots the real TI-84 firmware when it is cached or configured.' — plus buttons labeled 'Start next item' and 'Firmware', and a banner about 'firmware… cached or configured'. Nothing explains the two tracks, guided vs recall, the spaced-review scheduling, or the physical-vs-emulator choice. Mid-session copy leans on the internal 'clutch' metaphor ('The clutch is disengaged, so every key goes straight to the calculator') which means nothing to a 16-year-old. The pedagogy is sophisticated but never sold to the student, so the behavior (branching into a walkthrough of the WRONG procedure, demotions, due dates) will feel arbitrary.

**Where:** ti84-trainer-v2/app.js:2814-2832 (start panel copy + Firmware button), app.js:485 (initial banner), app.js:2643/2685/2730 ('clutch is disengaged' student-facing copy)

**Suggested fix:** Rewrite the start panel in student language ('1) Pick the right calculator procedure for the problem. 2) We walk you through the keys. Reviews come back on a schedule, like flashcards.') with a one-time 3-step intro; rename 'clutch disengaged' to 'free-play: keys go straight to the calculator'; move 'Firmware' out of the start panel (it already lives in Options).

**Effort:** S

**Verification verdict:** {'isReal': True, 'note': "Verified all citations verbatim. renderStartPanel (app.js:2814-2832) is the actual first-run screen (renderProblemColumn falls through to it when no question/walkthrough exists) and contains exactly the quoted developer-jargon copy plus 'Start next item'/'Firmware' buttons; app.js:485 initial banner matches; 'The clutch is disengaged' student-facing copy confirmed at 2643 (Data Setup), 2685 and 2730 (Result Review) — 'clutch' appears 74x and is never defined for students. Grep for onboarding/tutorial/welcome/help flows found none; the only 'intro' is branchIntro (the branch panel itself). Spaced review surfaces only as bare 'Due/New/Mastery' dashboard counts, never explained. Trainer is student-facing (linked from index.html, start-here.html, roadmap). One correction: 'nothing explains' the branching is slightly overstated — the question panel warns 'A wrong choice branches into a full guided walkthrough before this question returns' (line 2591), and terse one-liners exist at 2984/3187 plus guided/recall promotion-demotion result strings (2135/2152). These are single-sentence jargon-register notes, not onboarding, so the medium/student-ux severity stands; suggestion (student-language rewrite, rename clutch copy, move Firmware button) remains apt — note the Firmware button on the start panel duplicates the ROM dialog also reachable elsewhere (open-rom-dialog action)."}

### 12. [medium] (student-ux) 'Check Your Answer' reveals the expected value on the first miss — and offline/mock students are forced to use that leak

The result-review gate ('Finish review' disabled until verified) is meant to force students to read t/p/CI values off the calculator. But any failed check prints 'Expected: <value>' next to the field, so clicking Check with blank fields hands over the answers to copy in. Worse, when the ROM isn't available (download blocked/failed -> mock mode), the mock LCD never displays computed results at all: authored result screens store their content in 'lines', which screenLinesForMock never reads (it only checks items/fields/layout.content), so the screen shows just the procedure name. Offline students literally cannot read the values anywhere except the 'Expected:' leak — the verification step becomes copy-the-answer busywork.

**Where:** ti84-trainer-v2/app.js:2700-2705 (Expected reveal on wrong), app.js:3496-3503 (finish gated on verified), app.js:3396-3411 (screenLinesForMock skips screen.lines), ti84-procedures-data.json one-var-stats-result-page1 / t-test-result (result content in 'lines' with {value} placeholders)

**Suggested fix:** Show 'Expected' only after 2-3 failed checks (or show which digit diverges). In mock mode, render the result screen from native getComputedValues() (the values already exist — computeExpected prefers them) by substituting {value} placeholders in screen.lines, so offline students can genuinely read the answer off the LCD.

**Effort:** M

**Verification verdict:** {'isReal': True, 'note': "Confirmed all three cited behaviors. (1) app.js:2300-2344 + 2705: checkAnswerVerification stores `expected` unconditionally; one Check click with blank fields renders 'Expected: <value>' for every field — first-miss full-answer leak is real. (2) app.js:3497-3503: Finish review hard-gated on answerVerified for all procedures in VERIFICATION_FIELDS. (3) app.js:3396-3411: screenLinesForMock reads only items/fields/layout.content/step.narration, never screen.lines; result screens (one-var-stats-result-page1 json:646-658, t-test-result json:1116-1127) keep content solely in `lines`, so the mock LCD (bridge.js drawMockFrame/setMockLines, used as the offline fallback when the Supabase ROM fetch fails) shows no computed values — offline students can only complete the gate by copying the Expected leak. Refutation attempt failed but found a correction: the native fast-path in computeExpected (app.js:587 `app.backend?.getComputedValues?.()`) is DEAD CODE — backend is stored only as `app.bridge` (app.js:3645), `app.backend` is never assigned — so Expected always comes from the TI84StatMath recompute fallback (which the code admits may diverge from the ROM). This strengthens the finding; the suggestion's claim that 'computeExpected prefers them' describes intent, not behavior — the fix should also rebind that lookup to app.bridge. Cosmetic corrections: data file is repo-root ti84-procedures-data.json baked into ti84-trainer-v2/generated/data-procedures.js (not ti84-trainer-v2/ti84-procedures-data.json), and the mock LCD shows procedure name + 'Screen: <id>' + step narration rather than just the name. Severity medium/student-ux stands."}

### 13. [medium] (student-ux) 'Due' counter counts items the scheduler never picks, and grows the moment you finish something

The dashboard dueCount counts a procedure as due if track1 OR track2 is due, but nextQuestionProcedureId schedules ONLY by track1 (due -> fresh -> weakest). applyTrack2Outcome on a guided pass sets track2.nextReview = today, so every guided completion instantly re-enters the Due count — a student finishes their first walkthrough and watches 'Due' go from 0 to 1 as a reward. Since track2 dues are never drained directly, the number can stay nonzero indefinitely. The one progress affordance students have is misleading, undermining the 'work the queue to zero' motivation loop that makes SRS apps sticky.

**Where:** ti84-trainer-v2/app.js:1193-1196 (dueCount: track1 || track2), app.js:1232-1257 (scheduler uses track1 only), app.js:2127-2128 (guided pass sets track2.nextReview = today)

**Suggested fix:** Either make dueCount mirror the scheduler (track1-due only, plus fresh as 'New') or make the scheduler consume track2 dues too. For the guided->recall promotion, set track2.nextReview to tomorrow so finishing an item visibly clears it for today.

**Effort:** S

**Verification verdict:** {'isReal': True, 'note': "Confirmed all three citations in ti84-trainer-v2/app.js. dueCount (1193-1196) counts isDue(track1) || isDue(track2); nextQuestionProcedureId (1232-1257) schedules by track1 only (due -> fresh -> weakest), and no other code path selects by track2 due (startWalkthrough callers: question 2033, branch 2062, restart 2466). Guided completion (2125-2132) sets track2.nextReview = today, while applyTrack1Outcome/sm2 (2090-2118) always pushes track1.nextReview >= tomorrow, so finishing the first walkthrough verifiably flips Due from 0 to 1 on the immediately re-rendered dashboard (3199-3201), and Due climbs with each first-time item completed in a session. One correction: 'stays nonzero indefinitely' is overstated — track2 dues drain indirectly the next day (track1 interval=1 makes the item most-overdue, and any recall completion re-runs sm2 on track2), or same-day via the weakest fallback once fresh items run out; a shaky recall (errors>=3 or hints>=2, 2146-2149) does re-pin track2 due to today. Severity medium/student-ux stands: within a session the counter cannot be worked to zero and moves the wrong way on completion."}

### 14. [medium] (student-ux) Pattern-recognition (Track 1) is brute-forceable: cancel the branch and guess again, penalty-free

Choosing a wrong procedure opens the branch intro, but 'Back to choices' returns to the question with no record: branchCount/branchHistory only update when a branch walkthrough is COMPLETED, and track1 quality is computed solely from branchCount. So a student can click wrong choices repeatedly, cancel each branch, and once they hit the right one receive quality 5 ('perfect first-try'). The (genuinely good) contrast text in the branch intro even tells them why their choice was wrong, making the next guess trivial. Track 1 mastery and the SM-2 schedule are therefore gameable by the path of least resistance.

**Where:** ti84-trainer-v2/app.js:3480-3484 (cancel-branch resets with no penalty), app.js:2228-2233 (branch bookkeeping only on completion), app.js:2249-2251 + 2197-2211 (track1 quality from branchCount only)

**Suggested fix:** Track wrongChoices on the question object in handleChoice (regardless of branch completion) and feed it into track1QualityForBranches; keep the branch walkthrough optional but make a cancelled wrong guess cost the same quality as one branch.

**Effort:** S

**Verification verdict:** {'isReal': True, 'note': "Confirmed all citations. handleChoice (app.js:3428-3448) records nothing on a wrong guess — it flashes, names the wrong procedure in the banner, and opens the branch intro; cancel-branch (3480-3484) just nulls app.branchIntro with no bookkeeping. branchCount/branchHistory/remainingChoices update only in completeWalkthrough when sourceKind==='branch' (2225-2233), i.e., only after a fully completed branch walkthrough. Track 1 quality on finishing the correct walkthrough is computed solely from sourceQuestion.branchCount (2249-2251 via track1QualityForBranches 2197-2211): branchCount 0 → quality 5 → sm2() grows interval/easeFactor. So guess-cancel-repeat yields 'perfect first-try' quality 5 despite N wrong choices, and the contrast text (openBranchIntro 2047-2053, contrastFor 1259-1280) plus the 'Not quite. That leads to X' banner make each subsequent guess easier. Minor severity refinement: impact slightly exceeds local SM-2 — the quality 5 also flows to the gradebook via recordTrainerAttempt (score: quality/5, line 2192). Also note cancelled wrong choices remain in remainingChoices (not removed), which doesn't blunt the exploit. The exploit is the path of least resistance (cancelling skips an entire extra walkthrough). Suggested fix is consistent with the code structure."}

### 15. [medium] (student-ux) No per-procedure progress view, no targeted practice, no growth feedback

The entire progress surface is three aggregate numbers (Due/New/Mastery %) computed from an opaque heuristic. A student cannot see which of the 27 procedures they've learned, which are weak, or when each comes back; they cannot say 'I keep messing up Two-Sample T-Test, let me drill it' — the scheduler alone decides (filterUnit narrows to a unit at best, and that select sits at the very bottom of the page). There are no streaks, no session goal, and no 'all caught up for today' state (the scheduler falls through to 'weakest' forever, so practice never feels finished). Sibling apps in this ecosystem (Desk, worksheets, Blooket) all show concrete progress; this one shows the least despite collecting the richest data (per-procedure SM-2 records with dates, qualities, errors).

**Where:** ti84-trainer-v2/app.js:3193-3222 (dashboard = 3 numbers + unit select), app.js:1252-1256 (weakest fallback = infinite queue, no done state), app.js:1161-1189 (opaque mastery heuristic), app.js:2835-2853 (no procedure-list view in any column state)

**Suggested fix:** Add a 'My procedures' list (procedure x unit, mastery pill, next-review date, 'Practice this' button that calls startWalkthrough directly). Add an explicit caught-up state ('Nothing due — come back tomorrow, or practice your weakest: ...'). Even a per-unit progress bar would give the growth signal that keeps summer practice going.

**Effort:** L

**Verification verdict:** {'isReal': True, 'note': "Verified all 4 citations in ti84-trainer-v2/app.js. (1) renderDashboard (3193-3222) is exactly 3 aggregate cards (Due/New/Mastery%) + unit select, and render() places it as the LAST child of <main> (line 3359) — bottom-of-page claim is literal. (2) nextQuestionProcedureId (1232-1257) cascades due→fresh→weakest and always returns an id; grep confirms zero occurrences of streak/caught-up/session-goal/'nothing due' strings; all sessionResult states end with 'Next item' — no done state exists. (3) Mastery (1161-1189) is a magic-constant heuristic (0.22 + reps*0.18 + min(interval,21)*0.02 + min(exposures,4)*0.04; guided flat 0.16/0.34) averaged into one number. (4) renderProblemColumn (2835-2853) has 5 states, none a procedure list; audited all 31 data-action handlers — 'choose-procedure' (2579) is the Track-1 MC answer button, not a drill picker, so there is no targeted-practice entry point. Per-procedure records do carry repetitions/interval/nextReview/lastErrors/lastHints, none surfaced to the student. Finding fully confirmed at stated severity (medium, student-ux feature gap, not a bug). Minor nuance: dashboard is additionally hidden on mobile during walkthroughs (mobile-hidden), which slightly strengthens the finding."}

### 16. [medium] (student-ux) Click-only UI: no physical-keyboard input, and every render destroys focus

bindEvents listens for click/change/input plus Enter only inside answer inputs — there is no keyboard mapping for the on-screen keypad, so entering a 12-value list manually means mouse-clicking digit keys one at a time (auto-fill exists, but defeats the practice purpose of data entry). For keyboard/switch users it's worse: render() rebuilds root.innerHTML on every keypress, destroying focus, so Tab-to-key navigation resets to the top of the document after every single press — the trainer is effectively unusable without a mouse/touchscreen. There is also no aria-live region, so the banner (the only feedback channel) is silent for screen readers.

**Where:** ti84-trainer-v2/app.js:3631-3642 (bindEvents: no keypad keydown), app.js:3339-3374 (render replaces root.innerHTML on every state change; only the canvas is preserved), app.js:3350-3352 (banner without aria-live)

**Suggested fix:** Add a document-level keydown map (digits, Enter, arrows, Backspace->DEL, Esc->CLEAR -> pressButton) — this is a big quality-of-life win for everyone, not just a11y. Add aria-live='polite' to the banner span. Longer term, patch the DOM instead of full innerHTML replacement, or restore focus to the last-pressed key after render.

**Effort:** M

**Verification verdict:** {'isReal': True, 'note': "Confirmed all three citations in ti84-trainer-v2/app.js. (1) bindEvents (3631-3642) registers only click/change/input/keydown; handleKeydown (3622-3629) handles only Enter inside .answer-input — grep confirms no other key listeners and no keyboard map for the on-screen keypad. (2) render() (3339-3374) replaces root.innerHTML wholesale, preserving only the canvas; every keypad press path in pressButton (lines 2371, 2397, 2429) ends in render(), and no .focus()/activeElement code exists anywhere to restore focus — so keyboard activation of a keypad button (Enter/Space fires click) drops focus to body after every press, making Tab-navigation impractical. (3) Banner span (3350-3352) has no aria-live; grep finds zero aria-live/role=status in the whole trainer. Minor correction to the description: typing inside answer inputs does NOT trigger render (handleInput at 3603-3620 patches state in place via resetAnswerVerificationUi at 2281, which only toggles classList/disabled), so 'every keypress' is slightly overstated — focus destruction applies to keypad/action button activations, which is the workflow the finding targets. Severity medium is appropriate; suggestion (document-level keydown map + aria-live=polite + focus restore) is sound."}

### 17. [medium] (content) Stale '27 procedures' count pins in the native suite and pattern meta — the tripwire fired but points at the wrong fix

tests/verify-all-procedures.test.js:241 asserts `proceduresData.procedures.length).toBe(27)` and fails (data has 29). I re-ran the suite: 83/84 pass — the per-procedure walkthrough loop at line 109 is data-driven, so both new procedures (two-propztest, two-propzint) ARE walked through the native module and pass all their step/screen tests; their step data is structurally trustworthy (correct '6' menu shortcut for 2-PropZTest, correct ALPHA+APPS for the B: item per the project's own ceda16e letter-key convention). Only the count pin is stale. ti84-pattern-recognition-data.json:7 carries the same stale `procedureCount: 27`. The danger is someone 'fixing' this by bumping 27→29: the count mismatch is currently the only signal that procedures were added without pattern-data backfill (the high-severity filter bug above). Minor related gap: SAMPLE_VALUES (test lines 48-60) lacks 'x1'/'x2' tokens, so the 2-prop walkthroughs type the fallback '1' — works, but worth adding for fidelity.

**Where:** ti84-trainer-v2/native/tests/verify-all-procedures.test.js:241,109,48-60; ti84-pattern-recognition-data.json:7

**Suggested fix:** Replace the hardcoded count with two real invariants: (1) every procedure id in ti84-procedures-data.json has an entry in patternSignatures/canonicalProblems/distractorSets (this assertion would have caught the unreachable-procedures bug at add time), and (2) procedures.length === Object.keys(patternSignatures).length. Add x1/x2 to SAMPLE_VALUES while there.

**Effort:** S

**Verification verdict:** {'isReal': True, 'note': 'CONFIRMED by running the suite and reading the cited code. (1) verify-all-procedures.test.js:241 does pin `proceduresData.procedures.length).toBe(27)` while ti84-procedures-data.json (repo root) has 29 procedures incl. two-propztest/two-propzint — I ran `npx vitest run` from ti84-trainer-v2/native and got exactly 83 passed / 1 failed, the failure being \'expected 29 to be 27\' at line 241. (2) The walkthrough loop at line 109 is data-driven, so both new procedures are exercised and their hard step/screen tests pass. (3) SAMPLE_VALUES (lines 48-60) has n1/n2/x̄1/x̄2 but no \'x1\'/\'x2\'; the two-prop step data uses {x1}/{x2}, so they type the \'1\' fallback — gap confirmed. (4) ti84-pattern-recognition-data.json line 7 carries stale `"procedureCount": 27`, and patternSignatures has exactly 27 keys with two-propztest/two-propzint MISSING — corroborating that the count mismatch doubles as the only tripwire for missing pattern backfill; grep found no other test asserting pattern-data↔procedures coverage, so naively bumping 27→29 would indeed silence that signal. Two corrections: (a) the pattern JSON lives at the follow-alongs repo root, not under ti84-trainer-v2/ as the citation path implies (content is otherwise as cited); (b) \'step data is structurally trustworthy\' is slightly overstated — the always-pass diagnostic test logs DISCREPANCY warnings for BOTH new procedures (two-propztest ends on menu/stat-menu, two-propzint on wizard/linreg-tint-wizard instead of result/home/graph), so the new step data (or native menu handling of the \'6\' shortcut / ALPHA+APPS path) may still need fixes beyond the count pin. Severity content/medium/S is appropriate; the suggested invariant-based replacement assertions are sound.'}

### 18. [medium] (content) AP coverage gaps: Unit 3 has zero procedures; no random-number generation, no discrete-RV mean/SD via FreqList, no invT

Per-unit procedure counts from the data: U1:5 (1-Var Stats, histogram, boxplot, normalcdf, invNorm), U2:3 (LinReg, scatter, residual plot), U3:0, U4:4 (binom/geomet pdf+cdf), U5:2 (sampling normalcdf/invNorm), U6:4 (but only 2 reachable — see filter finding), U7:6, U8:3, U9:2. Inference coverage (U6-U9) is strong. Missing vs what an AP student actually uses: (1) randInt(/RandSeed (MATH>PROB) — Unit 3 random sampling/assignment and U4.3/4.5 simulation problems; U3 is the only unit with literally nothing, and U3 is inside the SUMMER26 U1-U5 prep window; (2) mean/SD of a discrete random variable via 1-Var Stats with X in L1 and P(X) as FreqList L2 — the core U4.7-4.8 calculator skill; ironically the one-var-stats wizard screen already models the FreqList field (the existing procedure just DOWNs past it), so this is a cheap variant procedure; (3) invT (DISTR 4:invT) for t* critical values in U7 interval construction. Honorable mentions, lower value: paired t on differences (L3=L1-L2, only implicitly covered by t-test-data), and tcdf/χ²cdf for computing p-values from given statistics. 2-SampZTest/Int and normalpdf are reasonably omitted for AP.

**Where:** ti84-procedures-data.json (29 procedures, units enumerated via script; FreqList field already at one-var-stats-wizard def, lines 625-644)

**Suggested fix:** Author 3 new procedures in priority order: (a) discrete-RV 1-Var Stats with FreqList (U4 — reuses existing screens, smallest lift, in the SUMMER26 window), (b) randInt with seed-setting (U3 — gives the unit its first procedure and matches simulation FRQs), (c) invT (U7). Each needs pattern-data entries too, given the app.js filter.

**Effort:** L

**Verification verdict:** {'isReal': True, 'note': 'Confirmed against C:/Users/rober/Downloads/Projects/school/follow-alongs/ti84-procedures-data.json (29 procedures). Per-unit counts match exactly: U1:5 (one-var-stats, histogram, modified-boxplot, normalcdf, invnorm), U2:3, U3:0 (no unit-3 procedures exist at all), U4:4 (binom/geomet pdf+cdf), U5:2, U6:4, U7:6, U8:3, U9:2. (1) randInt/RandSeed: zero occurrences anywhere in the JSON — confirmed absent. (2) invT: appears only once, at line 531 as menu item "4:invT(" in the DISTR menu screen — modeled in the menu but no procedure uses it. (3) Discrete-RV FreqList: the one-var-stats-wizard screen does define a FreqList field (label at line 635, within the cited 625-644 range), and the one-var-stats procedure\'s step 3 is DOWN past "FreqList: 1" ("Move to FreqList") — exactly the "DOWNs past it" claim, so a FreqList variant procedure would indeed reuse existing screens (cheap lift). The cross-referenced reachability claim also holds: app.js line 207 filters procedures by patternsData.patternSignatures[procedure.id], and ti84-pattern-recognition-data.json has 27 signatures — two-propztest and two-propzint lack them (U6 only 2 reachable), so the suggestion\'s "each new procedure needs pattern-data entries too" is accurate. Impact is plausible: U3 (random sampling/assignment, simulation) sits inside the SUMMER26 U1-U5 prep window per project memory, and randInt, 1-Var Stats with P(X) as FreqList (U4.7-4.8), and invT (U7 t*) are standard AP-exam calculator skills. Severity content/medium is appropriate; no correction needed.'}

### 19. [medium] (stability) IndexedDB failure aborts the ROM download that does not need IndexedDB

bridge.js init() wraps the cache read AND the Supabase download in one try/catch. If openDatabase rejects (bridge.js:97-98 'IndexedDB is not available.' — private browsing modes, locked-down Chromebooks, storage-blocked contexts), the rejection from readRecord at line 602 jumps straight to the catch at line 633 and the trainer drops into 'simplified mode' — even though downloadRomFromSupabase + bootRecord only need bytes in memory, and the later writeRecord failure is already individually caught and tolerated (line 627-630). Students whose browser blocks IDB but has a working network lose the real emulator for no reason. There IS a user-visible state (no hang): 'Offline Mode' pill + 'Could not load calculator firmware. Using simplified mode.' — but it fires prematurely.

**Where:** ti84-trainer-v2/bridge.js:599-645 (readRecord at 602 unprotected; contrast deleteRecord 608-614 and writeRecord 626-630 which are individually caught)

**Suggested fix:** Wrap the readRecord call in its own try/catch (treat any IDB read error as cache-miss) so the Supabase download still runs; bootRecord already works from an in-memory record.

**Effort:** S

**Verification verdict:** {'isReal': True, 'note': "Confirmed by reading ti84-trainer-v2/bridge.js. openDatabase rejects when window.indexedDB is absent (lines 97-99); readRecord (117-129) has no internal catch, so the rejection from line 602 propagates to init()'s single catch at line 633, which calls showFallback('offline', 'Could not load calculator firmware. Using simplified mode.') — skipping downloadRomFromSupabase (line 620) entirely. The contrast cited is accurate: deleteRecord (610-614) and writeRecord (626-630) ARE individually caught and tolerated, proving IDB failures were intended to be non-fatal. downloadRomFromSupabase builds the record purely in memory from fetch (lines 472-479) and bootRecord only needs in-memory bytes (module.FS.writeFile, line 576) — no IDB dependency, so the suggested fix (treat read error as cache-miss) is sound; the writeRecord failure afterward is already tolerated. Impact path is live: init exported (line 799), called from app.js:62/3658; 'Offline Mode' pill at app.js:3336. Severity medium/stability is fair for locked-down Chromebooks/private browsing. One correction: standalone.html embeds a duplicate inline copy of the bridge (mirrored showFallbacks at ~3373-3534, init call at 8180/11776), so the fix must be applied in BOTH bridge.js and standalone.html."}

### 20. [medium] (stability) Corrupt cached ROM is never auto-invalidated — boot failure loops into simplified mode on every load

isCacheCurrent (bridge.js:384-386) checks only record.version === cacheVersion, no size/integrity check. If the cached bytes are bad (bad manual ROM file via selectRomFile, or a truncated download that resolved cleanly — downloadRomFromSupabase never validates receivedBytes against content-length), bootRecord's catch (bridge.js:584-596) shows the offline fallback but does NOT delete the record. Every subsequent page load re-reads the same version-current corrupt record and fails again, permanently. Recovery requires the student to discover Options → Firmware → 'Clear cached firmware' — not something a 9th grader will find.

**Where:** ti84-trainer-v2/bridge.js:584-596 (catch shows fallback, no deleteRecord), 384-386 (version-only check), 472-479 (no content-length validation on the assembled download)

**Suggested fix:** In bootRecord's catch, deleteRecord(ROM_CONFIG.cacheKey) so the next load re-downloads; optionally reject downloads where receivedBytes !== content-length before caching.

**Effort:** S

**Verification verdict:** {'isReal': True, 'note': "Confirmed all three citations in ti84-trainer-v2/bridge.js (and its duplicate in standalone.html ~3036-3647). isCacheCurrent (384-386) is version-string-only; bootRecord's catch (584-596) shows the offline fallback and never deletes the record; init (604-606) returns bootRecord(record) immediately when the cache is version-current, so a corrupt version-current record re-fails on every load with no auto-invalidation — the permanent loop is real. downloadRomFromSupabase parses content-length only for progress and caches receivedBytes with no length check (472-479). Recovery really is buried: Options -> Firmware -> Clear cached firmware (app.js:3303-3305), and clearStoredRom requires a manual reload to re-download. Corrections to the finding: (1) the 'bad manual ROM via selectRomFile' vector is dev-only — supportsManualRomSelection() is false in the shipped config (Supabase URL hard-coded), so students never see the file input; (2) common truncations reject reader.read() before writeRecord, so they do NOT poison the cache — only a cleanly-short stream (rare) or a bad/replaced ROM.rom in the Supabase bucket does, the latter being the most plausible production trigger and made worse by version-only caching (fixed bucket file is never re-fetched until cacheVersion is bumped); (3) the suggested 'deleteRecord in bootRecord catch' is too broad — that catch also covers factory() instantiation which fetches WebCEmu.wasm over the network, so a transient offline boot would nuke a good ROM cache; prefer receivedBytes===content-length validation before caching plus invalidating only on errors after FS.writeFile/callMain. Severity medium is defensible given permanence and obscure recovery, though trigger likelihood is lower than the finding implies (medium-low)."}

### 21. [medium] (stability) savePersisted/saveListMemory call localStorage.setItem unguarded — a quota/private-mode throw skips render() and strands the UI

loadPersisted and loadListMemory are try/caught, but savePersisted (app.js:773-776) and saveListMemory (app.js:793-795) are not. In completeWalkthrough the call order is: mutate SM-2 records → null app.walkthrough → set sessionResult → savePersisted() → render() (app.js:2244-2245 and 2263-2264). If setItem throws (quota exceeded, Safari private mode), the exception propagates out before render() — the screen still shows the finished walkthrough while the state behind it is already cleared, and clicking around produces confusing no-ops until something else re-renders. It also recurs on every completion, so the student's SM-2 progress is silently never persisted with zero feedback. Same unguarded pattern in handleChange unit-filter (app.js:3572).

**Where:** ti84-trainer-v2/app.js:773-776, 793-795, 2244-2245, 2263-2264

**Suggested fix:** Wrap both save functions in try/catch (mirror loadPersisted), console.warn on failure, and optionally set app.banner once ('progress can't be saved in this browser') so completion flow always reaches render().

**Effort:** S

**Verification verdict:** {'isReal': True, 'note': 'Confirmed against ti84-trainer-v2/app.js. (1) savePersisted (773-776) and saveListMemory (793-795) call window.localStorage.setItem with no try/catch, while the matching loaders (745-771, 778-791) are guarded — the asymmetry is exactly as cited. (2) completeWalkthrough (2213-2265): both exit paths mutate state first (applyTrack1Outcome/applyTrack2Outcome write SM-2 records into app.persisted, app.walkthrough=null, app.question=null, sessionResult/banner set), then savePersisted() at 2244/2263, then render() at 2245/2264 — a throw skips render(). (3) No mitigation anywhere up the call chain: handleClick case \'finish-review\' (3503) has no try/catch, and the listener is `void handleClick(event)` (3633), so the async rejection is silently swallowed with no recovery render and no global error/unhandledrejection handler. (4) The stranded-UI claim holds concretely: after the throw the screen still shows the walkthrough, but re-clicking "Finish review" hits currentProcedure()===null (1341-1343) then completeWalkthrough\'s early return on null walkthrough (2216-2218) with no render — the visible primary button becomes a literal no-op until some other action re-renders. (5) Recurrence/silent-loss claim holds: every completion re-mutates records then throws at the same setItem, so SM-2 progress is never persisted with zero feedback. (6) handleChange unit-filter citation (3570-3574) also holds; there is one additional uncited instance at app.js:2478. Minor corrections: the \'Safari private mode\' trigger is dated (Safari 11+ no longer throws on setItem in private mode), but quota-exceeded and cookies-blocked SecurityError (where the localStorage getter itself throws — loadPersisted catches it, savePersisted hits it on every save) remain real triggers, so medium severity is reasonable, arguably low-medium given the unusual browser states required. Suggested fix (mirror loadPersisted\'s try/catch + one-time banner) is appropriate.'}

### 22. [medium] (stability) Native suite is permanently red: stale '27 procedures' pin vs 29 in data

Confirmed by running the suites: native suite = 359 passed / 1 failed (360 tests, 6 files); the only failure is verify-all-procedures.test.js:241 `expect(proceduresData.procedures.length).toBe(27)` while ti84-procedures-data.json has 29 procedures (verified by direct count). A permanently failing suite trains everyone to ignore red, so a real regression in the other 359 tests can hide behind 'oh, that's the known failure'. Root suites are green: tests/ti84-state-machine.test.js (54) + tests/ti84-plot.test.js (53) = 107/107 passed. build.mjs runs clean (exit 0) and its output is byte-identical to what's committed (git status clean after a rebuild).

**Where:** ti84-trainer-v2/native/tests/verify-all-procedures.test.js:241; ti84-procedures-data.json (29 procedures, units all ints)

**Suggested fix:** Replace the hard-coded 27 with the real intent: either pin 29 with a comment, or better, derive it (e.g., compare proceduresData.procedures.length against the list of procedure IDs the suite actually exercises) so adding a procedure without a test fails for the right reason.

**Effort:** S

**Verification verdict:** {'isReal': True, 'note': "Confirmed by direct reproduction. (1) ti84-trainer-v2/native/tests/verify-all-procedures.test.js:241 hard-codes expect(proceduresData.procedures.length).toBe(27); the file it loads (line 41 resolves to repo-root ti84-procedures-data.json) has 29 procedures (direct node count; extras = linreg-ttest, linreg-tint). (2) Ran `npx vitest run` in ti84-trainer-v2/native: 1 failed | 359 passed (360 tests, 6 files), sole failure is this assertion ('expected 29 to be 27') — suite is deterministically red. Minor severity nuance: the native suite is NOT included in root `npm test` (root vitest config includes only tests/** and lib/**; native has its own vitest.config.js run from its dir), so the permanent red only hits whoever runs the trainer suite, not the main repo suite — medium/S still reasonable. Suggestion (pin 29 with comment, or derive expected count from the exercised procedure IDs) is sound; also note two pre-existing diagnostic [DISCREPANCY] warnings (two-propztest, two-propzint end on wrong screens) that the always-pass diagnostic tests mask, reinforcing the normalization-of-red concern."}

### 23. [medium] (architecture) app.js is a 3,687-line single IIFE with ~150 functions and zero exports — the highest-value logic is structurally untestable

app.js (109KB) is two IIFEs: createBackend (lines 1-170) and everything else (171-3687) — SRS scheduling, persistence, list/data-setup tracking, walkthrough control, answer verification, gradebook recording, and ~900 lines of render functions (2528-3430) in one closure with nothing on window except via side effects. Nothing in it can be imported by a test. The repo already has the solution pattern in-house: native/ modules are small files exposing both window globals and module.exports (e.g. screen-renderer.js:543-546), tested by their own suite, and inlined by build.mjs via nativeScriptFilenames. Natural seams that would extract cleanly with the same dual-export pattern: (1) srs.js — recallQuality/sm2/applyTrack1Outcome/applyTrack2Outcome/track1QualityForBranches/isDue (app.js:2070-2212, 1137); (2) expected-values.js — computeExpected/valuesMatch/formatExpectedValue (584-744); (3) persistence.js — loadPersisted/savePersisted/loadListMemory (745-800). Also worth noting for 'next change pain': render() does a full root.innerHTML rewrite on every state change with a canvas-preservation hack (3365-3373) and a compensating syncAnswerInputsFromDom() (2267) — any new stateful DOM input must remember that dance.

**Where:** C:/Users/rober/Downloads/Projects/school/follow-alongs/ti84-trainer-v2/app.js:171 (IIFE open) through :3687; render at :3339

**Suggested fix:** Extract the three pure-logic seams (srs, expected-values, persistence) into ti84-trainer-v2/lib/ files using the native/ dual-export pattern, add them to nativeScriptFilenames in build.mjs and the index.html script list, and leave rendering/event-handling in app.js. This is mechanical (the functions are already pure or near-pure) and unlocks finding #4's tests.

**Effort:** L

**Verification verdict:** {'isReal': True, 'note': "Verified every citation directly. app.js is exactly 3,687 lines / 109KB, structured as exactly two IIFEs (createBackend 1-169 exposing only window.TI84V2Backend; main app 171-3687 exposing nothing — no module.exports anywhere, only side-effect writes like window.gradebookClient.record at 2181). All 14 cited function line numbers are exact: computeExpected 584, valuesMatch 709, formatExpectedValue 731, loadPersisted 745, savePersisted 773, loadListMemory 778, isDue 1137, recallQuality 2070, sm2 2090, applyTrack1Outcome 2113, applyTrack2Outcome 2121, track1QualityForBranches 2197, syncAnswerInputsFromDom 2267, render 3339, canvas-preservation hack 3365-3373 after full root.innerHTML rewrite at 3342. The in-house dual-export pattern is real: native/screen-renderer.js:543-546 (window.TI84ScreenRenderer + module.exports), build.mjs nativeScriptFilenames at line 24, and native/tests/ has 6 test files none of which can or do import app.js. Importing app.js in a test would throw at lines 179-181 (hard dependency on document.getElementById('app') + 4 window globals), confirming 'structurally untestable'. Minor corrections: function count is ~136 declared functions, not ~150; 'near-pure' is slightly generous — computeExpected reads window.TI84StatMath (line 593) and savePersisted/loadPersisted close over the app state object and localStorage, so extraction needs explicit state/deps parameters, though still the mechanical refactor described. Severity medium/architecture is appropriate."}

### 24. [medium] (architecture) Top 3 untested behaviors: SM-2 scheduling, answer-tolerance matching, and the brand-new gradebook write

Root tests cover only the route state machine (tests/ti84-state-machine.test.js) and the unrelated Desk plot; the native suite covers stat-math/menus/forms. Nothing tests: (1) sm2() + recallQuality() + the guided→recall promotion and errors>=3/hints>=2 demotion in applyTrack2Outcome (app.js:2070-2162) — this decides every student's daily review queue; an easeFactor or interval regression silently corrupts months of localStorage scheduling state with no visible error; (2) valuesMatch() (app.js:709-731) — the 0.5%-relative/0.0005-absolute tolerance plus '(-)' normalization that decides whether a student's hand-entered z/p values count as correct; tightening it during a refactor produces false 'wrong answer' verdicts, the worst student-facing failure this app can have; (3) recordTrainerAttempt() (app.js:2163-2196, added yesterday in f18c2ea) — the signed-out guard, itemId sanitization `TI84-${id.replace(...)}`, quality/5 score normalization, and attempt:1 upsert contract with gradebook-client are all unpinned; the gradebook side will trust whatever this sends. All three are pure or mockable and would be ~30 lines of test each once extracted per finding #3.

**Where:** C:/Users/rober/Downloads/Projects/school/follow-alongs/ti84-trainer-v2/app.js:2070-2212 (sm2/recall), :709-731 (valuesMatch), :2163-2196 (recordTrainerAttempt); vitest.config.js include excludes all app.js logic

**Suggested fix:** After (or even before, via the new Function loader trick verify-all-procedures.test.js already uses at line 26-38) extraction, add tests pinning: sm2 interval sequence 1→6→round(6*EF), EF floor 1.3, demotion thresholds, valuesMatch tolerance at boundary values including expected=0, and the exact recordTrainerAttempt payload for signed-in vs signed-out.

**Effort:** M

**Verification verdict:** {'isReal': True, 'note': "Confirmed. All three cited functions exist at the cited lines with the exact behaviors claimed: sm2/recallQuality/applyTrack2Outcome (app.js:2070-2161, incl. guided→recall promotion and errors>=3||hints>=2 demotion), valuesMatch (app.js:709-729, 0.5% rel / 0.0005 abs tolerance, (-) normalization, expected===0 case), recordTrainerAttempt (app.js:2163-2195, signed-out guard, TI84- itemId sanitization, quality/5 score, attempt:1), added in f18c2ea on 2026-06-10 as claimed. Test-gap verified: root vitest.config.js includes only tests/** and lib/** (app.js never loaded); grep for sm2|valuesMatch|recordTrainerAttempt|recallQuality|applyTrack2Outcome across all *.test.js returns zero hits; ti84-state-machine.test.js covers only route transitions; native suite covers native modules only. The suggested Function-loader trick exists at verify-all-procedures.test.js:26-38. Minor correction: f18c2ea also added roster-server/tests/ledger.test.js coverage for the 'trainer' source enum on the server side, so the server's acceptance path has some tests — but the client payload contract is unpinned exactly as the finding states. Severity M stands."}

### 25. [medium] (architecture) Multi-MB ROM download + emulator boot runs eagerly on every first load, even though the default mode hides the emulator entirely

physicalMode defaults to true (app.js:750, commit a976af6 made physical the default) and renderCalculatorColumn() short-circuits to renderPhysicalColumn() (app.js:3096) — the emulator canvas is never shown unless a student digs into Options. Yet init() unconditionally awaits app.bridge.init() (app.js:3658), which on first visit fetches the full CE ROM from the hardcoded Supabase signed URL (bridge.js:599-633, downloadRomFromSupabase at :400) and boots the wasm module. Inside the Desk iframe this is several MB of bandwidth + wasm boot CPU per student per device for a feature most never toggle (IndexedDB caching helps only after the first hit; school Chromebooks with cleared storage pay it repeatedly). The static weight is fine (standalone.html gzips to ~100KB, WebCEmu ~52KB gz, loaded lazily by the Desk only on openApp) — the ROM is the whole cost.

**Where:** C:/Users/rober/Downloads/Projects/school/follow-alongs/ti84-trainer-v2/app.js:3654-3668, :750, :3096; bridge.js:599

**Suggested fix:** Make bridge.init() lazy: in physical mode, skip ROM download/boot and show the existing 'simplified mode' status; trigger the full init the first time the user leaves physical mode (togglePhysicalMode, app.js:2474) or opens the ROM dialog. The bridge already has all the status plumbing to do this gracefully.

**Effort:** M

**Verification verdict:** {'isReal': True, 'note': "Confirmed every citation. physicalMode defaults true (app.js:750, :765; commit a976af6 verified, message even says 'the WASM emulator fails on school networks'). renderCalculatorColumn short-circuits to renderPhysicalColumn (app.js:3096-3098), so the emulator is hidden by default. init() at app.js:3654-3668 unconditionally awaits app.bridge.init(); the composite backend (app.js:58-65) awaits cemu.init() -> bridge.js:599-645, which on cache miss fetches the ROM from the hardcoded Supabase signed URL (bridge.js:13, downloadRomFromSupabase at :400) and boots the wasm module — no physicalMode gate anywhere; standalone.html inlines the same code (:8120/:11763). Live HEAD request: ROM is exactly 4,194,304 bytes (4.0 MB), Cache-Control: no-cache, so only IndexedDB caching helps. Desk loads standalone.html lazily in an iframe on openApp (ap_stats_roadmap_square_mode.html:12933-12958), confirming the ROM is the dominant cost. Two refinements that slightly STRENGTHEN it: (1) bootRecord also calls startRenderLoop (bridge.js:580), so the eager path costs ongoing frame-loop CPU, not just boot; (2) on school networks the fetch reportedly fails per the commit message, so the eager init buys nothing there either — just a failed-download delay. Severity medium is fair; the lazy-init suggestion is feasible (togglePhysicalMode at app.js:2474 exists, fallback/simplified-mode status plumbing exists)."}

### 26. [medium] (architecture) The script/module list is hand-maintained in three places — index.html, build.mjs, and the verify-all test

Adding or reordering a native module requires synchronized edits to: index.html script tags (lines 17-30), build.mjs nativeScriptFilenames (lines 24-34) + its HTML template (lines 99-133, which also duplicates the head/title/roster-script block of index.html), and native/tests/verify-all-procedures.test.js moduleOrder (lines 14-24). Dependency order matters (event-bus before stat-math before ... ti84-native), so a miss in one copy produces a silently divergent dev (index.html) vs prod (standalone.html) vs test (loadModules) environment — the exact class of bug that's hardest to notice because each surface 'works' where you looked. The roster-script trio added in f18c2ea was correctly synced into both HTML surfaces by hand, which shows the tax is being paid manually today.

**Where:** C:/Users/rober/Downloads/Projects/school/follow-alongs/ti84-trainer-v2/index.html:17-30; build.mjs:24-34,99-133; native/tests/verify-all-procedures.test.js:14-24

**Suggested fix:** Single-source the manifest: export the ordered list from one tiny module (e.g. native/manifest.mjs); have build.mjs import it AND generate index.html (or at least assert index.html's script tags match it), and have the test import it instead of redeclaring moduleOrder.

**Effort:** S

**Verification verdict:** {'isReal': True, 'note': "All three citations verified exactly: index.html:17-30 hand-ordered script tags; build.mjs:24-34 hard-coded nativeScriptFilenames (not a dir scan) + lines 99-133 HTML template duplicating head/roster block; verify-all-procedures.test.js:14-24 redeclared moduleOrder. No manifest, no cross-check assertion anywhere (grep confirmed). Order is load-bearing via window globals, and event-bus.js is documented optional in form-engine.js:10, so an omission in one copy fails silently rather than throwing — the 'silently divergent dev/prod/test' mechanism is plausible. Commit f18c2ea confirmed: roster trio was hand-synced into both index.html and build.mjs template. Correction: the finding UNDERCOUNTS — there is a fourth hand-maintained copy of the same 9-file ordered list in native/tests/ti84-native.test.js:28-38 (plus ad-hoc loading in form-engine.test.js). Severity medium/architecture is fair; suggestion (single-source manifest imported by build + tests, asserted against index.html) is apt."}

### 27. [medium] (architecture) 10-year Supabase signed URL to the TI ROM is hardcoded in bridge.js source

bridge.js:13 embeds a signed Supabase storage URL (token iat 2026-04-08, exp ≈ 2036) for ROM.rom directly in source, which gets inlined into standalone.html and published on the public GH Pages site. Two problems: (1) config-in-code — rotating/replacing the URL (e.g. if the bucket moves, token is revoked, or Supabase project changes — the same project hosts the roster DB) requires editing bridge.js and remembering the manual rebuild from finding #2; (2) it is a durable, public, hotlinkable download URL for TI's copyrighted OS firmware sitting in a public repo, which is a takedown exposure for the whole student-facing site, not just the trainer.

**Where:** C:/Users/rober/Downloads/Projects/school/follow-alongs/ti84-trainer-v2/bridge.js:10-14

**Suggested fix:** Move ROM_CONFIG into a sibling non-inlined config script (the repo already has this exact pattern: ../roster_config.js is script-src'd by both index.html and standalone.html rather than inlined), so rotation is a one-file edit with no rebuild. Separately consider whether the signed URL should be this long-lived/public at all.

**Effort:** S

**Verification verdict:** {'isReal': True, 'note': "Confirmed. bridge.js:13 embeds the signed Supabase URL; decoded token = iat 1775707891 (2026-04-09 UTC, finding said 04-08 — trivial off-by-one) and exp 2091067891, exactly 10 years later (~Apr 2036). URL is load-bearing (returned at bridge.js:214-215, fetched at :416). build.mjs:42/124-126 inlines bridge.js into standalone.html (URL verbatim at standalone.html:2850); both files are git-tracked in the public robjohncolson/apstats-live-worksheet repo, which serves the GH Pages site — so the durable ROM URL is public in source, build artifact, and served pages. Supabase project hgvnytaqmuybzbotosyj is the same one hosting the roster DB (per project memory), so rotation/revocation risk is real and a fix requires editing bridge.js plus the manual build.mjs rebuild of the committed standalone.html. Suggestion premise verified: ../roster_config.js is script-src'd (not inlined) in index.html:13 and the standalone template (build.mjs:112). cacheVersion 5.8.2.0029 matches a real TI-84 Plus CE OS version, making the copyrighted-firmware/takedown exposure plausible. Severity medium/architecture stands."}

### 28. [medium] (integration) Expired-token students silently lose all practice records — the trainer's guard checks the wrong thing

recordTrainerAttempt gates on rosterClient.current(), which only checks that a session object with studentId exists in localStorage — it never looks at the token. Tokens expire after 30 days (token.js THIRTY_DAYS_MS; verifyToken rejects). An expired-token student passes the trainer's guard, record() POSTs, the server answers 401, and gradebook-client returns {ok:false, reason:'network'} with only a console.warn — the red no-identity nudge fires ONLY when token is absent entirely (gradebook-client.js:93-95), not on 401. So a student who signed in 31+ days ago looks signed-in, practices all summer, and every row is dropped silently. Given SUMMER26 self-paced prep (long gaps between Desk sign-ins), this window is realistic, not theoretical.

**Where:** ti84-trainer-v2/app.js:2172; gradebook-client.js:93-95,126-132; roster-server/token.js:31,60

**Suggested fix:** Guard on window.rosterClient.token() (presence) in addition to current(), and surface record()'s resolved ok:false in the trainer UI (pairs with the finding above). A fuller fix — treating a 401 from /ledger/record as 'session expired, sign in again' in gradebook-client — would benefit all feeders, not just the trainer.

**Effort:** S

**Verification verdict:** {'isReal': True, 'note': "Confirmed end-to-end by reading all cited code. (1) ti84-trainer-v2/app.js recordTrainerAttempt (~2163-2196) gates only on rosterClient.current() and fire-and-forgets record(), never inspecting the result. (2) roster-client.js current() (lines 52-63) returns a truthy session from localStorage whenever studentId exists — it never checks the token, and nothing ever clears an expired session (clearSession only via explicit signOut). (3) roster-server/token.js: exp = now + THIRTY_DAYS_MS (line 31), verifyToken rejects past-exp (line 60). (4) roster-server/ledger.js POST /ledger/record → verifyToken → 401 {ok:false,'invalid token'}. (5) gradebook-client.js: the red nudge fires only on absent token; the 401 path is console.warn + {ok:false, reason:'network'} — invisible to the student. So an expired-token student looks signed-in, practices, and every ledger row is silently dropped; realistic under SUMMER26 self-paced gaps. Severity medium/S stands. One correction to the SUGGESTION: guarding on token() presence does NOT fix this — token() returns the stale token string without decoding exp, so only the second half of the suggestion (surface record()'s ok:false in the trainer UI and/or have gradebook-client treat /ledger/record 401 as 'session expired') actually addresses the bug. Also, the 401→silent-'network' mapping affects all feeders; the trainer is the worst case because it additionally discards the return value."}

### 29. [medium] (integration) recorded_at is never refreshed on re-practice upsert — the 'latest state' row carries a permanently stale timestamp

The trainer deliberately pins attempt:1 so re-practice upserts the same row ('latest state per procedure', app.js comment). But insertLedgerRow's upsert payload omits recorded_at, and the column's default now() fires only on INSERT — on conflict-update Postgres leaves it untouched. So a student who first practiced one-var-stats in September and re-practices weekly in May still shows recorded_at=September. Consequences: (a) the teacher's only window into trainer activity — the /teacher/student/:sid/recent drawer, sorted by recorded_at desc and capped at 20 — buries trainer rows permanently after the first 20 other submissions, so 'who is practicing NOW' is unanswerable even per-student; (b) any future recency-keyed trainer track inherits false data. The score/response DO update, so the row half-lies: fresh quality, ancient date.

**Where:** roster-server/ledger-db.js:28-47; roster-server/migrations/0002_item_ledger.sql:15; ti84-trainer-v2/app.js:2179-2180; roster-server/teacher.js:176-183

**Suggested fix:** Add recorded_at: new Date().toISOString() to insertLedgerRow's upsert payload. This is safe for the engine (latestPerItem only uses recorded_at as a tie-break within one item_id, and upsert keys guarantee one row per (item, attempt)) and makes every source's 'recent submissions' feed truthful, not just the trainer's. Verify against scoring.js latestPerItem and the persistent-answers fetchPrior dedupe before shipping.

**Effort:** S

**Verification verdict:** {'isReal': True, 'note': "Confirmed end-to-end. insertLedgerRow (roster-server/ledger-db.js:28-48) upserts on (student_id,source,item_id,attempt) with no recorded_at in the payload; PostgREST merge-duplicates only SETs supplied columns, and the column default now() (migrations/0002:15) fires only on INSERT. No touch-trigger exists on item_ledger (only roster has one, 0001). Trainer pins attempt:1 with the exact 'latest state per procedure' comment (ti84-trainer-v2/app.js:2179-2193) and POST /ledger/record (ledger.js:35-72) adds no server-side timestamp, so the row's recorded_at is frozen at first practice while score/response refresh. Impact (a) verified: /teacher/student/:sid/recent (teacher.js:131-194) sorts by recorded_at desc, default limit 20 (max 100 via ?limit — minor correction to 'capped at 20', but the stale sort key misorders at any limit); getLedgerByStudent/getLedgerByItem also order by recorded_at desc. Refutation attempt 'trainer rows can't exist' fails — migration 0016 adds 'trainer' to the source CHECK. Two notes: trainer rows are deliberately engine-invisible (0016 comment), so the suggested fix is lower-risk than implied; and the defect generalizes to ANY same-attempt re-upsert (worksheet attempt-1 re-answers, blooket re-import via class.js:295), trainer is just the only source designed around upsert-as-latest-state. Severity medium is appropriate."}

### 30. [medium] (integration) Latest-state overwrite can LOWER a recorded score — contradicts the repo's raise-only convention and erases the history a fair trainer track would need

Because attempt is pinned at 1, a student who once earned quality 5 (score 1.0) on a procedure and later has one shaky recall (3 errors -> quality 1 -> 0.2) overwrites the 1.0 with 0.2. Everywhere else the gradebook deliberately avoids punishing re-engagement: AI worksheet grading 'only ever RAISES grades', and the Blooket track takes max(game, flashcard) (lesson-grade.js:430-431). If a deliberate lesson-grade.js trainer track is ever built (the migration 0016 comment explicitly anticipates one), the natural key is: unit-scoped (parseItemLesson returns null for TI84- ids, and procedures carry only a unit, no lesson), denominator = procedures-per-unit (U1:5, U2:3, U4:4, U5:2, U6:4, U7:6, U8:3, U9:2 from ti84-procedures-data.json — the server has no copy of this; it would need a small manifest like worksheetBlankCounts), numerator = per-procedure score. The data MISSING today to grade fairly: attempt counts, per-day history, best-ever score, and a truthful last-practice date — all casualties of the attempt:1 upsert. That said, recording per-attempt forever has its own cost; the cheap middle ground preserves max while staying one-row.

**Where:** ti84-trainer-v2/app.js:2177-2194; ti84-trainer-v2/app.js:2070-2085 (recallQuality); roster-server/lesson-grade.js:27-78,430-431; roster-server/migrations/0016_item_ledger_trainer_source.sql:10-11

**Suggested fix:** Keep one row per procedure but make score raise-only: score = max(previous, quality/5) — the trainer already persists its own SM-2 state locally, and response already carries the latest {quality, errors, hints, mode} for diagnostics. Alternatively number attempts (latestPerItem already prefers highest attempt) if per-day history is wanted later. Decide BEFORE a cohort accumulates data — backfilling lost maxima is impossible.

**Effort:** M

**Verification verdict:** {'isReal': True, 'note': "Confirmed. All citations hold: app.js:2177-2194 pins attempt:1 and sends score = track2.lastQuality/5 (latest, not max; comment says 'latest state per procedure'); recallQuality (app.js:2070-2088) maps 3 errors→quality 1→0.2 and hints>=2→0, so a prior 1.0 is overwritten by 0.2. The overwrite is real server-side: roster-server/ledger-db.js:28-48 upserts on (student_id,source,item_id,attempt), destroying best-ever score, attempt history, and original recorded_at. parseItemLesson (lesson-grade.js:27-78) has no TI84- branch (returns null); Blooket max(game,flashcard) at lesson-grade.js:430-431 confirms the raise-only convention; migration 0016:9-11 explicitly anticipates a future trainer grading track while recording starts now — so irreversible loss of maxima accumulates before the track exists. Suggestion premises verified: latestPerItem (scoring.js:61-78) prefers highest attempt, and response already carries latest quality/errors/hints/mode for diagnostics. Corrections: (1) the description's data file 'ti84-procedures-data.json' does not exist — procedures live in ti84-trainer-v2/generated/data-procedures.js (window.TI84V2ProceduresData), though its claimed per-unit counts are exactly right (29 procedures: U1:5,U2:3,U4:4,U5:2,U6:4,U7:6,U8:3,U9:2); (2) severity leans medium-low since trainer rows are grade-inert today (engine consumes specific sources only) — impact is forward-looking data destruction, not a current grade bug; (3) the overwrite is documented intent (latest SM-2 state), so the right fix is raise-only on the score field while keeping the response payload latest-state, as the suggestion proposes."}

### 31. [medium] (integration) Teacher has no 'who is practicing' view — trainer rows surface only in the per-student drawer's 20-row mixed feed (opportunity)

Trainer rows are grade-inert by design (computeGrade filters to worksheet/frq/curriculum_quiz/pc/blooket; gradebook-grid.js has no trainer kind; parseItemLesson rejects TI84- ids), and the ONLY place a teacher can see them today is teacher-dashboard.html's per-student drill-down, which renders /teacher/student/:sid/recent?limit=20 — trainer rows mixed with every other source, newest-20 only, and (per the stale-recorded_at finding) sinking permanently. There is no class-level aggregate anywhere: not /class/grades, not the in-Desk class gradebook, not the dashboard. The cheap path exists already: /class/grades fans out getLedgerByStudent for EVERY student (class.js fanLedger) — those arrays already contain the trainer rows; they're just dropped on the floor by computeGrade. An additive per-student summary derived in that same pass costs no extra DB round-trips.

**Where:** roster-server/grade.js:103-135; roster-server/gradebook-grid.js:39-45; roster-server/class.js:39-51,167; teacher-dashboard.html:1817,1723-1736

**Suggested fix:** Opportunity, not a build: add an additive trainer:{procedures, avgScore, lastAt} field per student to the /class/grades response (computed from the already-fetched ledgerRows, ~20 lines), plus one column or a small 'TI-84 practice' section in teacher-dashboard.html. Grade math untouched; the engine continues to ignore the rows.

**Effort:** M

**Verification verdict:** {'isReal': True, 'note': "Confirmed on all counts. (1) Trainer rows really land in the ledger: ti84-trainer-v2/app.js:2163-2194 recordTrainerAttempt() fire-and-forgets gradebookClient.record({source:'trainer', itemId:`TI84-<procedureId>`, score:quality/5, attempt:1}) for signed-in students (mirrored at standalone.html:10297; source provisioned by migration 0016). (2) Grade-inert as claimed: grade.js computeGrade's bySource pulls only curriculum_quiz/pc/frq/worksheet (lines 103-135; blooket handled via the lesson machinery) and the string 'trainer' never appears in grade.js; gradebook-grid.js KIND_CATEGORY (39-45) has only followalong/quiz/blooket/pc/poster; lesson-grade.js parseItemLesson (27-78) has no TI84- pattern so trainer ids return null. (3) Sole teacher surface confirmed: teacher-dashboard.html:1817 fetches /teacher/student/:sid/recent?limit=20 and renderTscRecent (1711-1737) renders a mixed newest-20 itemId/source/score list (teacher.js:131-133, default 20, max 100); teacher-dashboard.html contains zero 'trainer'/'TI84' references, so there is no filter or aggregate. (4) No class-level view anywhere: /class/grades returns only computeGrade/buildGradebook outputs (trainer-inert); the Desk's trainer mentions are the app-launcher iframe and the Equation Trainer toast bridge (student-local); teacher-classroom.html matches are just Ti84Plot poll charts. Note: roster-server/trainer.js DOES have a teacher snapshot (GET /trainer/section/:section/summary/:deckId), but it reads the trainer_state table for the tmux-trainer/Equation-Trainer decks (allowlist ap-stats-formulas,joyo-kanji,jlpt-n5) — ti84-trainer-v2 never touches /trainer/state or a deckId, so that endpoint does not cover TI-84 practice. (5) Cheap path verified: class.js fanLedger (39-51) calls ledger-db.js getLedgerByStudent which selects('*') from item_ledger with no source filter, so trainer rows are already in the per-student arrays /class/grades fetches and are simply unused. Two small corrections to the description: (a) 'sinking permanently' depends on the separate stale-recorded_at finding (not re-verified here) — the newest-20 limitation stands regardless, and the upsert-on-attempt=1 design (one row per procedure, re-practice overwrites) makes drowning in the 20-row feed plausible either way; (b) severity 'opportunity/medium' is fair — this is a missing feature by design, not a defect; the suggested additive trainer:{} summary in the /class/grades fan-out is accurate and costs no extra DB round-trips."}

### 32. [medium] (integration) Desk never passes lesson/unit context and the trainer reads no URL params or hash — deep-linking is one small change away (opportunity)

APP_REGISTRY loads the trainer by a static absolute URL with no fragment, and app.js contains zero references to location/URLSearchParams — the only unit filter is app.filterUnit, restored from localStorage (persisted) and changed via an in-app dropdown. So a student who opens the trainer from a Unit 4 lesson lands on whatever unit they last filtered, and the Desk has no way to say 'practice U4 procedures'. Since the iframe src is set only on first open (openApp checks iframe.src before assigning), a hash would apply per fresh open — the natural granularity. Same-origin note: identity sharing works on the deployed site because both apps live on the robjohncolson.github.io origin; opened from file:// the trainer is correctly anonymous (the signed-out guard skips recording) — environment-dependent but fails safe.

**Where:** ap_stats_roadmap_square_mode.html:12934,12956-12958; ti84-trainer-v2/app.js:742,3214,3571 (filterUnit; no location.* anywhere in app.js)

**Suggested fix:** Honor '#unit=N' at trainer init: if location.hash parses to a unit present in the data, override the persisted filterUnit for that session. Then the Desk lesson modal (or the ti84 APP_REGISTRY entry per-lesson) can append the fragment. Edit app.js + run build.mjs to regenerate standalone.html; one jsdom test.

**Effort:** S

**Verification verdict:** {'isReal': True, 'note': "Confirmed all citations. APP_REGISTRY ti84 entry (roadmap HTML:12934) is a static absolute URL to standalone.html with no fragment; the only openApp('ti84') call sites (lines 1442, 1495) pass no context. openApp (12948-12958) assigns iframe.src only when empty/about:blank, and destroyApp resets to about:blank, so a hash would apply per fresh open as claimed. Grep over all of ti84-trainer-v2/ finds zero references to location, URLSearchParams, or hash; the sole unit filter is app.filterUnit (app.js ~741) restored from localStorage and changed only via the in-app #unit-filter dropdown (~3214 render, ~3570 handleChange). build.mjs exists and consumes app.js, so the suggested fix path (edit app.js + rebuild standalone.html) is feasible. Impact is plausible: students always land on their last-persisted filter regardless of which lesson they came from. Severity fine as a medium opportunity; one precision tweak — iframe src is set on first open AND after every close (destroy), not strictly first open only, which actually strengthens the per-open hash granularity."}

## Refuted (2) — checked, NOT real issues

### [?] (content) Pattern-recognition track drills input-side recognition only — no calculator-OUTPUT reading, an explicitly tested AP skill

### [?] (stability) Roster guard mismatch: trainer gates on rosterClient.current() but record() gates on token() — token-less session pops the red nudge with a 404 sign-in link

## Low-severity / unverified extras (14)

- **(student-ux)** Dialog and SRS-jargon polish: fake close boxes, no Escape, raw quality scores and ISO dates
  The System-7 close-box squares in the title bars are decorative spans that look clickable but do nothing (handleClick only matches buttons); dialogs have no role='dialog'/aria-modal, no Escape-to-close, and no focus trap. Session results expose internals: 'Track 1 quality: 5', 'Next review is 2026-06-18', '...demoted' — a student doesn't know quality is a 0-5 scale or what demotion means. Key buttons also concatenate secondary/alpha labels into their accessible names (e.g. the MATH key reads as 'test A math').
- **(student-ux)** Only 2-3 canonical problems per procedure — stems repeat fast over a summer of use
  canonicalProblems holds 1-3 stems per procedure (matrix-entry has exactly 1), chosen with pickRandom. An SRS brings the same procedure back many times; by the second or third review the student recognizes the coffee-shop/customer-satisfaction story and pattern-matches the surface text instead of the statistical structure — the exact failure mode Track 1 exists to prevent. Values are fixed per stem, so even the numbers repeat.
- **(content)** Step-data inconsistencies: wrong screen ref, swapped Color:/Calculate narrations, and a CNTB contradiction between narration and screen def
  Three citable data-quality defects found in the 5-procedure spot-check (one-var-stats, normalcdf, binompdf, t-test-stats, chi-square-gof-test were otherwise correct for OS 5.x with stat wizards ON, which the CE ROM defaults to): (1) t-test-stats step 17 declares screen 't-test-data-wizard' while steps 5-16 correctly use 't-test-stats-wizard' — a wrong screen pointer in a screen-keyed trainer; (2) the two-DOWN sequence through the Color: row has its narrations swapped in several procedures — the step narrated 'Move to Calculate or Draw' actually lands on Color: and the next step narrated 'Move past Color:' lands on Calculate (two-propztest steps 14-15, chi-square-gof-test steps 13-14, t-test-stats 16-17), mildly confusing during guided mode; (3) chi-square-gof-test's final narration still promises 'χ², p, df, and CNTB' (line 7003) while the chi2gof-result screen def lists only χ²/p/df, and meta line 21 claims ROM disassembly 'removed hallucinated CNTB' — but the real TI-84 Plus CE GOF test does display a CNTRB contribution list (the disassembly may have searched the wrong string), and contributions matter pedagogically since the U8 L3 worksheet rubrics grade them.
- **(content)** Inconsistent letter-item menu pedagogy: binompdf/binomcdf scroll with repeated DOWN while sibling procedures teach ALPHA+letter
  The project established (commit ceda16e) that letter menu items require ALPHA + the physical key, and most procedures follow it: geometpdf uses ALPHA then SIN (E:), chi-square-gof uses ALPHA then x^-1 (D:), two-propzint uses ALPHA then APPS (B:). But binompdf and binomcdf reach A:/B: in the same DISTR menu via a repeatable DOWN-scroll step ('Scroll until A:binompdf( is highlighted'). Both are valid on the calculator, but the trainer is teaching two different habits for the identical mechanic in the identical menu, and the scroll route is ~10 presses vs 2. For a muscle-memory trainer, consistency is the product.
- **(stability)** showFallback/clearStoredRom null the wasm module without cancelling its main loop — orphaned emulator keeps running
  destroy() correctly calls module._emsc_cancel_main_loop (bridge.js:783-789; export confirmed present in wasm/WebCEmu.js). But showFallback (bridge.js:355-367) and clearStoredRom (666-686) just do stopRenderLoop() + state.module = null. The emscripten main loop started by callMain keeps executing in the background — after 'Clear cached firmware' the student's tab silently burns CPU emulating a calculator nobody can see until reload. In the dev manual-ROM flow, selectRomFile → bootRecord also overwrites state.module (line 567) without cancelling the previous instance, so repeated ROM selection stacks live main loops. (Desk iframe open/close is fine: destroyApp sets src='about:blank' which discards the whole document, and re-open is a clean full re-init from the IDB cache — no leak or double-init across open/close cycles.)
- **(stability)** 'Clear cached firmware' click has no error handling — IDB-unavailable rejection leaves the dialog stuck with no feedback
  clearStoredRom awaits deleteRecord, which rejects when IndexedDB is unavailable (bridge.js:666-667 via openDatabase 97-98). The caller, handleClick case 'clear-rom' (app.js:3555-3563), has no try/catch — the rejection escapes through `void handleClick(event)` as an unhandled promise rejection, the ROM dialog stays open, the banner never updates, and the button appears dead. Every other async action in handleChange wraps its awaits; this one doesn't.
- **(stability)** Persisted-state 'version: 2' field is written but never read — no migration hook, and corrupt JSON silently wipes SM-2 progress
  loadPersisted (app.js:745-771) declares version: 2 in the fallback then spreads `{...fallback, ...parsed}` without ever inspecting parsed.version — any future schema change to the track1/track2 record shape will silently merge old-shaped records and blow up later in ensureProcedureRecord consumers (e.g., applyTrack2Outcome dereferences .track2 unconditionally at app.js:2122). Verified there is no live v1 hazard today: the old trainer used a different key entirely ('ti84trainer_state_v1' in ti84_trainer.html), and 'ti84trainer_v2_state' was born at version 2 (commit ca07b97). Also note corrupt JSON → fallback resets ALL spaced-repetition progress with only a console.warn; the student never learns their review schedule was wiped.
- **(stability)** Trainer keypad has zero physical-keyboard support despite the Desk handing it focus as a 'keyboard-driven' app
  The Desk's openApp calls iframe.focus() with the comment 'Embedded apps are keyboard-driven' (ap_stats_roadmap_square_mode.html:12964-12966), but the trainer's only keydown handler is Enter inside answer inputs (app.js:3622-3629) — the entire calculator keypad is mouse/touch-only. Nothing in the trainer claims focus on load, there is no audio (confirmed: no AudioContext in app.js, bridge.js, or wasm/WebCEmu.js), and resize handling is a debounced mobile-boundary check only (app.js:3674-3684) — so embedding is stable. But for a muscle-memory tool, students who try arrow keys/Enter/digits on a real keyboard get silence, and the focus handoff the Desk performs benefits nothing.
- **(stability)** prepareHome uses 4x CLEAR while every other reset path uses 2ND+QUIT — abandoned mid-wizard restarts can desync the real LCD from the guided steps
  bridge.js prepareHome (733-748) sends CLEAR x4 to reset the real emulator before each walkthrough, but app.js's own quitToHome (1666-1669) — used by all the auto-fill paths — sends 2ND+MODE (QUIT), the canonical way to exit TI-84 menus/wizards. If a student abandons a walkthrough inside a STAT TESTS wizard and clicks Restart, CLEAR presses may only clear the wizard's input field rather than return to HOME, leaving the real LCD on a different screen than the native state machine (which is hard-reset via native.reset(), app.js:83). The guided overlay then highlights keys for HOME while the firmware shows a wizard. Low confidence on exact ROM behavior (not verifiable without booting the ROM), but the inconsistency between the two 'go home' implementations is code-evident and quitToHome is strictly safer.
- **(architecture)** Old v1 trainer (250KB) still ships on the student-facing site but nothing links to it
  ti84_trainer.html (211KB) and scripts/ti84-trainer-runtime.js (39KB) are git-tracked and published via GH Pages, but a repo-wide grep finds references only in CLAUDE.md, codex prompt docs, and cross-agent state files — no live page links them (the Desk registry and start-here both point at ti84-trainer-v2/standalone.html). ti84-verify.html is similarly self-referenced only. Dead weight is mostly cognitive: the next agent/dev doing a 'trainer-wide' change has two plausible-looking trainers to consider, and the v1 file still answers at its public URL with stale behavior if a student has an old bookmark.
- **(architecture)** transformStateMachine's import-stripping regex is exact-match brittle and unverified
  build.mjs:53-57 removes ti84-state-machine.js's node-only header with a regex matching the exact three-line import/createRequire block (it does handle \r?\n). If anyone touches that header — adds a second import, a comment, reorders lines — String.replace silently no-ops, the bare `import { createRequire }` lands inside a classic inline <script>, and standalone.html throws SyntaxError at parse time, taking down the whole trainer in the Desk while index.html (which loads generated/state-machine.js... actually also generated, so both surfaces break) — and nothing at build time says a word. Today the output is verified correct (generated/state-machine.js has no import and defines createState), but the failure is silent-until-runtime.
- **(architecture)** Committed generated artifacts carry no 'generated — do not edit' banner
  generated/data-*.js, generated/state-machine.js, and standalone.html are committed (correctly, for GH Pages serving) but open with no marker that they are build outputs — standalone.html in particular looks exactly like the repo's other 70 hand-written single-file HTML apps, inviting a hand-edit that the next `node build.mjs` silently clobbers. Positive note worth recording: the build is fully deterministic (pure content-derived, no timestamps — verified today by a rebuild that left all generated files byte-identical to git), so the only churn is real content churn; the discipline just isn't self-documenting.
- **(integration)** Trainer's own native suite is permanently red: count assertion expects 27 procedures, data has 29
  verify-all-procedures.test.js:241 asserts proceduresData.procedures.length === 27, but ti84-procedures-data.json now carries 29 (the per-unit counts sum to 29). Known pre-existing, but a suite that is always red trains everyone to ignore it — the per-procedure walkthrough loop in the same file is exactly the kind of regression net you want green when bridge/native code changes. The file header comment ('all 27 TI-84 procedures') is stale too.
- **(integration)** No exam-date or seasonal assumptions found in trainer data/UI (verification result)
  Checked for the deadline-memory concern ('review-ready by ~2026-05-07'): app.js has no exam/date/deadline strings; the word 'exam' appears only inside practice problem stems in ti84-pattern-recognition-data.json ('college entrance exam', 'exam scores' — generic stats contexts, not AP-exam-dated); all scheduling is relative SM-2 arithmetic (todayIso/addDays/nextReview) with no anchored calendar dates. Nothing breaks or misleads across the exam->summer->Sept-cohort transition. The one seasonal consideration is content coverage, not dates: SUMMER26 prep is U1-U5 but the procedure bank is weighted toward inference (U6-U9 = 15 of 29 procedures); the existing unit filter already handles this, no change needed.

## Lens-level impressions

### student-ux

This is an ambitious, genuinely impressive piece of courseware: a real TI-84 Plus CE ROM running in WASM, a parallel native state machine that validates every keypress, a two-track SM-2 scheduler (pattern recognition + procedure execution), branch walkthroughs that turn wrong answers into contrast lessons, and a clutch system that cleanly separates guided steps from free exploration. The data layer (29 authored procedures with per-step commonErrors coaching, confusion-matrix contrasts) is the kind of content most commercial tools don't have. The day-old gradebook wiring is also done carefully (signed-out users skip cleanly).

But viewed as a student walks through it, the learning loop leaks at exactly the points the architecture exists to protect: recall mode displays the answer (narration, mock-screen footer, and wrong-key messages all name the next key), the default physical mode is an honor-system that records perfect scores to the teacher's gradebook, and Track 1 can be brute-forced by cancelling branches. On phones the situation is worse: the banner — the only feedback channel — is hidden, and the default physical mode has no escape hatch to Options or the emulator. Add a jargon-only first-run screen ('Track 2 validates each key with the native state machine') and a brand-new 16-year-old has no idea what this tool wants from them. Finally, 2-PropZTest/ZInt — core AP Unit 6 procedures — are fully authored but silently filtered out (the same defect the known native test failure points at).

The good news: most of the high-severity items are copy/gating fixes (gate narration by mode, add a mode toggle to the physical panel, route the banner into the mobile narration bar, author two pattern-data entries), not architecture work. Fixing the recall leak + physical-mode scoring before the Sept 2026 cohort would make the recorded 'trainer' data trustworthy and the SRS actually do its job; a per-procedure progress view is the biggest motivational lever after that.

### content

Content quality is unusually rigorous for a side tool — menus and wizard fields were verified against a real ROM dump (STAT TESTS 18-item ordering, DISTR menu, wizard field tables all match CE OS 5.8.2), the native suite walks every procedure data-driven (83/84 pass; the lone failure is a stale count pin), and unit tagging is 100% complete: all 29 procedures carry numeric units 1-9, and the new ledger write (app.js:2184) guards with Number.isFinite before stamping 'U'+n, so the f18c2ea recording path is sound for every procedure a student can reach. The two real problems: (1) the two newest procedures (2-PropZTest/2-PropZInt, core U6 content) are silently filtered out of the trainer because they never got pattern-recognition backfill — app.js:206 gates the entire procedure list on signature presence; and (2) a systemic keystroke pattern omits the ENTER commit on wizard choice fields (Inpt, alternative hypothesis, Tail), which against real TI-84 OS behavior would teach students a habit that silently runs the wrong test on exam day — this needs one session of verification against the bundled WebCEmu ROM. Coverage maps well to inference-heavy units (U6-U9) but U3 has zero procedures (no randInt/seed) and U4 lacks the discrete-RV mean/SD FreqList skill — both inside or adjacent to the SUMMER26 U1-U5 prep window, making them the highest-value content additions before September. The pattern track (27 signatures, 22 confusion pairs, 62 canonical problems) is genuinely good at stem-to-procedure recognition but has no output-interpretation drills, the other half of the exam skill.

### stability

Overall this is a well-engineered app for its class: every layer of the ROM pipeline has a designed user-visible fallback (no hangs — failures land in 'simplified mode' with a status pill and banner), the build is deterministic (node build.mjs exits 0 and reproduces the committed generated/* and standalone.html byte-for-byte), Desk iframe open/close is clean (src='about:blank' discards the document; re-open is a fresh init from the IDB ROM cache — no leaks, no double-init, no focus stealing, no audio), and the new roster wiring is correctly fire-and-forget with double-fire protection (app.walkthrough is nulled before a second finish-review click can land) and a working unit guard (procedure.unit is an int in all 29 data entries, so Number.isFinite passes). Suite results: root `npx vitest run tests/ti84-state-machine.test.js tests/ti84-plot.test.js` = 107/107 passed (54+53, 2 files); native suite = 359 passed / 1 failed of 360 (the known stale pin at verify-all-procedures.test.js:241 expecting 27 procedures vs 29 in data). The real risks cluster in three places: (1) the ROM cache lifecycle — an IndexedDB failure wrongly aborts a download that doesn't need IDB, and a corrupt-but-version-current cached ROM is never auto-invalidated, both of which strand students in simplified mode; (2) unguarded localStorage.setItem in the save paths, which in quota/private-mode browsers throws after state mutation but before render(), stranding the UI and silently never persisting SM-2 progress; and (3) a subtle predicate mismatch in the new roster guard (current() vs token()) that can resurrect exactly the red 404-link nudge the guard was written to suppress. All are small (S-effort) fixes. The permanently-red native test pin should be fixed before the Sept 2026 cohort work ramps up, so red actually means something again.

### architecture

A genuinely well-shaped small app with better-than-average discipline for this repo: the build is deterministic (verified byte-identical on rebuild), standalone.html is currently in perfect sync with sources, math/menus/forms live in nine small dual-export native/ modules with 359 passing tests, and app.js delegates math to stat-math rather than duplicating it. The shape of the risk is process, not code quality: (1) the one artifact students actually load (standalone.html, via the Desk's absolute GH Pages URL) is produced by a manual `node build.mjs` with no npm script, no drift test, and one historical commit proving the stale-prod failure mode already happened; (2) yesterday's content addition (two-prop procedures) shipped without native support and left the native suite permanently red via a hard-coded 27-procedure pin while its real assertions are soft — the worst combination, a failing test everyone ignores guarding nothing; (3) the highest-stakes logic (SM-2 scheduling that drives every student's review queue, the answer-tolerance matcher that judges student answers, and the day-old gradebook write) is locked inside a 3,687-line zero-export IIFE with no tests. The fixes compound: export a buildStandalone() + drift test (S), extract three pure seams using the native/ pattern already in the repo (the build inliner needs no new machinery), then pin SM-2/tolerance/record-payload behavior. The eager multi-MB ROM download in a default mode that never shows the emulator is the one student-facing perf win available for the Sept cohort.

### integration

Yesterday's wiring (f18c2ea) is structurally sound: recordTrainerAttempt -> gradebookClient.record() -> POST /ledger/record matches the frozen ledger contract exactly (itemId 'TI84-<kebab-id>' fits the [A-Za-z0-9-] charset, unit 'U<n>' matches the DN2b convention, score quality/5, upsert on attempt:1), nothing in the chain can throw into the practice loop, and the rows are correctly grade-inert (parseItemLesson and computeGrade both ignore them by design, per migration 0016's stated intent). The weakness is not the shape but the silence: there is no confirmation anywhere — no UI indicator, no client test, no teacher aggregate — that a single trainer row has ever landed, and migration 0016 is USER-RUN with no recorded evidence it was executed (the sibling 0017 demonstrably hit not-provisioned live on deploy day). The two data-design choices worth revisiting before a cohort accumulates data are the attempt:1 upsert overwriting better scores with worse ones (against the repo's raise-only convention) and the recorded_at column never refreshing on upsert (which quietly breaks the teacher's only recency view for ALL sources, not just the trainer). The path to counting is clear and cheap — a unit-scoped trainer track needs only a procedures-per-unit manifest server-side plus a max-score policy client-side — and teacher visibility is a ~20-line additive field on /class/grades since the ledger rows are already fetched there and discarded. No exam-date or seasonal assumptions exist in the trainer; it ages cleanly into the Sept 2026 cohort.
