Phase 80-2: Probe untested JT slot targets in 0x0a2xxx family (TI-84 CE ROM transpilation)

## Context
Phase 77 found 16 JT slots targeting the 0x0a2xxx helper region (0x0a2000-0x0a7000). Only 4 were probed (627→0x0a29ec, 635→0x0a2a68, 639→0x0a2b72, 647→0x0a32af). The remaining 12 slots are untested as function entries.

**Goal**: probe each of the 12 untested slot targets as direct function entries. Some may be screen renderers, some may be state helpers.

## Working directory
C:/Users/rober/Downloads/Projects/school/follow-alongs/TI-84_Plus_CE

## Deliverables

### 1. `probe-phase80-jt-slots.mjs`
Pattern-match `probe-phase78-parents.mjs` (same boot/init/probe harness). Probe these 12 entries:
- `slot591_0a2032` — entry=0x0a2032, note: 'slot 591 — register save routine'
- `slot595_0a215b` — entry=0x0a215b, note: 'slot 595 — cursor arithmetic at 0xd00595'
- `slot599_0a21bb` — entry=0x0a21bb, note: 'slot 599 — short load'
- `slot603_0a21f2` — entry=0x0a21f2, note: 'slot 603 — call wrapper'
- `slot607_0a22b1` — entry=0x0a22b1, note: 'slot 607 — iy+42 flag check + 0x025c33'
- `slot611_0a237e` — entry=0x0a237e, note: 'slot 611 — cursor+text prep (called by 0a29ec)'
- `slot615_0a26ee` — entry=0x0a26ee, note: 'slot 615 — push af + call 0a26f5'
- `slot619_0a27dd` — entry=0x0a27dd, note: 'slot 619 — iy+27 flag + state setup'
- `slot631_0a2a3e` — entry=0x0a2a3e, note: 'slot 631 — wraps 0a2a68'
- `slot643_0a2ca6` — entry=0x0a2ca6, note: 'slot 643 — iy+42 flag + 0x025dea'
- `slot851_0a5424` — entry=0x0a5424, note: 'slot 851 — iy+53 flag + 0x02398e (icon renderer!)'

For each probe, run ONCE with default regs (no DE/HL override) AND once with `_de=0x4f` (Normal token code) variant, `maxSteps: 80000, maxLoopIterations: 2000`.

Capture VRAM stats and ASCII preview if drawn > 300.

### 2. `phase80-2-jt-slots-report.md`
- Results table: | probe | entry | variant | drawn | fg | bg | bbox | steps | termination |
- Block trace (first 15 blocks) for any probe that rendered > 1000 px
- ASCII previews for any with drawn > 300
- Verdict section: which slot targets are renderers vs state helpers? Any new home-screen candidates?

## Reference files
- `probe-phase78-parents.mjs` — template (EXACT same harness works)
- `phase77-manual-report.md` — Section 3 has the slot → target mapping
- `phase77-jt-probes-report.md` — example probe results format

## Task type
IMPLEMENTATION — write the script, EXECUTE, produce the report.

## Timeout
600 seconds. 12 entries × 2 variants × ~10s = ~240s + 60s setup. Should complete in ~5 min.
