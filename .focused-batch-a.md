Task: Retheme study_guide_diagnostic.html for the v3 three-pane layout.

Spec reference: read `.study-guide-spec-focused.md` sections "Shape of the new UI", "Color themes", "CSS approach" before starting.

Files you may edit:
- `C:/Users/rober/Downloads/Projects/school/follow-alongs/study_guide_diagnostic.html` (CSS + `<body>` markup only, roughly lines 7 through 160, plus one new function `applyTheme` inside the IIFE near `init`)

You may NOT edit any other file.

Do:
1. Replace the `<style>` block so all colors come from CSS variables defined on `:root[data-theme="paper"]` and `:root[data-theme="night"]`. Use the exact palettes from the spec:
   - Paper: `--sg-bg:#f7f2e8; --sg-text:#2a2520; --sg-accent:#7a4a1f; --sg-bar-track:#e2d6c0; --sg-bg-card:#ffffff; --sg-border:#d8ccb0; --sg-text-dim:#5a5045; --sg-mastery-low:#c0392b; --sg-mastery-mid:#d4a017; --sg-mastery-high:#2e7d32;`
   - Night: `--sg-bg:#14171c; --sg-text:#e4e7ec; --sg-accent:#6ca6ff; --sg-bar-track:#222833; --sg-bg-card:#1a1f27; --sg-border:#2a3140; --sg-text-dim:#9aa3b2; --sg-mastery-low:#e57373; --sg-mastery-mid:#e6c35c; --sg-mastery-high:#66bb6a;`
   - `:root` without a data-theme should default to the Paper palette.
2. DELETE the `.dag-panel`, `.dag-svg-wrap`, `.dag-legend`, `.dag-legend-chip`, `.dag-legend-chip.low`, `.dag-legend-chip.mid`, `.dag-legend-chip.high` rules entirely.
3. Replace the `#units-container` region of `<body>` with:
   ```html
   <div id="sg-layout">
     <aside id="sg-rail" aria-label="Concept rail"></aside>
     <section id="sg-active" aria-label="Active probe"></section>
     <aside id="sg-remediation" aria-label="Remediation"></aside>
   </div>
   ```
   Add a CSS grid rule: `#sg-layout{display:grid;grid-template-columns:260px 1fr 320px;gap:20px;}` and a responsive fallback: `@media (max-width:900px){#sg-layout{grid-template-columns:1fr;}}`.
   Add sensible default styling for `#sg-rail`, `#sg-active`, `#sg-remediation` using the CSS variables (background `--sg-bg-card`, border `1px solid --sg-border`, radius `8px`, padding `16px`). Do not remove the `<header>` block or the existing progress summary markup — leave those alone for Batch M to handle.
4. In the header, add `<button id="theme-toggle" type="button">Paper / Night</button>` just before the Export button. Give it a small rule: `#theme-toggle{background:var(--sg-bg-card);color:var(--sg-text);border:1px solid var(--sg-border);padding:6px 12px;border-radius:6px;cursor:pointer;}`
5. Inside the main IIFE in the `<script>` block (NOT touching any existing function body), add a new helper near `init`:
   ```js
   function applyTheme(theme) {
     var safe = theme === 'night' ? 'night' : 'paper';
     document.documentElement.setAttribute('data-theme', safe);
     try { localStorage.setItem('apStatsStudyGuideDiagnostic.theme', safe); } catch (e) {}
   }
   ```
   Then wire a listener (NOT inside init itself, but guarded with a DOMContentLoaded check or a one-time hookup near where other buttons are hooked): on click of `#theme-toggle`, toggle between `'paper'` and `'night'` by reading `document.documentElement.getAttribute('data-theme')`. On page load, read `localStorage.getItem('apStatsStudyGuideDiagnostic.theme')` and call `applyTheme` with whatever is found (defaulting to `'paper'` if missing).
6. Do NOT touch:
   - any render function (`renderUnit`, `renderUnits`, `rerenderUnit`, `renderDagPanel`, etc.)
   - any state function (`makeDefaultState`, `normalizeState`, `getUnit`, `serializeState`, `runFocus`)
   - the test file `tests/study-guide.test.js`
   - any file under `lib/` or `data/`
   - the `<script src="lib/dag-renderer.js">` tag (Batch M will delete it)
   - Leave the existing `renderUnits()` call in `init` in place; it will still render cards alongside the new empty panes until Batch M.

Accept when:
- `document.getElementById('sg-rail')`, `sg-active`, `sg-remediation`, `theme-toggle` all exist in the HTML source.
- Clicking the theme toggle sets `data-theme="night"` on `<html>` and reloading keeps it.
- `grep -c "dag-panel\|dag-legend\|dag-svg-wrap" study_guide_diagnostic.html` returns 0 in the CSS section (there may still be JS references to `renderDagPanel` — leave those for Batch M).
- `npm test -- tests/study-guide.test.js` passes (existing DAG block may still pass because the JS function `renderDagPanel` is still defined; that is fine).

Self-check:
- Run `npm test -- tests/study-guide.test.js 2>&1 | tail -40`
- Run `npm test -- lib/ 2>&1 | tail -20`
- Both must be green (or have the same failures as before you started).

Report back: what you changed, line counts added/removed in the HTML, and any test output that looks unexpected.
