Task: Add pure-logic helpers for probe picking, next-weakest selection, curriculum deep links, and active-probe state mutation.

Spec reference: read `.study-guide-spec-focused.md` sections "Shape of the new UI" (rail behavior), "Deep links to curriculum_render", and "New / changed symbols in study_guide_diagnostic.html" before starting.

Files you may edit:
- `C:/Users/rober/Downloads/Projects/school/follow-alongs/study_guide_diagnostic.html` — add ONLY new functions inside the IIFE, immediately after `buildProbes` / `probesFor` / `buildProbesLegacy` (roughly line 420). Do NOT modify any existing function.

You may NOT edit any other file.

Context you need:
- `EMBEDDED_CURRICULUM` is an array of question objects (MCQ-style) with `id`, `type`, `prompt`, `answerKey`, etc. Questions are tagged via `window.SG_QUESTION_LO_MAP.questions[id]` which returns `{primaryLoId, secondaryLoIds, lesson}` or null.
- `tagEntry(qid)` already exists and returns that tag record.
- `loIdsForQuestion(qid)` already exists and returns `[primary, ...secondary]`.
- `unitTopology(unit)` already exists and returns `topology.units[unit]`.
- `ensureMasteryState(unit)` already exists.
- `getUnit(unit)` already exists.
- `window.ProbeSelector.selectProbes({unit, count, curriculum, tagMap, masteryState, alreadyAnswered, BKT})` returns picks with `{question, lesson, loIds, primaryLoId}`.
- `window.BKT.DEFAULT_PARAMS.pInit` is the default prior (0.3).

Do:
1. Add `function pickProbeForLo(unit, loId)`:
   - If `unit` or `loId` is missing, return null.
   - Scan `EMBEDDED_CURRICULUM` for questions whose `id` starts with `'U' + unit + '-L'`, whose type is `'multiple-choice'`, and whose `tagEntry(id)` has `primaryLoId === loId` OR `secondaryLoIds` includes `loId`.
   - Among those matches, prefer ones whose `id` is NOT a key in `getUnit(unit).mcqResults`.
   - If an untouched match exists, return `{unit, questionId: q.id, question: q, lesson: tag.lesson, loIds: loIdsForQuestion(q.id), primaryLoId: tag.primaryLoId}`.
   - If all matches are touched, return the first touched one in the same shape (so the student can still review it).
   - If no matches at all, fall back to `ProbeSelector.selectProbes({unit, count:1, curriculum:EMBEDDED_CURRICULUM, tagMap:window.SG_QUESTION_LO_MAP, masteryState:ensureMasteryState(unit), alreadyAnswered:Object.keys(getUnit(unit).mcqResults), BKT:window.BKT})[0]`. If the selector returns a pick, wrap it in the same shape and return it. Otherwise return null.
2. Add `function pickNextWeakest()`:
   - Build a flat list `[{unit, loId, mastery}, ...]` by iterating `topology.units[u].nodes[i].loIds` for every unit 1..9. `mastery` comes from `ensureMasteryState(u)[loId]`, defaulting to `window.BKT.DEFAULT_PARAMS.pInit` when missing.
   - Deduplicate by `unit + ':' + loId`.
   - Sort ascending by `mastery`.
   - Walk the sorted list; for each entry, call `pickProbeForLo(unit, loId)` and return the first non-null result.
   - If nothing matches, return null.
3. Add `function buildCurriculumLink(unit, lesson, questionId)`:
   - If `questionId` is a string, return `'../curriculum_render/index.html?q=' + encodeURIComponent(questionId)`.
   - Else return `'../curriculum_render/index.html?u=' + unit + '&l=' + lesson`.
4. Add `function setActiveProbe(unit, questionId, source)`:
   - Validate `source` is one of `'manual'|'next-weakest'|'lo-click'`; default to `'manual'` if invalid.
   - Set `state.activeProbe = {unit: unit, questionId: questionId, source: source}` on the top-level state object.
   - Call `saveSoon()` (or `saveNow()` if that's the pattern in this file — look at how mcqResults saves work).
   - Do NOT call any render function. Batch M wires the render hooks.
5. Do NOT touch:
   - renderUnit, renderUnits, renderDagPanel, rerenderUnit
   - any state function (makeDefaultState, normalizeState, getUnit, runFocus, applyFrqFocusToBkt)
   - Batch A's CSS, #sg-layout markup, or `applyTheme`
   - `tests/study-guide.test.js`
   - any file under `lib/` or `data/`

Acceptance:
- `pickProbeForLo(5, 'VAR-6.A')` returns an object whose `primaryLoId === 'VAR-6.A'` (or it's in `secondaryLoIds`) and whose `questionId` starts with `'U5-L'`.
- `pickNextWeakest()` after seeding `state.units[2].masteryState = {'DAT-1.D': 0.1}` returns a probe for unit 2 whose tagged LOs include `'DAT-1.D'`.
- `buildCurriculumLink(5, 2)` returns `'../curriculum_render/index.html?u=5&l=2'`.
- `buildCurriculumLink(4, 2, 'U4-L2-Q01')` returns `'../curriculum_render/index.html?q=U4-L2-Q01'`.
- None of your new functions are called from anywhere else yet (that's Batch M's job), so existing tests should be unaffected.
- `npm test -- tests/study-guide.test.js` should pass with the same status as before you started (passes or fails on the same cases).

Self-check:
- `npm test -- tests/study-guide.test.js 2>&1 | tail -30`
- `npm test -- lib/ 2>&1 | tail -20`

Report back: what functions you added, their line range, and the test output tail.
