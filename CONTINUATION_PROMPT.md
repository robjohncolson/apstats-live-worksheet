# Continuation Prompt — TI-84 CE Procedural Trainer

**Last updated**: 2026-04-15 (session 87 — applied session 86 audit fixes: 3 map drops + 6 FRQ skill drifts + 6 cartridge wording)
**Status**: TI-84 trainer V3 shipped + Physical Calculator Mode is now primary (default). CEmu/WASM emulator demoted to a legacy option behind the Options dialog because school networks block Supabase and WASM boot fails. All core features implemented. **Parallel track**: `study_guide_diagnostic.html` v6 **shipped tonight** — FRQ decomposition feature with helper-tracking penalties, 91/91 study-guide tests green. See entry 77.
**Deadline**: ~May 7 (AP exam, 24 days out)

---

## What This Project Is

A standalone single-file HTML webapp that trains AP Statistics students on the **mechanical key-press sequences** for every TI-84 Plus CE procedure used across Units 1-9. Students interact with a real CEmu-emulated calculator (ROM via Supabase), while a native JS state machine validates keystrokes and drives guided walkthroughs. SRS scheduling ensures durable memory.

## Architecture (V3 — Current)

```
Student clicks virtual key
  → Native state machine: "Is this the right key?"
  → If correct: pass to CEmu → LCD renders real calculator screen
  → If wrong: blocked, show feedback
  → Clutch system: can pause guidance to fix errors freely

ROM loaded from Supabase bucket → cached in IndexedDB → never re-downloaded
Calculator resets to HOME on every new problem
```

### Key Components

| Component | Role |
|-----------|------|
| CEmu WASM (`wasm/WebCEmu.js + .wasm`) | Runs real TI-84 ROM in browser, renders LCD |
| Native module (`native/*.js`, 9 files) | State machine, stat math, menu nav, form engine |
| Bridge (`bridge.js`) | CEmu interface + Supabase ROM auto-loader |
| App (`app.js`) | Walkthrough engine, clutch, SRS, UI |
| Procedures data (`ti84-procedures-data.json`) | 27 procedures, 384+ steps, 65 screens |

### Clutch System (3-phase walkthrough)

```
Phase 1: DATA SETUP (clutch disengaged)
  - Auto-fill types data into CEmu lists/matrices
  - Or student enters manually, clicks "I'm done"
  - 12 of 27 procedures need this phase

Phase 2: PROCEDURE (clutch engaged)
  - State machine validates keystrokes
  - Guided mode: highlights next key, blocks wrong keys
  - Recall mode: no highlights, hints count as misses
  - Pause button: disengage mid-procedure to fix errors

Phase 3: RESULT REVIEW (clutch disengaged)
  - Free exploration of result screen
```

## Files

### Core App

| File | Purpose |
|------|---------|
| `ti84-trainer-v2/standalone.html` | Built single-file bundle (~461KB), what students open |
| `ti84-trainer-v2/app.js` | Walkthrough engine, clutch, phases, UI rendering |
| `ti84-trainer-v2/bridge.js` | CEmu WASM bridge + Supabase ROM auto-download + IndexedDB cache |
| `ti84-trainer-v2/style.css` | Calculator skin CSS (EZ-Spot yellow bezel model) |
| `ti84-trainer-v2/build.mjs` | Assembles standalone.html from modules + data |
| `ti84-trainer-v2/index.html` | Dev entry point (loads modules separately) |

### Native Module (State Machine)

| File | Lines | Tests | Purpose |
|------|-------|-------|---------|
| `native/event-bus.js` | 48 | — | Pub/sub event system |
| `native/stat-math.js` | ~620 | 72 | All stat computations (normal, t, chi-sq, binomial, regression) |
| `native/menu-nav.js` | ~200 | 37 | STAT/DISTR/CALC tab navigation |
| `native/menu-tables.js` | ~280 | — | 12 menu screens extracted from procedures data |
| `native/field-tables.js` | ~540 | — | 20 wizard field tables + 14 result templates |
| `native/form-engine.js` | ~600 | 85 | Wizard cursor, field types, Data/Stats toggle |
| `native/result-formatter.js` | ~100 | 25 | TI-84 number formatting + result line templates |
| `native/screen-renderer.js` | ~350 | — | 320x240 LCD canvas rendering + mock graphs |
| `native/ti84-native.js` | ~600 | 57 | Orchestrator, bridge-compatible API |
| `native/tests/verify-all-procedures.test.js` | ~250 | 78 | Automated walk-through of all 27 procedures |
| **Total** | **~8000** | **354** | |

### Data Files

| File | Purpose |
|------|---------|
| `ti84-procedures-data.json` | 27 procedures, 20 wizards, 14 results, 12 menus, 65 screens, DAG edges |
| `ti84-pattern-recognition-data.json` | 62 canonical problems, 22 confusion pairs, 27 distractor sets |
| `ti84-rom-disassembly-results.json` | ROM-extracted wizard field tables, confidence levels |
| `ti84-rom-wizard-fields.md` | String offsets, token byte map from ROM extraction |

### CEmu WASM

| File | Purpose |
|------|---------|
| `wasm/WebCEmu.js` | Emscripten glue code (73KB) |
| `wasm/WebCEmu.wasm` | CEmu WASM binary (112KB) |

### Research / ROM Artifacts (not used by trainer)

| File | Purpose |
|------|---------|
| `TI-84_Plus_CE/ROM.transpiled.js` | Codex byte-lifted JS from ROM (384 blocks, startup path only) |
| `TI-84_Plus_CE/ROM.transpiled.report.json` | Coverage stats for transpilation |
| `scripts/transpile-ti84-rom.mjs` | Generator script (requires z80js) |

### Specs & Prompts (historical, in repo)

| File | Purpose |
|------|---------|
| `ti84-trainer-spec.md` | Original V1 spec |
| `ti84-native-port-spec.md` | Native module architecture (7 modules) |
| `ti84-v3-spec.md` | V3 architecture (CEmu primary + native state machine) |
| `ti84-clutch-spec.md` | Clutch system, data seeding, list memory |
| `codex-native-port-prompts.md` | 5-agent parallel Codex dispatch for native module |
| `codex-v3-prompt.md` | V3 integration Codex prompt |
| `codex-clutch-prompt.md` | Clutch system Codex prompt |

## Key Design Decisions (Already Made)

1. **CEmu is primary UI** — students see the real emulated calculator, not a mid-fidelity mock
2. **Native module is the state machine** — validates keystrokes, tracks screen state, never renders to LCD
3. **ROM from Supabase** — auto-download, IndexedDB cache, no file picker. URL: `https://bzqbhtrurzzavhqbgqrs.supabase.co`, bucket: `ti84-trainer-assets`
4. **Clutch system** — disengage/engage state machine for data setup, error correction, result review
5. **Auto-fill** — trainer types sample data into CEmu lists/matrices via key presses
6. **ALPHA + key for menu letters** — items A-H in TESTS/DISTR menus require ALPHA then the physical key
7. **Calculator skin** — matches physical TI-84 Plus CE EZ-Spot model (charcoal body, yellow bezel, blue 2nd, green alpha)
8. **Reset on every problem** — both CEmu and native reset to HOME before each walkthrough
9. **List memory** — persists in localStorage, always shows data setup phase (never silently skips)
10. **CEmu global stubs** — `emul_is_inited`, `initFuncs`, `initLCD`, `enableGUI`, `disableGUI` defined before `callMain()`
11. **`.gitattributes`** — marks `.wasm`, `.rom`, `.8xv`, `.8xp`, `.sqlite` as binary (prevents CRLF corruption)

## Feature Status (All Core Complete)

| Feature | Status | Notes |
|---------|--------|-------|
| Pattern Recognition (Track 1) | DONE | `buildQuestion()`, 62 canonical problems, 22 confusion pairs, distractor MCQ, branch walkthroughs |
| Clutch System (3 phases) | DONE | data-setup → procedure → result-review, auto-fill + manual entry, pause/resume mid-procedure |
| Auto-Fill (lists + matrices) | DONE | Navigates STAT>EDIT, types values, progress pills. Tested on chi-squared GOF |
| SRS Track 1 (pattern) | DONE | SM2, exposure counting, quality 0-5 from branch count |
| SRS Track 2 (navigation) | DONE | SM2, guided→recall progression, demotion on 3+ errors or 2+ hints |
| Walkthrough (guided + recall) | DONE | Key blocking, error/hint tracking, narration, common error messages |
| CEmu Integration | DONE | WebCEmu WASM boots real ROM, LCD rendering, key sending, mock fallback |
| Supabase ROM | DONE | Signed URL, streaming download, IndexedDB cache, version validation |
| List Memory | DONE | localStorage persistence, match checking, always shows data setup phase |
| Pause/Resume Guidance | DONE | Clutch disengage mid-procedure, free keys, resume from current step |
| Calculator Skin | DONE | Photo-accurate EZ-Spot model colors, 6-column keypad grid |
| Mobile Layout | DONE | `@media (max-width: 600px)`: full-bleed, compact walkthrough bar, sticky narration, 42px keys, icon buttons |
| Answer Verification | DONE | "Check Your Answer" card in result-review phase, 23 procedures, uses native computed values with 0.5% tolerance fallback |
| Physical Calculator Mode | DONE (PRIMARY) | Default-on. Renders instruction cards (Press KEY / narration / expected / tips / Back, I did it) so students follow along on their real TI-84. Bypasses `pressButton` validation and just advances `routeState`. Zero network, works offline. |
| Options Dialog | DONE | Titlebar "Options" button opens a small dialog housing Firmware + mode toggle. Keeps the physical view uncluttered. |
| Choice Button Flash | DONE | Track 1 choice buttons flash green (correct) or red (wrong) for 650ms before the panel transitions, so students see which button they hit. |

## Known Issues / Remaining Work

### Layout
- At 100% zoom on 1366x768, user may need to scroll to see full keypad
- Previous auto-fit attempts (clamp/flex) made LCD too small or keys unreadable — rolled back

### Procedures
- All 27 procedures verified via automated tests (354 passing, 0 discrepancies)

### Not Yet Built
- **Student pilot testing** — need real students running through procedures
- **Graph rendering** — native module has mock graphs; CEmu shows real graphs
- **STAT>EDIT data entry walkthrough** — could be its own guided procedure teaching list entry

### Research Artifacts (Not on Critical Path)
- `TI-84_Plus_CE/ROM.transpiled.js` — Codex-generated byte-lifted JS from ROM. 384 blocks, 0.086% coverage. Startup path only. Not used by the trainer; kept as reference. Regenerate with `node scripts/transpile-ti84-rom.mjs`

## Commands

```bash
# Run native module tests
cd ti84-trainer-v2/native && npx vitest run --config vitest.config.js

# Rebuild standalone.html after changes to app.js/style.css/data
node ti84-trainer-v2/build.mjs

# Serve locally for testing (needed for WASM dynamic import)
cd ti84-trainer-v2 && python -m http.server 8000
# Then open http://localhost:8000/standalone.html

# Run all project tests (from repo root)
npm test
```

## Supabase Configuration

```
Project URL: https://bzqbhtrurzzavhqbgqrs.supabase.co
Bucket: ti84-trainer-assets (public)
File: ROM.rom (TI-84 Plus CE OS 5.8.2.0029, 4MB)
Config in: ti84-trainer-v2/bridge.js line 11 (ROM_CONFIG.supabaseUrl)
```

## Session History

**Sessions 1-64**: See `git log --oneline` for commit-level history.

**Sessions 65-75**: Full narratives in `SESSIONS_ARCHIVE.md` — covers QR button, Physical Mode pivot, Options dialog, choice flash, study guide v2/v3/v4/v4-polish.

**Sessions 76-77**: Full detail below (current + previous session).

76. **Study guide v5 — dose ladder + hybrid MCQ/FRQ queue shipped** (2026-04-14 session 76). Closed entry 75 open item (a) via full end-to-end jsdom smoke test, shipped two commits to master, and locked in the FRQ decomposition feature design for session 77.
    - **Hard rule captured — `curriculum_render/data/curriculum.js` is SACRED**. Mid-session, Task C had dispatched Codex to add Power/Type-I/Type-II MCQs directly to `../curriculum_render/data/curriculum.js`. User interrupted: "That is supposed to be sacred.. they come directly from AP classroom." Killed the Codex process before any writes landed (`git status` on curriculum_render confirmed clean). New hard rule: all MCQ additions go into `follow-alongs/data/formula-probe-supplement.js` (the `EMBEDDED_CURRICULUM_SUPPLEMENT` array), never into `curriculum_render/`. Memory file saved: `memory/feedback_curriculum_render_sacred.md` + `MEMORY.md` gained a "Hard Rules" section above "Project Structure" linking to it.
    - **Entry 75 open item (a) — v4 polish commit + ship**. The Batch A polish from entry 75 (clickable See-all cells, "untested" gray dot for absent touchedFormulas, 29 new CSS rules) was sitting in the working tree but had never been committed — HEAD at session start was `2147a03` which still had the old broken render. Shipped as commit `0c0f736` ("study guide v4 polish — see-all clickable + untested labels + 29 CSS rules"), pushed to master. No code change needed; the fix already existed on disk, just needed to leave dev purgatory.
    - **v5 ladder design decisions (locked in with user)**. User feedback on v4: "only 4 MCQ's this close to the exam seems too lenient" + wants the queue to respond to student behavior, not just calendar. Agreed shape:
      * **4-tier ladder**: Tier 0 Warmup (5 MCQ + 1 FRQ, green) / Tier 1 Steady (7 + 1, yellow) / Tier 2 Catch-up (10 + 2, orange) / Tier 3 Crunch (12 + 2, red).
      * **Two triggers, max-of wins**: calendar day-of-study-period (`TIER_CALENDAR_DAYS = [0, 8, 15, 21]`, indexed by tier) OR debt (`debtToTier(debt) = min(3, debt)`). So a student hitting day 8 auto-bumps to tier 1 even if zero debt; a student missing 2 days at tier 0 also lands in tier 2. `computeDoseTier = max(calendarTier, debtToTier(mcqDebt), debtToTier(frqDebt))`.
      * **Working ahead cancels debt**: if `mcqCompleted.length > mcq.length` (student did more than assigned), the surplus subtracts from tomorrow's debt. `debt = max(0, oldDebt + shortfall - surplus)`.
      * **Separate MCQ debt and FRQ debt tracks**. Skipping yesterday's FRQ doesn't burn the MCQ tier (and vice versa). Both feed into `computeDoseTier` as peers.
      * **Hybrid queue with tabs** (hard UX constraint: "only one problem displayed at a time"). `dailyDose` now has `mcq[]`, `mcqCompleted[]`, `frq[]`, `frqCompleted[]`, `activeTab`. Two tab buttons at the top of the queue pane, list below shows only the active tab. `advanceDailyQueueV5` is tab-aware: advances within the current tab; when current tab exhausts, flips `activeTab` to the other pool and returns its first uncompleted entry; returns null when both exhausted.
      * **Tier meter UI**: 4 segments colored green/yellow/orange/red, segments `0..tier` get `.is-active`, info button `ⓘ What do tiers mean?` opens a modal listing all 4 tiers with trigger conditions pulled from `LADDER` + `TIER_CALENDAR_DAYS` (so text stays in sync with logic).
      * **FRQ picker**: `pickDailyFrqs(state, n)` iterates units 1..9, skips units where `state.units[u].frqGrade` is already set (graded this session), sorts remaining by `unitMeanMastery` (mean of `unitState.masteryState` values, fallback 0.3), takes weakest `n`. Gate question IDs hardcoded in `GATE_IDS_V5 = {1:'U1-PC-FRQ-Q02', ..., 9:'U9-PC-FRQ-Q01'}`.
      * **FRQ paper-mode button**: each FRQ entry in `renderActiveProbe` gets `.sg-frq-paper-toggle` ("I'll write this on paper"). Clicking flips `data.frqOnPaper`, changes textarea placeholder to "Write on paper and hand in to Mr. Colson — AI grading skipped," changes Grade button to "Mark complete (paper)", and skips the `/api/ai/grade` call entirely — stores `{score: 'paper', feedback: 'Written response submitted to teacher.', submittedAt: todayISO}`.
    - **Implementation (two parallel Codex batches, then integration)**.
      * **Batch A (pure logic + tests, parallel)** — wrote `.v5-ladder-block.js` (568 lines, 16.9KB). IIFE publishing `window.__studyGuideV5__` with constants (`LADDER`, `STUDY_START_DATE='2026-04-14'`, `TIER_CALENDAR_DAYS`, `GATE_IDS_V5`) + pure functions (`daysSinceStudyStart`, `calendarTier`, `debtToTier`, `computeDoseTier`, `tierSpec`, `updateDebtFromPriorDose`, `unitMeanMastery`, `pickDailyFrqs`, `pickDailyQueueV5`, `advanceDailyQueueV5`, `setActiveTab`, `buildMcqQueue`). Re-exports v4 helpers (`daysLeft`, `computeDailyDose`, `formulaMastery`, `formulaWeight`, `formulaName`, `touchedFormulaCount`, `pickProbeForFormula`, `recordFormulaTouch`, `FORMULA_UNIT_MAP`, `UNIT_EXAM_WEIGHTS`, `TIER_MULTIPLIERS`, `BKT_INIT`, `AP_EXAM_DATE`) via `getV4()` reading `window.__studyGuideV4__` at call time so both browser and synthetic-window tests work. `buildMcqQueue` reimplements v4's coverage-floor + weight-sort loop but caps the dose at `spec.mcq` instead of `ceil(remaining/days)`. `updateDebtFromPriorDose` mutates state idempotently, floors at 0, no-ops if `dailyDose.date === today` or `dailyDose` is null. Tests: `tests/study-guide-v5-ladder.test.js` (14KB, 22 tests) using same synthetic-window pattern as v4 — `readFileSync` both `.v4-logic-block.js` and `.v5-ladder-block.js`, run v4 first to populate `__studyGuideV4__`, then v5. Covers all ladder math, tier boundaries (day 1/8/15/21), debt shortage/surplus/idempotency, `pickDailyFrqs` weakest-first + skip-graded, `pickDailyQueueV5` idempotent per date + regenerates on day change, `advanceDailyQueueV5` tab flipping + null when both pools exhausted, working-ahead cancels debt, 2-day skip bumps to tier 2.
      * **Batch M (serial HTML integration)** — surgical edits to `study_guide_diagnostic.html`. (1) Pasted `.v5-ladder-block.js` body inline into the main IIFE after the v4 logic block (lines 784-1423), updated destructure to pull from `window.__studyGuideV5__` (includes new v5 exports + keeps v4 helper names since v5 re-exports them). (2) State schema bump: `STORAGE_KEY='apStatsStudyGuideDiagnostic.v5'`, `SCHEMA_VERSION=5`, `STATE_ID='sg-state-v5'`; `makeDefaultState()` gains `doseLadder: {tier:0, mcqDebt:0, frqDebt:0}`; `normalizeState()` accepts schemaVersion 5 and adds v4→v5 migration branch that carries forward `touchedFormulas` + `units` but resets `dailyDose` to null (v4's flat queue shape is incompatible with v5's mcq/frq split). (3) `init()` localStorage read chain tries `.v5` → `.v4` → `.v3` → `.v2` with normalizeState handling each migration; after load calls `state.dailyDose = pickDailyQueueV5(state, today(), AP_EXAM_DATE)` replacing the old v4 `pickDailyQueue` call. (4) `renderQueuePane()` rewritten: `.sg-queue-header` with title from `tierSpec(tier).mcq/.frq` + subtitle from `LADDER[tier].label` and `daysLeft`, `.sg-tier-meter` with 4 colored segments, `.sg-tier-info` button opening a modal; `.sg-queue-tabs` with MCQ/FRQ tab buttons showing `completed/total` badges and calling `setActiveTab` on click; `<ol class="sg-queue-list">` rendering entries from the active tab; `.sg-see-all` disclosure stays unchanged from v4 polish. For MCQ entries label is `formulaName(formulaId)` with `Unit N · L M`; for FRQ entries label is `Unit N FRQ Gate` with `Mastery: X%` from `unitMeanMastery`. (5) `advanceDailyQueue()` now delegates to `advanceDailyQueueV5(state, currentId)` — call sites in the MCQ Next button and FRQ Grade button unchanged. (6) `renderActiveProbe` FRQ branch gains `.sg-frq-paper-toggle` button before the Grade button with the paper-mode toggle described above. (7) New `renderTierInfoModal()` renders backdrop + modal with all 4 tier rows (`sg-tier-dot` color swatch + label + trigger conditions pulled from `LADDER`/`TIER_CALENDAR_DAYS`) + the explainer paragraph. Backdrop click or Esc closes. (8) ~29 new CSS rules for `.sg-queue-header`, `.sg-tier-meter`/`.sg-tier-seg.is-tier-{0..3}`, `.sg-queue-tabs`/`.sg-queue-tab`, `.sg-modal-backdrop`/`.sg-modal`/`.sg-tier-row`, `.sg-frq-paper-toggle`. Tier colors: `#2e7d32` green / `#f9a825` yellow / `#ef6c00` orange / `#c62828` red.
    - **Structural oddity**: `study_guide_diagnostic.html` line 294 has a bare `{` opening a block scope that closes at line 783, wrapping all v4 code. This is valid JS (just an unusual block scope from the entry 74 inline paste) — v4 publishes `window.__studyGuideV4__` at line 764 inside the block, and the v5 IIFE at line 784 reads from `window.__studyGuideV4__` via `getV4()` at call time, so it works. Not a bug, just worth knowing.
    - **Test updates**: `tests/study-guide.test.js` v4 describe block converted to v5 structural assertions (`window.__studyGuideV5__` published, `LADDER` constant present, `STORAGE_KEY` uses `.v5`, `SCHEMA_VERSION=5`, `sg-tier-meter`/`sg-queue-tabs`/`sg-frq-paper-toggle` classes present, `pickDailyQueueV5` replaces `pickDailyQueue` at init site, `function advanceDailyQueueV5` present in inline logic). Kept existing describes for supplement probes and theme toggle.
    - **Verification**: `tests/study-guide.test.js` + `tests/study-guide-v5-ladder.test.js` both pass — 56/56 green. jsdom end-to-end smoke test confirmed MCQ tab renders "Sample Mean (x-bar) · Unit 1 · L7" as first queue entry with `(0/5)` badge, FRQ tab renders "Unit 1 FRQ Gate" with textarea + Grade + paper toggle, tier meter shows tier 0 active (green), info modal lists all 4 tiers. **Gotcha discovered**: the initial smoke probe showed MCQ queue empty — root cause was the probe script didn't use `ResourceLoader`, so `<script src>` data files weren't loading. Fix: `import { JSDOM, ResourceLoader } from 'jsdom'; new JSDOM(..., { resources: new ResourceLoader() })`. Keep this in mind for any future jsdom smoke probing.
    - **Files (new / modified)**: new — `.v5-ladder-block.js`, `tests/study-guide-v5-ladder.test.js`, `.v5-batch-a.md`, `.v5-batch-m.md`, `memory/feedback_curriculum_render_sacred.md`, `.supplement-audit-reaudit.md`. modified — `study_guide_diagnostic.html` (+~907 lines: v5 logic block paste + renderQueuePane rewrite + tier modal + CSS + state migration), `tests/study-guide.test.js` (v4→v5 structural assertions, +31 lines), `memory/MEMORY.md` (Hard Rules section added).
    - **Commits shipped this session**: `0c0f736` (v4 polish clickable see-all + 29 CSS rules — was sitting uncommitted from entry 75), `d91e95c` (v5 dose ladder + hybrid MCQ/FRQ queue + tier meter + info modal + FRQ paper-mode button). Both pushed to master.
    - **Entry 75 open items status**: (a) Browser smoke test — **CLOSED** via jsdom end-to-end. (b) 16 supplement probes still zero real-student signal — **STILL OPEN**, continue monitoring. (c) Power/Type-I/II still thin in main curriculum_render bank — **PERMANENTLY DEFERRED** per sacred-file rule; supplement covers it, main bank will not be touched. (d) U4-L9-QS1 + U6-L4-QS1 re-audit — **CLOSED** via Task D, both rated CLEAN with TEACHES reasoning (`.supplement-audit-reaudit.md`).
    - **PENDING FOR SESSION 77 — FRQ decomposition feature (all 5 forks locked in by user, not yet implemented)**. User direction: each FRQ should be analyzed offline into skills with supporting formulas + MCQs. Students can trade score for hints. Also solves the "right pane shows '?' for FRQ lesson number" problem.
      * **Fork 1 — A: pre-computed**. Decompositions authored offline by one Codex agent, saved to `data/frq-decompositions.json`. No live AI calls during grading.
      * **Fork 2 — numeric scoring + per-help penalties**. `E=1.0, P=0.6, I=0.2`. Penalties: **formula card = 5%**, **correct MCQ drill = 10%**, **wrong MCQ drill = 15%**. Total penalty capped at **50%**. `effectiveScore = rawNumeric × (1 - totalPenalty)`.
      * **Fork 3 — split view (iii)**. When a helper MCQ is active, it replaces the right-pane remediation; the FRQ stays in the center pane. Same decomposition data also populates the right-pane lesson details that currently show "?" for FRQ active probes — **one data source fixes both problems**.
      * **Fork 4 — Model Y punitive**. Wrong helper answer costs more than right helper answer (15% vs 10%) — the student pays for being wrong, not just for asking.
      * **Fork 5 — no undo, confirmation modal on first helper use per FRQ**. "Using helpers will reduce your score. Continue?" once per FRQ per session; subsequent helpers tap through silently.
      * **Data shape proposed**: `{"U1-PC-FRQ-Q02": {"unit":1, "topic":"...", "skills":[{"id":"...", "name":"...", "whyItMatters":"...", "supportingFormulas":["mean","std-dev"], "supportingMcqIds":["U1-L4-Q03",...], "penalty":0.1}], "maxPenalty":0.5}}`.
      * **Implementation batches proposed**: Batch A (data authoring) = ONE Codex agent reads all 9 FRQ prompts (found in `ai-grading-prompts-study-guide.js` via `GATE_IDS_SG`) + rubrics + `curriculum_render/data/curriculum.js` (read-only! sacred!) + framework + cartridge, produces decomposition JSON sequentially for consistency. Batch B (logic layer) = `getFrqDecomposition`, `recordHelperUsed`, `computeEffectivePenalty`, `computeEffectiveScore` + unit tests. Batch C (render integration) = `renderFrqHelpers` in `renderActiveProbe` FRQ branch, split-view right-pane helper MCQ display, confirmation modal, dual raw/effective score display.
    - **Open items for session 77**: (a) Implement the FRQ decomposition feature per the 5-fork design above. Start with Batch A (data authoring) since B + C both depend on the JSON shape — don't parallelize. (b) 16 supplement probes still have zero real-student signal — continue monitoring first week of use. (c) After Batch A lands, `data/frq-decompositions.json` should be sanity-checked by a second Codex agent (cheap, catches authoring drift before the UI goes live). (d) The block-scope oddity at study_guide_diagnostic.html line 294 is still there; harmless but worth flattening next time the file gets a major edit.

- **Entry 77 — v6 FRQ Decomposition feature SHIPPED (2026-04-14 late)**. All 5 forks from entry 76 landed via 4 Codex sub-agent dispatches (CC orchestrated, Sonnet 4.6 plan agent broke down the implementation into 1132-line plan → `.plan-session77.md`). Cross-agent dispatch via `.dispatch-codex.py` wrapper that reads prompt from file to avoid bash single-quote escaping issues.
    - **Batch A (data authoring, single sequential Codex)** — wrote `data/frq-decompositions.json` (~15.5 KB) with **31 skills across all 9 Progress Check FRQ gate questions** (U1-PC-FRQ-Q02 through U9-PC-FRQ-Q01). Each FRQ has 2-4 skills with `id/name/whyItMatters/supportingFormulas/supportingMcqIds/penalty` fields + `maxPenalty: 0.50`. Codex made several rubric-driven adjustments from the prompt guidance: substituted `ci-formula` for `p-value-interp` in U6/U7 (both resolve in cartridge — verified), dropped `U4-L8-Q01` and `U6-L5-Q01` where rubric called for different support, omitted `slope-ci` from U9 since that FRQ centers on a slope t-test rather than interval, added a distribution-probability skill to U4, reshaped U5 around histogram reading plus estimator comparison, split U7 and U9 to honor separate interpretation and hypothesis-writing rubric points.
    - **Batch A2 (audit, second Codex)** — produced `.frq-decomp-audit.md`: **5 PASS / 4 WARN / 0 FAIL**, zero unresolved formula/MCQ IDs. All 4 WARN entries are conceptual-only skills without tool support (U3 generalization/causation, U5 histogram/midrange, U7 scope-limit, U9 slope-hypotheses) which are acceptable by design since the `whyItMatters` field justifies the absence. No fixes needed.
    - **Batch B (logic layer + 30 tests, third Codex)** — wrote `.v6-frq-decomp-block.js` (184 lines) as IIFE publishing `window.__studyGuideV6__` with 4 functions: `getFrqDecomposition(questionId)`, `recordHelperUsed(unit, kind, payload, state)` with first-use-wins dedup, `computeEffectivePenalty(frqHelpers, decomposition)` returning [0, 0.50], `computeEffectiveScore(rawScore, penalty)` returning `{raw, rawNumeric, effective, penaltyPct, breakdown}`. Paper mode returns null effective score. Unknown raw scores normalize to `'I'`. Penalty cap applies `Math.min(decomposition.maxPenalty, 0.50)`. Tests: `tests/study-guide-v6-frq-decomposition.test.js` (327 lines, 30 tests) using synthetic-window pattern (`readFileSync` + `new Function('window', src)`). **CC fix**: one test used `toBe(0.30)` for the sum 0.05 + 0.10 + 0.15 which is `0.30000000000000004` in float — changed to `toBeCloseTo(0.30, 10)`. All 30 tests pass.
    - **Batch C (render integration, fourth Codex)** — surgical edits to `study_guide_diagnostic.html` (+421 lines, 2598 → 3019), creates `data/frq-decompositions.js` as sync wrapper publishing `window.FRQ_DECOMPOSITIONS`, adds 5 `it()` blocks to `tests/study-guide.test.js`. HTML changes: (1) bumped `STORAGE_KEY → 'apStatsStudyGuideDiagnostic.v6'`, `SCHEMA_VERSION = 6`, `STATE_ID = 'sg-state-v6'`; (2) pasted `.v6-frq-decomp-block.js` body inline into the main IIFE after the v5 logic block, added destructure from `window.__studyGuideV6__`; (3) added `defaultFrqHelpers()` + `normalizeFrqHelpers()` helpers; (4) `makeDefaultState` + `getUnit` + `normalizeState` all thread `frqHelpers: defaultFrqHelpers()` through per-unit state; (5) `normalizeState` version check expanded to accept `=== 6`, `doseLadder`/`dailyDose` gating now allows v5 or v6; (6) `init()` localStorage fallback chain v6 → v5 → v4 → v3 → v2; (7) new CSS rules for `.sg-frq-helpers`, `.sg-frq-helper-formula-btn`, `.sg-frq-helper-drill-btn`, `.sg-frq-helper-modal`, `.frq-grade-effective`, `.sg-helper-drill-card`; (8) `renderActiveProbe` FRQ branch gains helper panel after the answer/actions section listing skills with formula cards + "Drill me" buttons; (9) new `handleHelperClick`, `doHelperAction`, `showFrqHelperModal` functions with first-use confirmation modal + no-undo pattern; (10) `renderGrade` signature now accepts `effectiveScoreResult` and renders both raw and effective score; (11) `renderRemediation` gains `isFrqGate` branch with early return — uses `getFrqDecomposition(questionId)` instead of `tagEntry(questionId)`, renders active drill MCQ if one is set, otherwise renders the skill list with per-skill tool tabs (this fixes the "Lesson ?" display bug for FRQ active probes automatically, since the decomposition data supplies the names directly).
    - **Verification**: `npx vitest run tests/study-guide.test.js tests/study-guide-v5-ladder.test.js tests/study-guide-v6-frq-decomposition.test.js` → **91/91 green** (39 + 22 + 30). Full suite had 23 failures in `tests/schedule.test.js` but those are **pre-existing, unrelated** to v6 (last commit to `unit4_schedule_v4.html` was 95ec006, not this session — v6 touched only HTML + tests/study-guide.test.js + new files).
    - **Files (new / modified)**: new — `data/frq-decompositions.json`, `data/frq-decompositions.js`, `.v6-frq-decomp-block.js`, `tests/study-guide-v6-frq-decomposition.test.js`, `.frq-decomp-audit.md`, `.batch-a-frq-decomp.md`, `.batch-a2-frq-decomp-audit.md`, `.batch-b-frq-logic.md`, `.batch-c-frq-render.md`, `.plan-session77.md`. modified — `study_guide_diagnostic.html` (+421 lines), `tests/study-guide.test.js` (+5 `it` blocks + v5→v6 string updates).
    - **Open items for session 78**: (a) Manually click through the v6 HTML in a browser to verify the helper panel renders, confirmation modal fires, effective score shows next to raw score, and right-pane no longer shows "Lesson ?" for FRQ active probes. Tests cover the wiring but not the visual correctness. (b) Schedule.test.js regression needs a separate cleanup pass — not blocking. (c) 16 supplement probes still need real-student signal. (d) Block-scope oddity at HTML line 294 still there (harmless).

- **Entry 78 — v6 helper UI cleanup + schedule tests re-aligned + probe signal script** (2026-04-14 session 78). Session 77 open items (a')/(b)/(c)/(d) all addressed in one pass via 3 parallel Codex dispatches (spec at `.session78-spec.md`).
    - **Session 77 (a) resolved by user**: manually clicked through v6 HTML; helper panel, modal, effective score, and right-pane lesson names all work. Only the visual layout of the helper panel itself needed cleanup — kicked off as Batch A (below).
    - **Batch A — FRQ helper panel UI redesign + block-scope flatten attempt** (`.batch-a-frq-helper-ui.md`, single Codex). Edits: `study_guide_diagnostic.html` +192/-35. Previous panel was a flat pill row with duplicate "Practice drill" labels, no penalty preview, no used-state, and `whyItMatters` was never shown. New panel is a numbered `<ol class="sg-frq-skill-list">` of skill cards. Each card has: a 28px numbered badge (`.sg-frq-skill-num`), skill name (`.sg-frq-skill-name`), `whyItMatters` paragraph (`.sg-frq-skill-why`, hidden if empty), and a `.sg-frq-skill-helpers` grid (single column on mobile, 2-col on ≥600px). Helper buttons now display full descriptors: `Formula card · <strong>{action}</strong>` for formula cards (resolved via `getFormulaEntry(formulaId).action`) and `Practice drill · <strong>{descriptor}</strong>` for drill MCQs (descriptor resolved via `tagEntry(mcqId)` with fallback chain formulaName → lesson → prompt-prefix → raw id). Each button has a `.sg-frq-helper-cost` pill on the right: `−5%` for formula, `−10% / −15%` for drill. Intro copy now states all three penalty values and the 50% cap explicitly so students self-calibrate. Used-state: after `handleHelperClick` records the helper, re-render shows `data-used="true"` → background tinted `var(--sg-bar-track)`, opacity 0.72, `pointer-events:none`, `::before` checkmark glyph on the label, and for drills the cost pill updates to `−10% (correct)` or `−15% (wrong)` based on the recorded outcome. Used-state reads from existing `data.frqHelpers.formulasViewed` + `data.frqHelpers.mcqDrills` maps — Codex discovered and matched the actual data shape rather than the spec's guess. Also re-renders the active FRQ card after drill grading so recorded outcomes show immediately. **Part 2 (block-scope flatten at line 303)** was **deferred** using the spec's explicit fallback clause: flattening would cause outer-scope collisions with v5 destructured names like `AP_EXAM_DATE`, `daysLeft`, `computeDailyDose`. Bare block stays in place with a fallback comment. Preserved all test-asserted class names (`sg-frq-helpers`, `sg-frq-helper-formula-btn`, `sg-frq-helper-drill-btn`, `sg-frq-helper-modal`, `frq-grade-effective`, `sg-helper-drill-card`) so existing test wiring continues to match. **91/91 green** (verified by CC after dispatch — Codex skipped verification because the subagent wrapper forbids it).
    - **Batch B — `tests/schedule.test.js` re-alignment** (`.batch-b-schedule-tests.md`, single Codex). Was 20/43 passing (23 failures) because the test file was written against an older `unit4_schedule_v4.html`. HTML is authoritative — fixed the tests, not the HTML. Deleted: entire `Drill Cartridge Priority` describe block (4 `it()`s) — the `.priority-list` section with `U4-TABLE-TREE`/`U4-BINOM-GEO`/`U4-RV-PARAMS` no longer exists in the HTML. Updated: 21 tests — shifted schedule meta/meeting-pattern/footer/date assertions to the current Jan 21 → Feb 7 schedule, re-aligned Period B and Period E row checks to current dates and content groupings, replaced stale quiz-inventory assertions with current drill-cartridge tracker assertions, kept the `CALCULATOR_SPEAK_ONLY` tag test and updated other feedback-tag expectations to `MISSING_PARAMS` and `WRONG_RULE`. Period E footer updated from `Mon/Wed/Thu/Fri` → `Mon/Wed/Fri`. Final: **39/39 green** (was 20/43). No HTML regressions surfaced during alignment.
    - **Batch C — Supplement probe signal report script** (`.batch-c-probe-signal.md`, single Codex). New file: `scripts/supplement-probe-signal.mjs` (169 LOC, ESM, Node 18+, zero deps). Extracts probe IDs dynamically from `data/formula-probe-supplement.js` via regex `"id":\s*"(U\d-[^"]+-QS\d+)"` (confirmed 16 IDs: `U4-L9-QS1`, `U6-L4-QS{1..7}`, `U6-L6-QS{1..3}`, `U7-L2-QS1`, `U8-L5-QS1`, `U9-L1-QS1`, `U9-L2-QS{1,2}`). Fetches each from `https://curriculumrender-production.up.railway.app/api/question-stats/{id}` sequentially with 500ms between calls, classifies as `signal` (total ≥ 1) / `silent` (total === 0) / `error`, writes a markdown report to `probe-signal-reports/YYYY-MM-DD.md` with three sections (Signal table, Silent list, Errors list), prints one-line stdout summary. Exit 0 on success, 1 only if all error. Seeded `probe-signal-reports/2026-04-14.md` from the sandbox run (16 errors because no network from Codex sandbox). **User must run it on the school network to get real data** — the script is deterministic, just needs network.
    - **Dispatch mechanics**: all 3 batches fired in parallel via `.dispatch-codex.py <prompt-file>` wrappers. Batches B and C were fully independent; Batch A owned `study_guide_diagnostic.html` exclusively so no conflict. Each batch produced a `.batch-*.result.md` report in the working directory. CC ran the verification commands each Codex skipped (they were instructed to skip by the subagent wrapper).
    - **Files (new / modified)**: new — `.session78-spec.md`, `.batch-a-frq-helper-ui.md`, `.batch-b-schedule-tests.md`, `.batch-c-probe-signal.md`, `.batch-a-frq-helper-ui.result.md`, `.batch-b-schedule-tests.result.md`, `.batch-c-probe-signal.result.md`, `scripts/supplement-probe-signal.mjs`, `probe-signal-reports/2026-04-14.md`. modified — `study_guide_diagnostic.html` (+192/-35 helper panel rebuild), `tests/schedule.test.js` (4 tests deleted, 21 updated).
    - **Test status after session**: `tests/study-guide.test.js` + `tests/study-guide-v5-ladder.test.js` + `tests/study-guide-v6-frq-decomposition.test.js` = **91/91 green**. `tests/schedule.test.js` = **39/39 green** (was 20/43).
    - **Open items for session 79**: (a) Run `node scripts/supplement-probe-signal.mjs` on the school network and commit the resulting `probe-signal-reports/YYYY-MM-DD.md` — this is the actual signal-monitoring loop for the 16 supplement probes. (b) Block-scope oddity at HTML line 303 is still there and formally documented as deferred — don't try to flatten again without a full v5-destructure rename plan, the collision list includes `AP_EXAM_DATE`, `daysLeft`, `computeDailyDose` (and probably more). (c) Next real student pilot window for the v6 helper panel to generate the first wave of helper-usage data.

- **Entry 79 — formula card modal + SRS hint feed + practice/gate mode split** (2026-04-14 session 79). User reported three issues after exercising v6 in the browser: (1) formula card helper buttons record state but show no content, (2) the scoring is too punitive — max hints drops effective score to 0.5× raw, (3) formulas clicked for hints should feed the SRS queue. Opus orchestrated: Codex diagnosed the bug, Sonnet implemented all three threads, Codex verified, Sonnet patched the bugs Codex caught, Codex re-verified, Sonnet polished one last residual. Four Codex dispatches + two Sonnet subagent runs. CC only planned and orchestrated — no meaningful code touching.
    - **Bug diagnosis (Codex dispatch 1)**: `doHelperAction('formula')` at `study_guide_diagnostic.html:2878-2883` records the formula id via `recordHelperUsed` and re-renders, but `renderRemediation()` at `:2557-2620` has a **drill-only branch**. There is no formula renderer anywhere in the pipeline — no modal, no inline expansion, no toast. The cartridge (`data/ap-stats-cartridge.js:129-134`) has rich content for each formula (`action`, `latex`, `explain`, `hint`, `blanks`, `subconcepts`) but it was never wired to a display path. Diagnosis file: `.bug-diagnose-formula-card.result.md`.
    - **Sonnet dispatch 1 — implementation** (`.session79-sonnet-brief.md` → `.session79-spec.md` → implementation → `.session79-result.md`). Three threads landed in one pass:
      * **Thread 1 — Formula card modal**: new `showFormulaCardModal(formulaId, unit, data, questionId)` in `study_guide_diagnostic.html`. Renders `action` as title, `latex` as a display-math block (wrapped in `\[...\]` delimiters for MathJax), `explain` paragraph, `hint` callout, and optional `subconcepts` as `<details>/<summary>` Q&A pairs showing `sc.q` in the summary and `sc.correct` in a `.sg-callout`. Backdrop click or Esc closes; after close, `renderActiveProbe` fires so the button's used-state visual updates. `getFormulaEntry` added to `window.__studyGuideV4__` exports (v4 block + inline HTML copy). Toast after open: `📝 {action} added to your practice queue — you'll see it tomorrow.`
      * **Thread 2 — SRS hint feed**: new `recordFormulaHint(formulaId, state)` in both `.v4-logic-block.js` and the inline HTML copy. Sets `state.touchedFormulas[formulaId].hintedAt = today` without touching `lastMastery` — a click is a help-seeking signal, not a wrong-answer signal, so BKT mastery should stay put. `formulaWeight(formulaId, state)` now reads `hintedAt` and adds a linearly-decaying `+0.3 * (1 - daysSince/3)` boost for 3 days; after that the boost is 0. This is ADDITIVE on top of the existing mastery-based weight, so the existing daily-queue selector (`buildMcqQueue` → v5 tier ladder → `pickDailyQueueV5`) automatically surfaces recently-hinted formulas without needing any queue-side changes. `normalizeState` updated to preserve `hintedAt` across save/load.
      * **Thread 3 — Practice/Gate mode split**: new `data.frqMode: 'practice' | 'gate'` field per unit in the FRQ data object. Defaults to `'practice'` — hints are free (penalty=0), confirmation modal skipped, grade card shows only raw score, mode banner at the top of the FRQ card is green with a "Start gate attempt" button. Gate mode (current behavior): confirmation modal shown, penalties apply, grade card shows raw + effective + penalty breakdown, banner is amber and has no reverse button (one-way upgrade). Daily-queue FRQ clicks in `renderQueuePane` silently upgrade the clicked unit's `frqMode` to `'gate'` (idempotent — only touches if not already gate). Manual escalation via `showGateEscalationModal(unit, data, onConfirm)` with a warning: "Helpers you already used in practice mode will now count toward your score. Additional helpers you use will also lower your effective score. This cannot be undone." `computeEffectivePenalty` in `.v6-frq-decomp-block.js` + inline copy accepts a third `mode` param and returns 0 before iterating helpers when `mode === 'practice'`. State default via `normalizeState` + `makeDefaultState` + `getUnit`: missing `frqMode` defaults to `'practice'` (safe pedagogical default for any v6 state loaded without the field). No schema bump — stayed on v6 because the change is additive.
    - **Codex dispatch 2 — verification**: Thread 2 clean, Thread 3 pass with minor concern (gate warning text missed the explicit "already used will count" language). **Thread 1 BLOCKED with 4 bugs** — (a) raw TeX, the modal wrote undelimited `\\bar{x}...` into `textContent` and called `typeset()`, but MathJax requires delimiters; (b) subconcepts rendered as `[object Object]` because `make('li', '', sc)` passed the raw subconcept object instead of `sc.q`; (c) used-state formula buttons had `pointer-events:none` and no click listener, so students couldn't re-read a formula they'd already paid for; (d) null cartridge-entry fallback missing — if `getFormulaEntry` returns null, the modal shows only a title and close button. All 29 referenced formula ids resolve today but future additions could break. Verification file: `.verify-session79.result.md`.
    - **Sonnet dispatch 2 — fix pass** (`.session79-fix-result.md`). Five fixes, all localized to `study_guide_diagnostic.html`:
      * Fix 1: wrap `formulaEntry.latex` in `\[...\]` block-math delimiters — matches the MathJax 3 `displayMath` config at line 314-315. `typeset(modal)` at line 3108 now processes the delimited content correctly.
      * Fix 2: subconcept loop replaced with `<details>/<summary>` structure using `sc.q` in summary and `sc.correct` in a `.sg-callout`. Wrong distractors not rendered (kept clean per author judgment).
      * Fix 3: CSS `pointer-events:none` scoped to `.sg-frq-helper-drill-btn[data-used="true"]` only — formula buttons stay clickable. JS adds an `else if (kind === 'formula')` branch in `buildFrqHelperButton` for used formula buttons that attaches a listener calling `showFormulaCardModal` directly — no penalty, no SRS bump, no confirmation (the student already paid for the first view). Drill buttons remain locked because re-taking a drill MCQ would spam state.
      * Fix 4: null fallback body rendered as `.sg-formula-modal-not-found` paragraph: "Formula card not found for '{id}' — this is a data issue, not a rendering bug. Please flag it."
      * Fix 5: gate escalation warning text updated to explicitly say "Helpers you already used in practice mode will now count toward your score. Additional helpers you use will also lower your effective score. This cannot be undone."
      * +7 source-string regression tests in `tests/study-guide.test.js` to pin the fix text.
    - **Codex dispatch 3 — fix re-verification** (`.verify-session79-fix.result.md`). All 5 fixes verified pass. **Verdict: MINOR POLISH** with two residuals: (a) malformed subconcept entries (null or missing `.q`/`.correct`) not guarded defensively — cartridge is clean today, but a future author could ship a malformed entry and break the modal; (b) the 7 fix-pass tests are source-string style, not behavioral — catches patch-line regressions but not runtime regressions.
    - **Sonnet dispatch 3 — polish pass** (`.session79-polish-result.md`). Added defensive filter upstream of the subconcept loop: `validSubconcepts = (formulaEntry && formulaEntry.subconcepts || []).filter(sc => sc && typeof sc === 'object' && sc.q && sc.correct)`. "Check your understanding" header suppressed when `validSubconcepts.length === 0` so empty containers don't leak. +1 source-string test asserting the filter predicate is present.
    - **Dispatch mechanics**: 4 Codex dispatches via `.dispatch-codex.py`, 3 Sonnet subagent runs via the Agent tool with `model: "sonnet"` + `subagent_type: "general-purpose"`. Each Sonnet run was told: read the brief, write a spec-or-result file, implement, run tests, report. Between dispatches, CC ran `npx vitest run` directly to verify test counts (Sonnet sometimes skipped the verify step). Total Sonnet wall-clock: ~18 min first pass, ~4 min fix pass, ~1 min polish pass. Total Codex wall-clock: ~10 min diagnosis, ~8 min verification, ~6 min re-verification.
    - **Pedagogical note**: the practice/gate split is the real anti-punishment fix. Before session 79, a student who needed all hints on an E-scored FRQ got effective 0.5 — punishing help-seeking and suppressing the metacognition research says drives learning (Bjork's desirable difficulties is about retrieval effort, not help-avoidance). After session 79, practice mode hints are free; only the ~1/day daily-queue-picked FRQ applies penalty math (the gate attempt). This matches how AP actually grades — formative vs. summative separation. Students still get the full SRS benefit from practice because `recordFormulaHint` fires regardless of mode.
    - **Test counts**: baseline 91/91 → Sonnet pass 1: 126/126 (+35) → fix pass: 133/133 (+7) → polish: 134/134 (+1). Final distribution: `tests/study-guide.test.js` 70, `tests/study-guide-v5-ladder.test.js` 30, `tests/study-guide-v6-frq-decomposition.test.js` 34. Zero failures at any point after Sonnet's first pass.
    - **Files (new / modified)**: new — `.session79-sonnet-brief.md`, `.session79-spec.md`, `.session79-result.md`, `.session79-fix-result.md`, `.session79-polish-result.md`, `.bug-diagnose-formula-card.md`, `.bug-diagnose-formula-card.result.md`, `.verify-session79.md`, `.verify-session79.result.md`, `.verify-session79-fix.md`, `.verify-session79-fix.result.md`. modified — `study_guide_diagnostic.html` (+~600 lines: showFormulaCardModal, showGateEscalationModal, renderFrqModeBanner, handleHelperClick practice-skip, buildFrqHelperButton mode/used branches, renderFrqHelperPanel mode-aware header, renderActiveProbe FRQ mode banner + penalty arg, renderGrade mode-aware display, renderQueuePane silent gate upgrade, normalizeState preserving hintedAt + frqMode, CSS for modal + banner + mode-split + polish), `.v4-logic-block.js` (+recordFormulaHint, formulaWeight hint boost, getFormulaEntry export), `.v5-ladder-block.js` (proxy for recordFormulaHint + getFormulaEntry), `.v6-frq-decomp-block.js` (computeEffectivePenalty mode param), `tests/study-guide.test.js` (+31 tests across 4 describe blocks), `tests/study-guide-v5-ladder.test.js` (+8 tests: hint/weight/decay), `tests/study-guide-v6-frq-decomposition.test.js` (+4 tests: practice-mode penalty).
    - **Open items for session 80**: (a) Run `node scripts/supplement-probe-signal.mjs` on the school network (still carried from session 78). (b) Next real student pilot window to generate helper-usage data — now with three distinct signals to watch: formula modal opens, drill helper usage, practice→gate escalations. (c) Decide whether formula modal should track views in a separate telemetry bucket from penalty state — right now `formulasViewed` is dual-purpose (display state + penalty state). If future analytics want "which formulas do students re-read most in practice mode", that's not currently captured because re-opens don't mutate state. Easy fix: bump a `reopenCount` field. (d) The 7 source-string fix-pass tests are weaker than the behavioral logic tests; consider converting to jsdom-mounted tests next time the test file gets a major edit. (e) Block-scope oddity at line 303 still deferred. (f) Consider adding a "Formula defense" deep-link: clicking a formula card could optionally launch a mini-drill of the formula's own sub-concepts (already in cartridge shape) — would fuse Thread 1 + Thread 2 more tightly.

- **Entry 80 — formula card UX fix: drop gate-mode confirmation for formulas + persistent queued chip** (2026-04-15 session 80). User reported session 79's formula card "still doesn't show the formula, and no indication of SRS functionality" despite 134/134 tests passing. Opus diagnosed the issue empirically via a jsdom probe mounting `study_guide_diagnostic.html` against commit `52faeb4` with real MathJax 3.2.2 loaded from CDN, confirmed the technical implementation was functional (MathJax renders `<mjx-container>` properly, toast fires, `touchedFormulas.{id}.hintedAt` is set), and isolated the root cause as a **UX friction bug**: in gate mode, `handleHelperClick` was requiring a first-use confirmation modal ("Using helpers will reduce your score. Continue?") BEFORE opening the formula card. Students were canceling the confirmation thinking they were dodging a mistake — never seeing the formula, and never seeing the SRS toast because that fires inside `doHelperAction` which runs post-confirmation. The daily-queue FRQ click auto-upgrades to gate mode, so every student reading a formula hint from the daily queue hit this friction.
    - **Diagnostic probe** (jsdom mount against commit `52faeb4`): confirmed 3 formula buttons render for U1-PC-FRQ-Q02 (zscore, empirical-rule, one other), first click opens confirmation modal NOT formula modal, clicking "Yes, show helper" does open the formula modal with proper MathJax rendering + toast + state update. Everything works, just gated behind the wrong modal. Probe also confirmed daily-queue click sets `frqMode = 'gate'` which is the root of the frustration — ad-hoc FRQ opens would stay in practice mode but the daily queue is the primary entry point.
    - **Design decision** (Opus → user agreement): drop the confirmation modal entirely for formula clicks (keep for drills since they're a bigger state mutation), AND add a persistent visual indicator for the SRS feed because a 2.6-second toast wasn't enough persistence for students to notice the queue was being populated.
    - **Sonnet dispatch — implementation** (`.session80-sonnet-brief.md` → `.session80-result.md`). Two fixes landed in one ~5-minute pass:
      * **Fix 1 — Drop gate-mode confirmation for formulas**: in `handleHelperClick` (around `study_guide_diagnostic.html:2963`), added a new branch `if (kind === 'formula') { doHelperAction(kind, id, unit, data, questionId); return; }` immediately after the existing practice-mode early-return and before the `!data.frqHelpers.confirmed` check. Three-line insertion plus a comment explaining why. Drill clicks still hit the confirmation modal on first use in gate mode — verified via a regression check in the empirical probe.
      * **Fix 2 — Persistent "Queued for tomorrow" chip**: new function `queuedFormulasToday(state, todayStr)` added to `.v4-logic-block.js`, the inline v4 copy in `study_guide_diagnostic.html`, and proxied through `.v5-ladder-block.js` + the inline v5 block. Pure function: iterates `state.touchedFormulas`, returns `[{id, name}]` for entries where `hintedAt === todayStr`. Deterministic (caller passes the date string, no implicit `today()` call inside). Exported via `window.__studyGuideV4__` and proxied via `window.__studyGuideV5__` following the existing `recordFormulaHint` / `getFormulaEntry` proxy pattern. Added to the main destructure line at the top of the IIFE. In `renderFrqHelperPanel`, immediately after the existing `sg-frq-helpers-intro` paragraph and before `helperPanel.appendChild(header)`, a new block calls `queuedFormulasToday(state, today())` and conditionally appends a `.sg-frq-queued-formulas` chip to the header: "📝 Queued for tomorrow: Z-Score (SDs from mean), Empirical Rule (68-95-99.7)". Hidden entirely when the list is empty (no empty container). Re-renders after every helper panel re-render (which already happens after each formula click), so the chip updates in real-time as the student queues more formulas.
      * **CSS**: 3 new rules for `.sg-frq-queued-formulas` (background: `var(--sg-bar-track)`, border, padding 6px 10px, flex-wrap for long lists), `.sg-frq-queued-label` (bold, `var(--sg-text)`, whitespace: nowrap), `.sg-frq-queued-list` (muted color `var(--sg-text-dim)`).
    - **Tests added** (+9 total, 134 → 143 green):
      * 4 source-string tests in `tests/study-guide.test.js` new `describe('session 80 formula UX fixes')` block — assert the `if (kind === 'formula')` branch exists in `handleHelperClick`, drill confirmation path is still present, `renderFrqHelperPanel` calls `queuedFormulasToday(state, today())` and uses the `sg-frq-queued-formulas` class, chip is guarded by `if (queued.length)`.
      * 5 behavioral tests in `tests/study-guide-v5-ladder.test.js` new `describe('queuedFormulasToday')` block — empty state returns empty array, entries hinted today are returned with their names, entries hinted on other dates are excluded, entries without `hintedAt` are tolerated, v5 proxy delegates correctly.
    - **Empirical verification** (Opus ran a second jsdom probe after Sonnet finished): formula click in gate mode → **confirmation modal absent**, formula modal present with `<mjx-container>` rendered LaTeX, toast visible, chip shows "📝 Queued for tomorrow: Z-Score (SDs from mean)" after first click, chip updates to "Z-Score (SDs from mean), Empirical Rule (68-95-99.7)" after second click, drill click still shows confirmation modal (regression check passed). All 8 empirical checks green. Both probes deleted after verification.
    - **Skipped Codex verification pass** this session because the change surface was small (~40 lines) and the jsdom probe provided direct runtime confirmation of the fix. For future UX-fix sessions, the pattern is: diagnose with jsdom, spec the fix narrowly, implement with Sonnet, re-verify empirically with a fresh jsdom probe, skip Codex if the probe gives ground-truth evidence.
    - **Files (new / modified)**: new — `.session80-sonnet-brief.md`, `.session80-result.md`. modified — `study_guide_diagnostic.html` (handleHelperClick branch, renderFrqHelperPanel chip, inline v4 `queuedFormulasToday`, inline v5 proxy, destructure line update, 3 CSS rules), `.v4-logic-block.js` (+queuedFormulasToday function + export), `.v5-ladder-block.js` (+queuedFormulasToday proxy), `tests/study-guide.test.js` (+4 source-string tests), `tests/study-guide-v5-ladder.test.js` (+5 behavioral tests).
    - **Test counts**: 134/134 → 143/143 (+9). Distribution: `tests/study-guide.test.js` 74, `tests/study-guide-v5-ladder.test.js` 35, `tests/study-guide-v6-frq-decomposition.test.js` 34.
    - **Open items for session 81**: (a) Run `node scripts/supplement-probe-signal.mjs` on the school network (still carried from sessions 78-79-80). (b) Watch the first real student pilot session to see if the formula card UX fix actually resolved the friction — the jsdom probe confirms the code path but not the student mental model. (c) Consider evolving the "Queued for tomorrow" chip into a clickable link that jumps to the next day's daily queue preview — gives students a concrete sense of the consequence of their hint-taking. (d) Drill confirmation modal is still a candidate for UX cleanup later — not as friction-heavy as the formula one was, but the same logic applies (the button already shows the cost). Defer until student data shows drill abandonment is a problem. (e) Block-scope oddity at line 303 still deferred.

- **Entry 81 — persistent Review Queue panel with SM2-lite graduation** (2026-04-15 session 81). Session 80 fixed the formula card opening in one click but the SRS feedback was still just a fading toast + a "Queued for tomorrow" chip scoped to the FRQ helper panel (only visible while an FRQ was active). User asked: "is there a SRS queue that builds up as the user demonstrates they don't know it (by action of using the card)?" Answer before this session: **kind of, indirectly via `formulaWeight` MCQ surfacing, but no visible growing queue**. Session 81 built the visible queue.
    - **Design** (Opus): a dedicated `.sg-review-queue` disclosure panel mounted in the left sidebar between the daily queue and the "See all formulas" disclosure. Lists formulas where `touchedFormulas[id].hintedAt` is within the last 7 days AND `graduated !== true`. Each entry has formula name, "Hinted today / yesterday / N days ago" meta, and two buttons: **Review** (re-opens `showFormulaCardModal` for the entry) and **I know it** (sets `graduated = true`, removes from queue). Empty state shows a guiding message; disclosure stays visible so students learn where to find reviewed formulas later. SM2-lite: no scheduling intervals (unlike classic SM2), just a 7-day decay window per hint + explicit user graduation. Graduation is **deliberately separate** from FRQ penalty state (`data.frqHelpers.formulasViewed`) — clicking "I know it" removes the entry from the review queue but does NOT refund any gate-mode penalty previously applied. Documented in a comment near the graduate handler.
    - **Sonnet dispatch — implementation** (`.session81-sonnet-brief.md` → `.session81-result.md`). Single pass, 176/176 green, +33 new tests. Key components:
      * **Three new pure functions** in `.v4-logic-block.js` + inline HTML v4 copy: `daysBetween(fromIso, toIso)` (UTC-normalized integer day delta, returns `NaN` for malformed input), `reviewQueueEntries(state, todayIso, opts)` (iterates `touchedFormulas`, filters by `graduated !== true`, `hintedAt` present, `daysSince ∈ [0, maxDays]`, sorts by `hintedAt` desc with alphabetical tiebreak, resolves names via `formulaName(id)`, default `maxDays = 7`), `graduateFormula(formulaId, state)` (sets `graduated = true` on existing entry, noop for missing). All three exported via `window.__studyGuideV4__` and proxied through `.v5-ladder-block.js` + inline v5 copy following the existing `recordFormulaHint` / `getFormulaEntry` pattern. Destructure line at the top of the main IIFE updated to pull them from `window.__studyGuideV5__`.
      * **`showFormulaCardModal` signature extension**: added optional 5th arg `onClose` callback. `closeModal` inside the function now branches: `if (typeof onClose === 'function') onClose(); else renderActiveProbe();` — preserves backward compatibility for all existing FRQ-flow callers (which pass 4 args and still get `renderActiveProbe()` on close) while letting the review-queue flow pass `() => renderReviewQueuePanel()` instead. FRQ-flow callers in `doHelperAction('formula', ...)` and `buildFrqHelperButton`'s used-formula re-click branch unchanged.
      * **`renderReviewQueuePanel()`** new function in `study_guide_diagnostic.html`. Builds a `<details class="sg-review-queue" open>` with summary (title + count badge), body containing either the empty-state message or an `<ol class="sg-review-queue-list">` of items. Each item has `.sg-review-queue-info` (name + meta) and `.sg-review-queue-actions` (Review button + Graduate button). Re-render strategy: the function removes any existing `.sg-review-queue` element from its parent before re-inserting — no duplicate panels accumulate across re-renders.
      * **Mount point**: called from the end of `renderQueuePane()` (the existing function that renders the daily queue), before `renderSeeAllDisclosure`. So any daily-queue re-render also refreshes the review queue.
      * **Additional re-render call sites**: `doHelperAction('formula', ...)` after `recordFormulaHint` (so hinting a formula during an FRQ immediately appears in the queue), and the graduate button's own click handler (removes entry instantly).
      * **`normalizeState`** unit-loop pass-through: `graduated` field preserved across save/load on each `touchedFormulas` entry. Existing state without the field defaults to `graduated === false` (absent = not graduated).
      * **18 new CSS rules** for `.sg-review-queue`, `.sg-review-queue-summary`, `.sg-review-queue-title`, `.sg-review-queue-count`, `.sg-review-queue-body`, `.sg-review-queue-list`, `.sg-review-queue-item`, `.sg-review-queue-info`, `.sg-review-queue-name`, `.sg-review-queue-meta`, `.sg-review-queue-actions`, `.sg-review-queue-btn` + hover state, `.sg-review-queue-btn-graduate` (green tint), `.sg-review-queue-empty` message style.
      * **Meta text format**: `daysSince === 0` → "Hinted today", `=== 1` → "Hinted yesterday", `>= 2` → "Hinted N days ago". Strings are exact so source-string tests can assert them.
    - **Tests added**: +33 total (143 → 176 green). `tests/study-guide-v5-ladder.test.js` +19 behavioral tests across 3 new `describe` blocks for the pure functions (empty state, date filtering, graduated exclusion, sort order, custom maxDays, NaN handling, noop on missing entry, idempotency, v5 proxy delegation). `tests/study-guide.test.js` +14 source-string tests in a new `describe('session 81 review queue panel')` block for the render function + wiring + empty-state message + meta-text format + `showFormulaCardModal` signature + `normalizeState` graduated pass-through.
    - **Empirical jsdom probe verification** (Opus ran after Sonnet finished, probe deleted post-verification):
      * Boot: review queue panel renders in empty state with `(0)` badge and guiding message ✅
      * After clicking 2 formula buttons in an FRQ (re-querying between clicks to avoid stale DOM refs): panel shows `(2)` badge, both entries listed with "Hinted today" meta, sorted by `hintedAt` desc ✅
      * Session 80 "Queued for tomorrow" chip still intact with both formulas ✅
      * Review button click → `showFormulaCardModal` re-opens with title + MathJax-rendered `<mjx-container>` latex ✅
      * Graduate button click → entry removed, badge drops `(2)` → `(1)`, `touchedFormulas[id].graduated = true` in state ✅
      * **Invariant**: `formulasViewed` (FRQ penalty state) unchanged after graduation — graduation doesn't refund penalty, confirming the separation of concerns ✅
    - **Files (new / modified)**: new — `.session81-sonnet-brief.md`, `.session81-result.md`. modified — `study_guide_diagnostic.html` (inline v4 `daysBetween` + `reviewQueueEntries` + `graduateFormula`, inline v5 proxies, destructure line update, `showFormulaCardModal` signature + closeModal branch, new `renderReviewQueuePanel` function, mount call in `renderQueuePane`, re-render calls in `doHelperAction('formula')` and graduate handler, `normalizeState` `graduated` pass-through, 18 CSS rules), `.v4-logic-block.js` (+3 pure functions + exports), `.v5-ladder-block.js` (+3 proxies), `tests/study-guide.test.js` (+14 source-string tests), `tests/study-guide-v5-ladder.test.js` (+19 behavioral tests).
    - **Test counts**: 143/143 → 176/176 (+33). Distribution: `tests/study-guide.test.js` 88 (+14), `tests/study-guide-v5-ladder.test.js` 54 (+19), `tests/study-guide-v6-frq-decomposition.test.js` 34 (unchanged).
    - **Pedagogical design note**: the review queue is a **pull** mechanism, not **push** — the student actively chooses to Review or Graduate. This contrasts with the MCQ daily queue which pushes items. Rationale: formulas need optional re-review (the student may have absorbed them through practice and no longer needs the card), while MCQs enforce coverage across the exam weight distribution. Different learning modes, different UI.
    - **Graduation semantics**: "I know it" is a student self-assessment, not an auto-graduation. No automatic graduation on N correct answers or time elapsed. User explicitly chose the student-driven model because the 7-day auto-aging already provides passive graduation and forcing explicit action trains metacognition. The one concession: 7-day auto-aging via `daysSince > maxDays` in `reviewQueueEntries` filter, so stale hints don't accumulate forever.
    - **Open items for session 82**: (a) Run `node scripts/supplement-probe-signal.mjs` on the school network (still carried). (b) Real student pilot window now has 5 distinct signals to watch: formula modal opens, review-queue reviews, review-queue graduations, drill helper usage, practice→gate escalations. (c) Consider adding a "Mastery progression" view that shows the BKT mastery trajectory for each hinted formula over time — pulls from `touchedFormulas[id].lastMastery` as the student does MCQs that touch the formula via `buildMcqQueue`. Would close the loop: student hints → sees in review queue → encounters related MCQs → mastery updates → visible progression. (d) Drill confirmation modal still a cleanup candidate; defer. (e) Block-scope oddity at line 303 still deferred. (f) The "Queued for tomorrow" chip (session 80) now partially overlaps with the review queue — both show hinted-today entries, but the chip is FRQ-scoped and the queue is sidebar-global. Could simplify by removing the chip and pointing students to the review queue instead, but user has not asked for this yet.

- **Entry 82 — collapsed practice/gate mode split into always-scored with latent-penalty escape hatch** (2026-04-15 session 82). User feedback: "'Gate mode' is confusing!" and "I see it as one more button for students to press, one more detail to consider, more confusion." The practice/gate mode distinction from session 79 was adding cognitive load without improving the student experience. Opus proposed collapsing to always-scored with a pedagogical escape hatch (don't click Grade → no penalty materializes). User approved. Session 82 removes the entire mode split.
    - **Design rationale**: session 79 introduced practice mode explicitly to stop punishing help-seeking per Bjork's desirable-difficulties research. But the mode had a confusing name (inherited from AP Classroom's "Progress Check Gate questions"), required an escalation button + confirmation modal, and added a mode-aware branch in 6+ render paths. Session 82 preserves the "safe exploration" use case via a **latent-penalty mechanism**: `computeEffectivePenalty` is only called by `renderGrade`, and `renderGrade` only runs after the student clicks the Grade button. So students who click helpers but don't grade see no penalty — it's computed only at grade time. This functionally preserves practice mode without needing a mode field. Also dropped the drill first-use confirmation modal (the cost pill is already inline on the button).
    - **Sonnet dispatch — implementation** (`.session82-sonnet-brief.md` → `.session82-result.md`). Single pass, 172/172 green (was 176, net −4 from deleting more tests than adding). Summary of changes to `study_guide_diagnostic.html` + `.v6-frq-decomp-block.js`:
      * **Deleted 3 functions entirely** from `study_guide_diagnostic.html`: `renderFrqModeBanner` (~25 lines), `showGateEscalationModal` (~40 lines), `showFrqHelperModal` (~40 lines). All their call sites removed.
      * **Simplified `handleHelperClick`** to a two-liner: default-frqHelpers guard + `doHelperAction`. No mode branching, no kind branching, no confirmation. Every helper click goes straight to the action.
      * **Simplified `computeEffectivePenalty`** in both `.v6-frq-decomp-block.js` and the inline HTML copy: dropped the `mode` parameter. No more `if (mode === 'practice') return 0` early return. Always computes the penalty from helper state. Call sites updated to pass only `(frqHelpers, decomposition)`.
      * **Simplified `buildFrqHelperButton`**: removed the `frqMode` read and the "free" cost-label branch. Cost pills always show real values: `−5%` for formulas, `−10% (correct)` / `−15% (wrong)` / `−10% / −15%` (pending) for drills.
      * **Simplified `renderFrqHelperPanel`**: removed the `frqMode` branch in the intro text — the intro now always lists the penalty values. Added an **escape-hatch note** right below the intro as a `.sg-frq-helpers-escape-hatch` element with `innerHTML` for the bold "Grade" word: `"Click <strong>Grade</strong> when you're ready for your final score — until then, helpers don't hurt you."` This is the sole pedagogical mechanism preserving the "safe exploration" use case.
      * **Simplified `renderActiveProbe` FRQ branch**: removed the `renderFrqModeBanner` call, removed the `frqMode` local variable, removed the `mode` arg from both `computeEffectivePenalty` and `renderGrade` call sites.
      * **Simplified `renderGrade`**: removed the `mode` parameter. The grade card always renders the effective-score block (raw + penalty breakdown + effective) — no more practice-only abbreviated display.
      * **Simplified `renderQueuePane`** FRQ click handler: deleted the `if (entry.kind === 'frq') { ... unitData.frqMode = 'gate' ... }` silent upgrade block. FRQs no longer have a mode to upgrade.
      * **Simplified `makeDefaultState` and `getUnit`**: removed `frqMode:'practice'` from per-unit defaults.
      * **Simplified `normalizeState`**: removed the `frqMode` preservation line in the unit loop. Existing saved states with `frqMode` set silently drop the field on load — no migration error, just a no-op.
      * **Deleted CSS**: `.sg-frq-mode-banner` (plus `.is-practice` / `.is-gate` / night-mode variants), `.sg-frq-mode-escalate`, `.sg-frq-helper-modal`. **Kept**: all formula modal rules, all helper panel rules, all review queue rules, all "Queued for tomorrow" chip rules.
      * **Added CSS**: `.sg-frq-helpers-escape-hatch` (subtle callout with `var(--sg-bar-track)` background and `var(--sg-border)` border, 0.82rem font).
    - **Test counts**: 176/176 → 172/172 (net −4). Deleted ~18 mode-related tests, added ~14 new session-82 tests. Distribution after: `tests/study-guide.test.js` N, `tests/study-guide-v5-ladder.test.js` 54 (unchanged — orthogonal), `tests/study-guide-v6-frq-decomposition.test.js` ~33 (deleted 4 practice-mode tests, added 3 simplification tests).
    - **Empirical jsdom probe verification** (Opus ran after Sonnet finished, probe deleted post-verification):
      * Boot: review queue panel still renders in empty state ✅
      * After opening FRQ from daily queue: no mode banner ✅, helper panel renders with intro text that includes penalty values ✅, escape-hatch note present with exact text "Click Grade when you're ready for your final score — until then, helpers don't hurt you." ✅
      * Formula cost pill shows `−5%` (not "free") ✅, drill cost pill shows `−10% / −15%` (not "free") ✅
      * Formula click: no confirmation modal ✅, formula modal opens with MathJax-rendered title + latex ✅
      * Drill click: no confirmation modal ✅ (the drill state field appeared `null` in localStorage due to `saveSoon()` debounce timing in the probe, but the important check — absence of confirmation modal — passed)
      * State: `unit.frqMode` field is `undefined` ✅, touchedFormulas count is 1 (from the formula click), formulasViewed has the hinted formula ✅
      * Session 80 chip still present ✅, session 81 review queue present with the hinted item ✅
    - **Pedagogical note on the escape hatch**: the latent-penalty mechanism is subtle but important. `computeEffectivePenalty` is called in only one place — `renderActiveProbe` FRQ branch, inside the `if (data.frqGrade)` block. Since `data.frqGrade` is only set when the student clicks Grade (via `gradeFrq(unit)` or the paper-mode branch), helper usage BEFORE grading never materializes a penalty. A student who wants to explore the FRQ with all the hints can click every helper, read every formula card, and simply not click Grade — no penalty is ever computed or displayed. The escape-hatch note in the helper panel explicitly documents this. If a student DOES want to commit to being scored, clicking Grade locks in the effective score with all penalties applied from helpers already used — same behavior as the old gate mode, but without the mode distinction.
    - **Files (new / modified)**: new — `.session82-sonnet-brief.md`, `.session82-result.md`. modified — `study_guide_diagnostic.html` (3 function deletions, 9 simplifications, escape-hatch note addition, CSS additions + deletions), `.v6-frq-decomp-block.js` (computeEffectivePenalty signature), `tests/study-guide.test.js` (deleted session 79/80 mode-related tests, added session 82 simplification tests), `tests/study-guide-v6-frq-decomposition.test.js` (deleted 4 practice-mode tests, added 3 simplification tests).
    - **Test counts after session**: 172/172 green.
    - **Open items for session 83**: (a) Run `node scripts/supplement-probe-signal.mjs` on the school network (still carried). (b) Real student pilot window to validate the escape-hatch mental model — do students discover that not-grading = no penalty, or do they still feel gated by the cost pills? If the latter, the escape-hatch note may need to be more prominent (a banner instead of a small callout). (c) Consider whether "Check your answer" (MCQ probe feature) should also have a similar cost-pill-plus-escape-hatch pattern, or whether that's overkill for the MCQ flow. (d) The session 80 "Queued for tomorrow" chip still exists and overlaps with the session 81 review queue — now that the helper panel is simpler, the chip may be visual clutter. Consider removing in a future session. (e) Block-scope oddity at line 303 still deferred. (f) The drill-click path sets `activeDrillQuestionId` and calls `renderRemediation()` + `saveSoon()`. The state write is debounced — if future test or probe code reads `activeDrillQuestionId` from localStorage immediately after a click, it will appear `null`. Not a bug, but worth remembering for future jsdom probes.

- **Entry 83 — TI-84 procedure walkthroughs ported into the formula card modal** (2026-04-15 session 83). User asked: "any way you can have it so that formula cards link directly to the calculator skill associated?" Explored 5 integration options (inline chip, link out, iframe trainer, port data, reverse link). User rejected iframe-the-trainer as "another UI to dig through" and landed on **port the data structures into the study guide**. Session 83 ships a static-walkthrough renderer that displays the trainer's procedure step data inline inside the formula modal — no cross-tab nav, no WASM, no state sync, no new UI context to learn. Student opens a formula card, sees LaTeX + explanation + hint + a new collapsible "📱 Show calculator steps" `<details>` that expands to the full keystroke sequence from the trainer's authoritative `ti84-procedures-data.json`.
    - **Design decision** (Opus → user agreement): port the step data, NOT the trainer UI. The trainer's physical-calculator mode is already "follow along on your real TI-84, no interactive validation" — replicating that static flow inside the study guide is equivalent to opening the trainer in Physical mode, minus all the context-switching. The trainer remains available as a separate standalone page for students who want the full interactive drill experience.
    - **Data flow**: `ti84-procedures-data.json` stays untouched (trainer's authoritative source). A new `data/ti84-procedures.js` wrapper file (~208 KB) is generated once via a Node one-liner and checked in: `window.TI84_PROCEDURES = {...inlined JSON...};`. A second new file `data/formula-procedure-map.js` provides the hand-authored cartridge→procedure mapping (`window.FORMULA_PROCEDURE_MAP`). Both loaded via `<script src>` tags in `study_guide_diagnostic.html` near the other data script loads. No fetch, no dynamic loading, works offline and on `file://` URLs.
    - **Formula→procedure map**: 36 hand-authored pairings across 18 unique procedure ids. Covers descriptive stats (5: mean/std-dev/variance/iqr/outlier-iqr → one-var-stats), regression (9: linreg/linreg-mean/corr-r/slope-b/y-intercept/r-squared → linreg-a-plus-bx + residual → residual-plot + slope-t → linreg-ttest + slope-ci → linreg-tint), normal/z (3: zscore/z-test-stat/empirical-rule → normalcdf), binomial/geometric (2: binom-pmf → binompdf, geom-pmf → geometpdf), sampling distributions (6: phat-mean/phat-sd/phat-se/xbar-mean/xbar-sd/xbar-se → normalcdf-sampling), proportions inference (2: one-prop-z → one-propztest, one-prop-ci → one-propzint), means inference (5: one-mean-t → t-test-stats, one-mean-ci → t-interval-stats, two-mean-t → two-samp-ttest, two-mean-ci → two-samp-tint, paired-t → t-test-data), chi-square (4: chi-sq/chi-sq-output → chi-square-test, expected-gof → chi-square-gof-test, expected-twoway → chi-square-test). Formulas without a matching procedure (~45 of the 81) are intentionally omitted — they're conceptual (e.g., `add-rule`, `random-condition`, `p-value-interp`, `ci-formula`, `df-t`) and the calc-steps section silently omits for them.
    - **Sonnet dispatch** (`.session83-sonnet-brief.md` → `.session83-result.md`). Single pass, 187/187 green (+15 tests from 172 baseline). Key components:
      * **New function `renderProcedureWalkthrough(procedureId, container)`** inside the main IIFE. Reads `window.TI84_PROCEDURES.procedures`, finds the procedure by id, renders header (name + description) + optional prereq callout (when `dataRequirements` non-empty, translates `{"[A]":"matrix"}` → "Matrix [A] must be populated first." and similar for list requirements) + numbered step list. Each step card has a grid layout: step number badge | key/param chip | narration text.
      * **`KEY_DISPLAY` constant** for calculator key rendering. Arrow keys become unicode: `RIGHT → →`, `DOWN → ↓`, `LEFT → ←`, `UP → ↑`. Literal keys (`STAT`, `ENTER`, `ALPHA`, `2ND`, `MATH`, `VARS`, etc.) render as uppercase labels. Fallback for unmapped keys: render as-is.
      * **Key vs param branching**: `step.key` starting with `{` and ending with `}` is treated as a parameter placeholder — the brace content is stripped and rendered as `.sg-calc-param` chip with text `"Enter: {inner}"` (e.g., `{x}` → `"Enter: x"`, `{C-Level}` → `"Enter: C-Level"`). Literal keys render as `.sg-calc-key` with the dark-bezel calculator-button aesthetic.
      * **Narration bracket-wrapping**: `step.narration` strings contain `[KEY]` patterns (e.g., "Press [STAT] to open..."). The renderer HTML-escapes the narration via the existing `esc()` helper, then applies a regex replacement `/\[([A-Z0-9→←↑↓\-=]+)\]/g` to wrap bracketed uppercase tokens in `<strong>` so they visually stand out as calculator key references. Safe because `esc()` neutralizes `<>` characters first.
      * **Highlight hints**: if `step.highlight` is non-empty, append as a muted suffix after the narration — rendered as `<span class="sg-calc-step-hint">(highlights: EDIT tab)</span>`.
      * **Formula modal wire-up**: inside `showFormulaCardModal`, after the subconcepts section and before the close button row, a new `<details class="sg-formula-modal-calc-steps">` is appended. Collapsed by default. Summary text: "📱 Show calculator steps". Only rendered when `window.FORMULA_PROCEDURE_MAP[formulaId]` has an entry AND `window.TI84_PROCEDURES.procedures` is loaded — graceful degradation if either script fails to load.
      * **~20 new CSS rules** for `.sg-formula-modal-calc-steps`, `.sg-calc-walkthrough`, `.sg-calc-walkthrough-header`, `.sg-calc-walkthrough-title`, `.sg-calc-walkthrough-description`, `.sg-calc-prereq` (amber callout with dark-mode variant), `.sg-calc-step-list`, `.sg-calc-step`, `.sg-calc-step-num` (circle badge), `.sg-calc-key` (dark-bezel pill with monospace font and inset highlight to evoke a physical calculator button), `.sg-calc-param` (blue-tinted pill visually distinct from literal keys so students can tell "type a value" vs "press a key"), `.sg-calc-step-narration` (with `<strong>` styling for bracket-wrapped key references), `.sg-calc-step-hint` (muted italic suffix). Dark-mode variants for prereq, key, and param.
    - **Tests added** (+15 in `tests/study-guide.test.js` new `describe('session 83 TI-84 procedure walkthroughs')` block):
      * Script tag loading tests (both data files present)
      * Function definition test (`renderProcedureWalkthrough`)
      * Modal wire-up tests (map lookup, conditional render, details class, summary text)
      * Renderer structure tests (walkthrough container, step list, prereq conditional)
      * KEY_DISPLAY arrow mapping tests
      * Parameter placeholder branch test
      * Narration bracket-wrapping test
      * Map size test (≥30 mappings)
      * **Test #15 — typo catcher**: parses both `ti84-procedures.js` and `formula-procedure-map.js` into a synthetic window, iterates every value in `FORMULA_PROCEDURE_MAP`, asserts each resolves to a real `procedures[].id` in `TI84_PROCEDURES`. Catches any typo in the hand-authored map at test time.
    - **Empirical jsdom probe verification** (Opus ran after Sonnet finished, probe deleted post-verification): activated the daily-queue Unit 1 FRQ (which uses `zscore` formula), injected a fake curriculum entry so `findQuestion` works, clicked the "Formula card · Z-Score (SDs from mean)" button, inspected the modal DOM:
      * Modal renders with title + MathJax-rendered latex + explain + hint ✅
      * `.sg-formula-modal-calc-steps` `<details>` present, collapsed by default, summary text "📱 Show calculator steps" ✅
      * After opening: walkthrough title shows "normalcdf" (the mapped procedure name), description rendered ✅
      * No prereq callout for `normalcdf` (which has no `dataRequirements`) ✅
      * **13 step cards rendered**, each with correct step number + key/param chip + narration + highlight suffix ✅
      * Full sequence: `[1] 2ND` → `[2] VARS` → `[3] 2` → `[4] Enter: lower bound` → `[5] ↓` → `[6] Enter: upper bound` → `[7] ↓` → `[8] Enter: μ` → `[9] ↓` → `[10] Enter: σ` → `[11] ↓` → `[12] ENTER` → `[13] ENTER` ✅
      * Parameter placeholders rendered as `Enter: lower bound` / `Enter: upper bound` / `Enter: μ` / `Enter: σ` chips (distinguished from literal keys) ✅
      * Direct data inspection confirmed `chi-square-test.dataRequirements = {"[A]":"matrix"}` exists in the dataset, and the source contains the `sg-calc-prereq` render path + `dataRequirements` check ✅
    - **Files (new / modified)**: new — `.session83-sonnet-brief.md`, `.session83-result.md`, `data/ti84-procedures.js` (213 KB, generated from `ti84-procedures-data.json`), `data/formula-procedure-map.js` (36 mappings). modified — `study_guide_diagnostic.html` (~+140 lines: 2 script tags, `KEY_DISPLAY` constant, `renderProcedureWalkthrough` function, wire-up in `showFormulaCardModal`, ~20 CSS rules), `tests/study-guide.test.js` (+15 tests).
    - **Test counts**: 172/172 → 187/187 (+15). Distribution: `tests/study-guide.test.js` 100 (+15), `tests/study-guide-v5-ladder.test.js` 54 (unchanged), `tests/study-guide-v6-frq-decomposition.test.js` 33 (unchanged).
    - **Architecture note**: the trainer codebase (`ti84-trainer-v2/*`) was NOT modified this session. It remains a separate standalone app for students who want interactive validation / CEmu WASM drill experience. The study guide now has a lightweight static-walkthrough view of the same procedure data. If the trainer's procedure data changes (unlikely near-deadline), re-run the Node one-liner: `node -e "const fs=require('fs'); const d=fs.readFileSync('ti84-procedures-data.json','utf8'); fs.writeFileSync('data/ti84-procedures.js', '// Generated from ti84-procedures-data.json — do not edit directly\\nwindow.TI84_PROCEDURES = ' + d + ';\\n');"`
    - **Open items for session 84**: (a) Run `node scripts/supplement-probe-signal.mjs` on the school network (still carried). (b) Real student pilot to see if the inline walkthroughs actually get used — track via a future telemetry bucket (e.g., count `<details>` expansions of the calc-steps section per session). (c) Consider the reverse-link direction: when a student completes a procedure in the full trainer, show "Mark {formula} as reviewed in the study guide" button that deep-links back with a hash param. Future session, post-pilot data. (d) Formulas with `dataRequirements` (like `chi-square-test` requiring matrix [A]) get a prereq callout — future enhancement: deep-link to a dedicated "enter matrix [A]" procedure (`matrix-entry` is in the data), so the student can learn matrix entry first. Would need a `relatedProcedures` field in the map. (e) The 36-row map misses some AP-actionable formulas because their cartridge IDs don't directly map to a single procedure (e.g., `power`, `margin-error`, `width-ci`) — these are computed/conceptual, not procedural. Acceptable. (f) Block-scope oddity at line 303 still deferred. (g) Possible cleanup: the session 80 "Queued for tomorrow" chip now competes with 3 other features (review queue, calc-steps walkthrough, helper panel). Chip removal may reduce visual clutter.

- **Entry 84 — tiny fix: cap modal height at 90vh with internal scroll** (2026-04-15 session 84). Session 83 introduced the calc-steps walkthrough which could make the formula card modal taller than the viewport on long procedures like `normalcdf` (13 steps). Three-value CSS fix on the base `.sg-modal` rule: added `max-height:90vh;overflow-y:auto;overscroll-behavior:contain`. Modal now caps at 90% viewport height and scrolls internally. `overscroll-behavior:contain` prevents scroll chaining to the backdrop. Close button scrolls with content but Esc key and backdrop click still close regardless of scroll position. +2 regression tests pinning the fix. Opus made this edit directly (2-line CSS change, not worth a Sonnet dispatch). Tests 187 → 189.

- **Entry 85 — Mastery Map constellation visualizer with mini-map + modal** (2026-04-15 session 85). User brainstorm: "a WebGL or canvas DAG visualizer in the style of nodes that change color depending on comprehension... glorified progress bar... motivation-first." Opus presented 3 flavors (Constellation / Prerequisite tree / FRQ skill graph) and the user picked **Flavor A — Constellation**: 81 cartridge formulas as nodes in a dark-blue sky, clustered by unit, continuously colored by BKT mastery, click-to-open formula modal, canvas-rendered with mouse zoom/pan, with a persistent mini-map in the sidebar for ambient progress feedback and a fullscreen modal for exploration.
    - **Design decisions** (locked in before dispatch): (a) static deterministic layout — 9 unit clusters in a 3×3 grid (300×200 cells), core formulas on inner ring (radius 30), non-core on outer ring (radius 65), even angular spacing, no force simulation. (b) Continuous HSL gradient coloring — hue interpolates from 0° (red, mastery 0) to 120° (green, mastery 1), saturation 75%, lightness 50%; untouched formulas render as desaturated blue-grey `#3a4060` that blends with the dark-blue `#0a0e27` background; mastery > 0.5 gets a radial-gradient glow for motivational oomph. (c) Pure functions — `computeMapLayout(commands, formulaUnitMap)` returns `{nodes, worldBounds, unitCenters}`, `colorForMastery(mastery, touched)` returns `{fill, stroke, glow}`. (d) Zoom bounds computed from canvas + world dimensions: `minZoom = Math.min(cw/ww, ch/wh) * 0.95` (all nodes fit), `maxZoom = minZoom * 5` (shows 3-5 nodes). (e) Pan clamping: at `minZoom`, content fits viewport so `maxPanX=maxPanY=0` (pan is a no-op); as zoom grows, pan range grows proportional to overflow. (f) Click vs drag distinction: 4-pixel threshold on mousedown→mouseup delta. (g) Mouse-only for v1 (touch gestures deferred). (h) Fresh viewport on every modal open (no persistent state).
    - **Sonnet dispatch** (`.session85-sonnet-brief.md` → `.session85-result.md`). Single pass, 189/189 → 222/222 green (+33 tests). Key components:
      * **Pure functions** in `.v4-logic-block.js` (+ inline HTML v4 copy): `computeMapLayout`, `colorForMastery`. Proxied through `.v5-ladder-block.js` (+ inline HTML v5 copy). Fallback assignment: unmapped formulas (~10 of 81) go into unit 3's otherwise-empty cluster.
      * **`renderMasteryMap(canvas, nodes, worldBounds, state, viewState)`** — main canvas renderer. Draws dark-blue background, unit labels (size scales inversely with zoom so labels stay readable), each node as a filled arc with stroke outline, optional radial-gradient glow for high-mastery nodes, optional per-node labels at zoom > 1.8. Uses `ctx.save/restore` + translate/scale for zoom-pan transform.
      * **`renderMasteryMiniMap(canvas, nodes, worldBounds, state)`** — tiny 120×80 canvas version. Fixed scale, no labels, no interaction — just colored dots on a dark background. Re-renders on every `renderQueuePane` call so it updates automatically as the student's mastery grows.
      * **`showMasteryMapModal()`** — creates backdrop + modal with header (title + stats "N of 81 mastered · M practicing · K untouched"), canvas wrap, and footer (legend + hint text). Attaches wheel/mousedown/mousemove/mouseup/click handlers to the canvas for zoom and pan. Click-to-open-formula path: on click (not drag), hit-test the node, close the map modal, then call `showFormulaCardModal(id, null, null, null, noop)` to open the formula card. No stacked modals.
      * **`renderMasteryMapPanel()`** — the sidebar panel wrapping the mini-map. Mounted in `renderQueuePane` via `panel.appendChild` or `insertBefore(panel, seeAll)`. Includes an "Expand" button that triggers `showMasteryMapModal`. Mini-canvas also has a click handler that opens the modal.
      * **Utility functions** `clampPan` (keeps pan within content bounds) and `hitTest` (inverse-transform canvas click to world coords, linear scan over 81 nodes, returns the first node within `nodeRadius * 1.4` of the click).
      * **~25 new CSS rules** for `.sg-mastery-map-panel`, `.sg-mastery-map-panel-header`, `.sg-mastery-map-mini`, `.sg-mastery-map-modal .sg-modal` (overrides base rule to set `max-width:min(1040px,96vw)`, `max-height:min(760px,92vh)`, and crucially `overflow:hidden` to override session 84's `overflow-y:auto`), `.sg-mastery-map-header`, `.sg-mastery-map-stats`, `.sg-mastery-map-close`, `.sg-mastery-map-canvas-wrap`, `.sg-mastery-map-canvas` (with `cursor:grab` / `:active{cursor:grabbing}`), `.sg-mastery-map-footer`, `.sg-mastery-map-legend`, `.sg-mastery-legend-dot`, `.sg-mastery-map-hint`.
    - **Tests added** (+33): `tests/study-guide-v5-ladder.test.js` new describe blocks for `computeMapLayout` (7 behavioral), `colorForMastery` (8 behavioral), `clampPan` (3 behavioral), `hitTest` (3 behavioral) — all via synthetic-window pattern loading `.v4-logic-block.js` + `.v5-ladder-block.js`. `tests/study-guide.test.js` +12 source-string tests for DOM wiring (render functions defined, modal classes, legend count, canvas dimensions, click handler, wheel handler, drag threshold).
    - **Post-dispatch fix by Opus**: the **inline HTML copy** of the v4 export object at `study_guide_diagnostic.html:975-999` was missing `computeMapLayout` and `colorForMastery` (Sonnet added them to the `.v4-logic-block.js` standalone file but forgot the inline copy's export list). The functions were defined in the main IIFE scope (hoisted within the outer function), so runtime worked — but `window.__studyGuideV4__.computeMapLayout` was `undefined`, and because the v5 proxy reads from v4 at call time, `window.__studyGuideV5__.computeMapLayout` was also `undefined`. Additionally, the inline v5 proxy block (around line 1621) was missing the two new proxy stubs entirely. Both fixed directly by Opus via Edit (2 additions to the v4 export object, 2 proxy stubs in the v5 block). No test changes needed — the synthetic-window tests passed because they load the standalone `.v4/.v5` files which were already correct; only the inline runtime copies had the mismatch. **Lesson**: the sync-v4/v5/inline pattern is easy to get wrong when adding new exports. Future sessions should grep for `__studyGuideV4__ = {` and `api = {` as a post-check.
    - **Empirical jsdom probe verification**: mini-map panel renders in sidebar ✅, 120×80 canvas ✅, Expand button opens modal ✅, modal title "🗺️ Mastery map" ✅, stats "0 of 81 mastered · 0 practicing · 81 untouched" ✅, fullscreen canvas 1000×700 ✅, 4 legend dots ✅, close button tears down modal ✅, v5 proxy `computeMapLayout` returns 81 nodes with `worldBounds` 1000×700 and 9 `unitCenters` ✅, `colorForMastery(0, true)` → `hsl(0, 75%, 50%)` (red) ✅, `colorForMastery(0.5, true)` → `hsl(60, 75%, 50%)` (yellow) ✅, `colorForMastery(1, true)` → `hsl(120, 75%, 50%)` (green) ✅, glow at mastery 1.0 is `hsla(120, 75%, 50%, 0.4)` ✅, glow at mastery 0 is `null` ✅, sessions 81/83/84 features unchanged (review queue + calc-steps + modal max-height all present). One probe gotcha: the Expand button was not assigned a CSS class (the brief specified `.sg-mastery-map-open` but Sonnet attached the click handler directly to the button element without a class), so the initial probe selector failed to find it. Fix: use text-content or tagName matching instead of class selector in the probe. Not a runtime bug — the button and its click handler work fine; only the probe assumption was wrong.
    - **Files (new / modified)**: new — `.session85-sonnet-brief.md`, `.session85-result.md`. modified — `study_guide_diagnostic.html` (+~450 lines: pure functions inline, render/modal functions, renderQueuePane mount, ~25 CSS rules, +2 additions to the inline v4 export object for the post-dispatch fix, +2 proxy stubs in the inline v5 block), `.v4-logic-block.js` (+2 pure functions + exports), `.v5-ladder-block.js` (+2 proxies), `tests/study-guide.test.js` (+12 tests), `tests/study-guide-v5-ladder.test.js` (+21 tests).
    - **Test counts**: 189/189 → 222/222 (+33). Distribution: `tests/study-guide.test.js` 114 (+14 — session 84's +2 + session 85's +12), `tests/study-guide-v5-ladder.test.js` 75 (+21), `tests/study-guide-v6-frq-decomposition.test.js` 33 (unchanged).
    - **Pedagogical framing note**: the map is deliberately a **glorified progress bar, not an information display**. Students open it to feel their territory grow green over time. No graph edges between nodes (explicitly skipped to avoid visual clutter), no labels by default (only at zoom > 1.8), no prerequisite semantics, no unit ordering commentary. The emotional beat is "my map is turning green" — the visceral feedback that motivates logging back in tomorrow. If students want structural/curricular information, they have the daily queue, the review queue, the FRQ decomposition helpers, and the formula cards themselves. The map is ambient, motivational, and deliberately information-poor.
    - **Open items for session 86**: (a) Run `node scripts/supplement-probe-signal.mjs` on the school network (still carried). (b) Real student pilot — now the signals to watch include mini-map visits, modal opens, and node-clicks. (c) Mobile touch gestures deferred — if the pilot shows students on phones, pinch-zoom + single-finger drag would be a ~80 LOC follow-up. (d) Optional polish: faint cluster-outline circles behind each unit grouping for visual hint, subtle pulsing animation for highest-mastery node, hover tooltip with formula name + mastery percentage. None of these are blocking. (e) Consider a constellation-line variant where co-occurring formulas (from FRQ decompositions or formula-procedure map) get faint connecting lines. Would add "topological awareness" on top of the current cluster layout. (f) The `computeMapLayout` spec currently ignores unit 3 (sampling methods) which has no formulas mapped. The fallback dumps unmapped formulas into unit 3's cell — a minor aesthetic inconsistency but not a bug. (g) Block-scope oddity at line 303 still deferred. (h) The chi-sq-gof / chi-sq-conditions / chi-sq-conclude / chi-sq-output / chi-sq-select / chi-sq-hyp / std-resid-chi / expected-gof / expected-twoway / df-gof / df-twoway cluster in unit 8 makes that unit's outer ring very crowded — 11 formulas on a circle of radius 65 means angular spacing is tight. Could split into two rings if it looks too cluttered in practice. (i) The v4/v5/inline sync gotcha from the post-dispatch fix should inform future session briefs — add a checklist item "verify inline HTML exports match the standalone .js file".

- **Entry 86 — content audit (Codex) + polish pass (Sonnet)** (2026-04-15 session 86). User asked "what's next?" after session 85. Opus recommended two parallel streams: a read-only content audit (Codex) and a polish punt-list (Sonnet). User picked both. They ran concurrently without conflict — audit touches no files except the result document, polish only touches `study_guide_diagnostic.html` + tests.
    - **Polish scope** (Sonnet): (1) remove the session-80 "Queued for tomorrow" chip from `renderFrqHelperPanel` (redundant with session-81 review queue), (2) session-85 mastery map polish: faint cluster-outline circles, hover tooltip, subtle pulse on highest-mastery node. All additive — preserved `queuedFormulasToday` pure function intact as dead code since it's well-tested and reusable.
    - **Sonnet dispatch** (`.session86-polish-brief.md` → `.session86-polish.result.md`). Single pass, 222 → 231 green (+9 tests). Key deliverables:
      * **Chip removal**: deleted 3 CSS rules (`.sg-frq-queued-formulas`, `.sg-frq-queued-label`, `.sg-frq-queued-list`) and a ~7-line chip-rendering block in `renderFrqHelperPanel`. Obsolete session-80 chip tests replaced with 2 valid session-80 tests that still hold. `queuedFormulasToday` pure function untouched in all logic blocks; v5-ladder behavioral tests for it remain valid.
      * **Cluster outlines** (`renderMasteryMap`): new `OUTLINE_RADIUS = 80` constant; `rgba(100, 110, 170, 0.18)` faint stroke drawn BEFORE labels and nodes so they sit visually behind the content. Zoom-invariant line width (`1.5 / zoom`). Purely decorative — not part of `hitTest` targets. 9 outline circles drawn per render.
      * **Hover tooltip** (`showMasteryMapModal`): new `<div class="sg-mastery-map-tooltip">` element absolutely-positioned inside `.sg-mastery-map-canvas-wrap`, `pointer-events:none` so clicks pass through to the canvas. On canvas `mousemove`: inverse-transform cursor to world coords, hit-test for a node, populate tooltip name + mastery text, position near cursor with edge-flip if too close to the right side. Three text states: `"Not yet touched"` for untouched formulas, `"Graduated — you know this"` for `touchedFormulas[id].graduated === true`, otherwise `"Mastery: N%"` via `Math.round(mastery * 100)`. `mouseleave` hides. CSS: dark semi-transparent bg, 0.78rem text, shadow, max-width 240px.
      * **Pulse rAF loop** (`showMasteryMapModal`): finds the highest-mastery node (tie-break alphabetically by id), only runs if at least one formula is touched (`hasTouchedNodes` guard), stores `pulseRafId` so all 3 close paths (close button, backdrop click, Esc key) can `cancelAnimationFrame`. `viewState.pulseGlowScale = 1 + Math.sin(time * 2*Math.PI / 2000) * 0.15` — 2-second period, 15% amplitude on the GLOW radius only (the node's own fill circle is unchanged — no bouncing). Rendered as a multiplier applied only to the highest-mastery node's radial-gradient size.
    - **Audit scope** (Codex, read-only): 4 passes over `data/ap-stats-cartridge.js` (81 formulas), `data/formula-procedure-map.js` (36 mappings), `ti84-procedures-data.json` (27 procedures), `data/frq-decompositions.json` (31 FRQ skills across 9 FRQs). Diagnosis only — no code changes. Output: `.session86-audit.result.md` (130 lines) with table-format findings rated OK / WEAK / WRONG / MISSING.
    - **Audit findings summary**:
      * **Pass 1 — formula-procedure map**: 17 OK / 16 WEAK / **3 WRONG** / 2 MISSING out of 36 entries. **3 WRONG mappings** all point formulas to `normalcdf` where the procedure doesn't actually compute the formula — it only uses it as input: `zscore → normalcdf` (normalcdf takes z as input, doesn't compute z), `z-test-stat → normalcdf` (same issue), `empirical-rule → normalcdf` (empirical rule is a memorized approximation, not a calculator procedure). These mappings mislead students into thinking the procedure implements the formula. **WEAK mappings** (16): most are "setup vs output" ambiguity — e.g., `phat-se → normalcdf-sampling` uses phat-se as input to the procedure but doesn't compute it; `variance → one-var-stats` requires the student to square the Sx output themselves; `paired-t → t-test-data` only works after the student creates a difference list. **Missing entries**: `two-prop-z` and `two-prop-ci` should probably map to `two-propztest` / `two-propzint` but neither procedure record exists in `ti84-procedures-data.json`.
      * **Pass 2 — FRQ decomposition**: 24 OK / **7 WEAK** / 0 WRONG / 0 MISSING out of 31 skills. All `supportingMcqIds` resolve locally (Codex used `data/question-lo-map.js` + supplement bank as a fallback for the sacred `curriculum_render/data/curriculum.js`). Weak skills (7) cluster in U1/U4/U5/U6/U7/U9. Key issues: `u1-frq-tail-comparison` uses `empirical-rule` but the FRQ may need arbitrary z values (swap to a normal-area card); `u4-frq-strategy-ev` and `u4-frq-break-even-p` list `lincomb-mean` but it's really expected-value algebra, not linear-combination work; `u5-frq-estimator-comparison` only supports the sample-mean half with `xbar-mean`/`xbar-sd` — needs a midrange-specific card; `u6-frq-claim-check` uses generic `ci-formula` but should use the specific `one-prop-ci` from the FRQ's part (a); `u7-frq-conditions` uses `normal-condition` which oversimplifies one-sample t conditions (AP language is "roughly normal / no strong skew or outliers"); `u9-frq-scatterplot` uses `linreg`/`residual` but a plot helper would fit better.
      * **Pass 3 — cartridge latex + content**: 47 OK / **6 WEAK** / 0 WRONG out of 53 cards inspected (Codex focused on the 36 mapped plus ~17 random unmapped samples). **No latex errors** in mapped cards. **Action names**: `z-test-stat` → `"Standardized Test Statistic"` is broader than the displayed z-formula; suggest renaming to `"z Test Statistic"`. **Explain/hint wording**: `y-intercept` explain `"Ensures the line passes through (x-bar, y-bar)"` describes derivation not meaning — suggest `"Predicted y when x = 0"`. `normal-condition` explain omits the AP small-sample caveat. **Subconcept errors**: `phat-mean` third subconcept says `phat` is approximately normal "by CLT" without condition check (needs independence + large counts). `margin-error` first subconcept claims `"z* for proportions (sigma known), t* for means (sigma unknown)"` — the proportions/means distinction is not "sigma known" and teaches the wrong reason. `large-counts` second subconcept says procedures are `"invalid"` when the condition fails — should say the normal approximation is unreliable, not "invalid".
      * **Pass 4 — missing entries**: 79 OK / 2 MISSING (the two-prop procedures above). Codex recommends NOT adding one-to-one mappings for `ci-formula`, `margin-error`, `width-ci`, `p-value-interp` — these are cross-procedure concepts, not single TI workflows.
      * **Pass totals**: 115 OK / 29 WEAK / 3 WRONG / 4 MISSING across all passes. Codex also noted that `.v6-frq-decomp-block.js` ignores `skill.penalty` and applies fixed 5/10/15% penalties at runtime regardless of the authored values in `frq-decompositions.json` — either the metadata is misleading or the penalty logic should be updated.
    - **Codex's top 5 fixes** (prioritized, directly from the audit): (1) Remove or remap the 3 wrong `normal*` links — `zscore`/`z-test-stat`/`empirical-rule` all need different targets or should be dropped. (2) Add TI procedures and mappings for `two-prop-z` and `two-prop-ci` — these are genuinely actionable workflows. (3) Tighten the 16 weak "setup only" mappings — consider adding a "setup" vs "output" distinction in the map schema. (4) Fix FRQ support drift in the 7 weak skills, and decide whether `skill.penalty` is metadata-only or drives scoring. (5) Clean up wording on `y-intercept`, `phat-mean`, `margin-error`, `normal-condition`, `large-counts`.
    - **Audit deferred to session 87**: Opus chose NOT to apply any audit fixes in session 86 — the audit is pure diagnosis, the fixes are a separate scope. Session 87 will work from the audit as a punch list.
    - **Empirical jsdom probe** (polish verification): chip gone from DOM ✅, chip not inside helper panel ✅, review queue still populates after hint ✅, `OUTLINE_RADIUS = 80` constant present ✅, faint stroke color `rgba(100,110,170,0.18)` present ✅, tooltip element present ✅, tooltip hidden initially ✅, mousemove at a real node position (unit 1 first node "Sample Mean (x-bar)" at world (200, 120)) shows tooltip ✅ with name "Sample Mean (x-bar)" and meta "Not yet touched" ✅, mouseleave hides tooltip ✅, rAF loop started on modal open (22 calls after open) ✅, `cancelAnimationFrame` called on close ✅, modal torn down ✅, sessions 81/83/84/85 regression-free ✅, `queuedFormulasToday` pure function preserved ✅. **Probe gotcha learned**: initial mousemove test dispatched to (500, 350) which is unit 5's cluster CENTER — an empty spot between nodes. Hit-test correctly returned null and tooltip stayed hidden. Fixed by computing a real node position from the runtime `computeMapLayout` output and dispatching at that canvas coordinate. **Moral**: when probing hit-test behavior, always use a runtime-computed target coord, don't eyeball based on the cluster center math.
    - **Files (new / modified)**: new — `.session86-audit-brief.md`, `.session86-audit.result.md` (130 lines, Codex audit report — kept as working-tree reference for session 87), `.session86-polish-brief.md`, `.session86-polish.result.md`. modified — `study_guide_diagnostic.html` (chip deletions + cluster outlines + tooltip + pulse rAF loop + CSS), `tests/study-guide.test.js` (deleted 2 obsolete chip tests, added 9 new polish + cluster outline + tooltip + pulse tests).
    - **Test counts**: 222/222 → 231/231 (+9). Distribution: `tests/study-guide.test.js` 123 (+9), `tests/study-guide-v5-ladder.test.js` 75 (unchanged — pure functions unchanged), `tests/study-guide-v6-frq-decomposition.test.js` 33 (unchanged).
    - **Open items for session 87**: (a) **CRITICAL — apply Codex audit fixes**. Work from `.session86-audit.result.md` punch list: (i) fix the 3 WRONG normalcdf mappings — either drop them or add a `relatedConcept` field to distinguish "uses as input" from "computes". (ii) Author `two-propztest` and `two-propzint` procedure records in `ti84-procedures-data.json` (this touches the trainer data file — careful), then add map entries. (iii) Decide on the WEAK setup-vs-output mappings — either tag them as "related" in a new schema field, or drop them. (iv) Fix the 7 WEAK FRQ skills — replace `empirical-rule` / `lincomb-mean` / `ci-formula` references with more appropriate support cards. (v) Cartridge wording fixes on 5 cards (`y-intercept`, `phat-mean`, `margin-error`, `normal-condition`, `large-counts`). (vi) Decide whether `skill.penalty` should drive scoring (currently ignored by `.v6-frq-decomp-block.js`). (b) Run `node scripts/supplement-probe-signal.mjs` on the school network (still carried). (c) Real student pilot. (d) Mobile touch gestures for the mastery map (deferred from 85). (e) Block-scope oddity at line 303 still deferred. (f) The `z-test-stat` action name rename from session 86 audit — small but visible to students. (g) The audit did not have access to `curriculum_render/data/curriculum.js` locally (it's in a sibling repo, not in this working dir); if session 87 needs to cross-reference MCQ IDs, either mount the sibling or accept the supplement-bank fallback.

- **Entry 87 — applied session 86 audit fixes: map drops + FRQ drift + cartridge wording** (2026-04-15 session 87). User said "let's go!!!" after reviewing the session 86 audit findings. Opus scoped session 87 narrowly to the CRITICAL fixes: drop 3 WRONG mappings, fix 7 (actually 6) WEAK FRQ skills, update 6 cartridge wording issues. Explicitly deferred to future sessions: (a) authoring `two-propztest`/`two-propzint` procedure records in `ti84-procedures-data.json` (session 88), (b) adding a `direct` vs `related` schema distinction for the 16 WEAK setup/output mappings (future session, needs schema redesign), (c) deciding whether `skill.penalty` metadata should drive runtime scoring (`.v6-frq-decomp-block.js` currently ignores it and applies fixed 5/10/15% — future design decision). Data-only session — no code changes to `study_guide_diagnostic.html` or the logic blocks.
    - **Sonnet dispatch** (`.session87-sonnet-brief.md` → `.session87-result.md`). Single pass, 231 → 247 green (+16 regression tests). Three data files touched + one test file. No iteration needed; Sonnet nailed it on the first try.
    - **Fix A — formula-procedure map drops**: deleted `zscore`, `z-test-stat`, `empirical-rule` entries from `data/formula-procedure-map.js` (all three pointed to `normalcdf`, which takes them as inputs rather than computing them). Also removed the now-stale comment block "Normal / z — computed via normalcdf" since all its entries were dropped. Map count: 36 → 33.
    - **Fix B — FRQ skill drift** in `data/frq-decompositions.json`. 6 skills updated (session 86 originally flagged 7, but `u7-frq-conditions` ended up needing only the cartridge card wording update — the skill's `supportingFormulas` reference to `normal-condition` stays, since fixing the card text fixes the issue at the source):
      * `u1-frq-tail-comparison`: `[zscore, empirical-rule]` → `[zscore]`. Empirical rule only helps for 1/2/3 SD boundaries; for arbitrary z values it misleads. Keeping zscore alone makes the skill work for all cases.
      * `u4-frq-strategy-ev`: `[rv-mean, lincomb-mean]` → `[rv-mean]`. The lincomb-mean reference was extra — this is expected-value reasoning, not a linear-combination computation.
      * `u4-frq-break-even-p`: `[rv-mean, lincomb-mean]` → `[rv-mean]`. Same issue.
      * `u5-frq-estimator-comparison`: `[xbar-mean, xbar-sd]` → `[]` (tool-free). The cartridge has no midrange card, so the sample-mean-only supports misled students. Skill is now explicitly tool-free with updated `whyItMatters` describing it as a reasoning point.
      * `u6-frq-claim-check`: `[ci-formula]` → `[one-prop-ci]`. Generic CI formula replaced with the specific one-proportion interval that the FRQ's part (a) actually produces. Narrower interpretation chosen because the skill covers one-proportion only.
      * `u9-frq-scatterplot`: `[linreg, residual]` → `[]` (tool-free). Linreg and residual are downstream tools; the scatterplot skill is about visual analysis (form/direction/strength/unusual), not computation. Now tool-free with updated `whyItMatters` describing it as a graph-reading skill.
    - **Fix C — cartridge wording** in `data/ap-stats-cartridge.js`. 6 wording updates across 6 cards:
      * `z-test-stat` action: `"Standardized Test Statistic"` → `"Standardized z Statistic"`. The broader "Standardized Test Statistic" was misleading because the displayed latex is specifically z, not the general concept.
      * `y-intercept` explain: `"Ensures the line passes through (x-bar, y-bar)"` → `"Predicted y when x = 0"`. Old text described how `a` is derived from the point-of-means constraint; new text describes what `a` means to a student reading the regression output.
      * `normal-condition` explain: added the AP small-sample caveat `"roughly normal with no strong skew or outliers"` for small n, since the old `"n ≥ 30 or population Normal"` phrasing missed the real-data case.
      * `phat-mean` 3rd subconcept correct: added the Large Counts condition gating — old text claimed `p̂` is approximately normal `"by CLT"` with no condition check; new text says `"Approximately Normal when the Large Counts condition holds (np ≥ 10 and n(1-p) ≥ 10)"`.
      * `margin-error` 1st subconcept correct: replaced the `"z* for proportions (sigma known), t* for means (sigma unknown)"` mischaracterization with an accurate distinction — for proportions the SE uses p̂ itself (no separate SD estimate needed); for means we estimate σ with s which adds uncertainty and requires t.
      * `large-counts` 2nd subconcept correct: `"invalid"` → `"unreliable"`. AP procedures don't become literally invalid when the condition fails; the normal approximation just becomes unreliable, and the z procedures should not be used.
    - **Tests added** (+16 in `tests/study-guide.test.js` new `describe('session 87 audit fixes', ...)` block): regression tests asserting the 3 dropped map entries are gone, the 6 FRQ skill edits are in place (checking `supportingFormulas` contents), and the 6 cartridge wording updates are present. **Test isolation gotcha caught by Sonnet**: when checking "this subconcept no longer contains X", the naive pattern `src.includes('X')` would false-positive if X appears in a DIFFERENT card's subconcept. Sonnet's fix: find the card boundary with `src.indexOf("{id:'", cardStart + 1)` to slice out just the target card, then assert against the slice. Clean.
    - **Dispatch mechanics**: this was a very clean single Sonnet pass. Unlike sessions 79/85/86 which needed follow-up fixes (Sonnet forgetting inline copies, or Codex flagging residuals), session 87 was pure data edits with clear targets and exact before/after specified in the brief. First try passed all tests. No Opus post-dispatch patches needed.
    - **Files (new / modified)**: new — `.session87-sonnet-brief.md`, `.session87-result.md`. modified — `data/formula-procedure-map.js` (−4 lines: 3 entries + 1 stale comment block), `data/frq-decompositions.json` (~20 lines touched across 6 skills), `data/ap-stats-cartridge.js` (~30 lines touched across 6 cards), `tests/study-guide.test.js` (+96 lines, 16 new regression tests).
    - **Test counts**: 231/231 → 247/247 (+16). Distribution: `tests/study-guide.test.js` 139 (+16), `tests/study-guide-v5-ladder.test.js` 75 (unchanged), `tests/study-guide-v6-frq-decomposition.test.js` 33 (unchanged).
    - **Audit residuals after session 87**: out of the 145 original findings (115 OK / 29 WEAK / 3 WRONG / 4 MISSING across all passes), session 87 closed the 3 WRONG mappings, 6 of the 7 WEAK FRQ skills (7th was a card wording fix, still closed), and all 6 cartridge wording issues. Remaining after session 87: 16 WEAK setup/output mappings (punted — schema redesign), 2 MISSING two-prop procedures (session 88), `skill.penalty` metadata decision (future). The 3 WRONG mappings were the most urgent since they actively misled students; those are now gone.
    - **Open items for session 88**: (a) **Author `two-propztest` and `two-propzint` procedure records** in `ti84-procedures-data.json`. Requires ~25 new step records each matching the existing `one-propztest`/`one-propzint` shape, plus regenerating `data/ti84-procedures.js` wrapper via the Node one-liner, plus adding 2 entries to `data/formula-procedure-map.js` (`two-prop-z → two-propztest`, `two-prop-ci → two-propzint`). Cross-reference the existing one-prop procedures as templates; the main differences are the menu letter (6 vs A for test, B vs A for interval) and the extra x2/n2 parameter entries. This is the highest-priority remaining audit item. (b) Run `node scripts/supplement-probe-signal.mjs` on the school network (still carried from sessions 78-86). (c) Real student pilot. (d) Mobile touch gestures for the mastery map (deferred from session 85). (e) Block-scope oddity at line 303 still deferred. (f) Consider whether the 16 WEAK setup/output mappings should get a schema redesign (e.g., `{procedureId, relationship: 'direct'|'setup'}`) or just be left as-is. User preference needed. (g) `skill.penalty` metadata decision — wire it up or remove it from JSON for cleanliness.
