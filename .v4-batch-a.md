# Batch A — Study Guide v4 Data Layer

You are Codex, invoked via cross-agent.py. **Read the spec first**: `.study-guide-spec-v4.md` in the current working directory (`C:/Users/rober/Downloads/Projects/school/follow-alongs`). This batch is one of five (A, B, C, M1, M2). Batch A is the data-layer writes. It runs in parallel with B and C.

## What you must produce (exactly 4 files)

### 1. `data/ap-stats-cartridge.js` (NEW)

**Source**: `C:/Users/rober/Downloads/Projects/tmux-trainer/ap-stats-cartridge.js` (1793 lines, verified).

**What to do**:
1. Copy the file verbatim as `data/ap-stats-cartridge.js`. Preserve all 1793 lines, including the `AP_STATS_CARTRIDGE` const declaration, every formula entry, every tier, every action string.
2. Check whether the source file already has a `window.AP_STATS_CARTRIDGE = AP_STATS_CARTRIDGE;` attachment at the bottom. If yes, leave it. If no, append this block at the end (after any existing module.exports):

```js
if (typeof window !== 'undefined') {
  window.AP_STATS_CARTRIDGE = AP_STATS_CARTRIDGE;
}
```

**Do NOT** add any imports, transpile, remove comments, reformat, or "improve" the source. Byte-for-byte copy plus the one appended block (conditional on what's already there).

### 2. `data/formula-probe-map.js` (NEW)

**What to do**: Wrap `data/formula-probe-map.json` so it attaches to `window.FORMULA_PROBE_MAP` for `<script src>` use, AND exposes a Node `module.exports` so Vitest can `require()` it.

Write exactly this file (replace `<JSON_CONTENT>` with the full verbatim contents of `data/formula-probe-map.json`, properly pasted as a JS object literal):

```js
// Auto-generated wrapper for data/formula-probe-map.json so browsers can load
// it via <script src>. The JSON file is the source of truth — re-run the
// generator (or hand-edit both) if you change the mapping.
const FORMULA_PROBE_MAP = <JSON_CONTENT>;

if (typeof window !== 'undefined') {
  window.FORMULA_PROBE_MAP = FORMULA_PROBE_MAP;
}
if (typeof module !== 'undefined' && module.exports) {
  module.exports = FORMULA_PROBE_MAP;
}
```

The `<JSON_CONTENT>` must be the exact same shape as the JSON file's top-level object, starting with `{ "generated": "2026-04-13", ...`. You can paste the JSON inline (valid JSON is valid JS object-literal syntax for this case since all keys are quoted and all values are string/number/array/object/null).

Before writing, verify `data/formula-probe-map.json` exists and contains a `"map"` key with formula IDs as children.

### 3. `data/formula-probe-supplement.js` — edit ONE entry only

The file currently has an entry for `U6-L4-QS1` at (approximately) lines 131–147. The current entry tests sample size for a MEAN (uses σ, z*, tomatoes) but Topic 6.4 in the AP stats framework is "Setting Up a Test for a Population Proportion." Rewrite it to use a proportion context.

**Method**: Use string-match replacement. Find the current entry by searching for the string `"U6-L4-QS1"` — there should be exactly one match. Replace the entire JS object (from the opening `{` on that entry's line through the closing `}` before the next entry's comma) with the block below. Do NOT touch any other entry. Do NOT touch the file header comment or the `EMBEDDED_CURRICULUM_SUPPLEMENT` const declaration or the `module.exports` block at the bottom.

Replacement entry (paste verbatim including trailing comma if the original had one):

```js
  {
    "id": "U6-L4-QS1",
    "type": "multiple-choice",
    "formulaId": "width-ci",
    "prompt": "A political pollster wants to estimate the proportion of voters who support a ballot measure with 95% confidence and a margin of error of at most 3 percentage points (0.03). A conservative estimate uses p-hat = 0.5 to maximize the needed sample size. Using z* = 1.96, what is the minimum sample size required?",
    "answerKey": "C",
    "attachments": {
      "choices": [
        { "key": "A", "value": "267, because 0.5 times 0.5 times (1.96/0.03) = 32.67." },
        { "key": "B", "value": "534, because 0.5 times (1.96/0.03)^2 = 533." },
        { "key": "C", "value": "1068, because n >= (1.96/0.03)^2 times 0.5 times 0.5 = 1067.11 and we round up." },
        { "key": "D", "value": "33, because 1.96/0.03 times 0.5 is about 33." },
        { "key": "E", "value": "385, using p-hat = 0.1 instead of 0.5." }
      ]
    },
    "reasoning": "For a proportion CI: n >= (z*/ME)^2 * p-hat(1 - p-hat). Using the conservative p-hat = 0.5: n >= (1.96/0.03)^2 * 0.25 = 4268.4 * 0.25 = 1067.11. Always round UP: n = 1068."
  }
```

After replacing, verify the file still parses as valid JS by visually checking that the outer `const EMBEDDED_CURRICULUM_SUPPLEMENT = [` array is still balanced (has `];` closing), that every entry is separated by a comma, and that no existing entry was accidentally duplicated or deleted.

### 4. `data/ap-stats-cartridge-VERSION.txt` (NEW)

One-line text file, exact content (no BOM, trailing newline):

```
Copied 2026-04-13 from ../../tmux-trainer/ap-stats-cartridge.js
```

## Constraints

- You may ONLY edit paths under `data/`. Do not touch `study_guide_diagnostic.html`, `tests/`, `lib/`, `ti84-trainer-v2/`, any markdown file, or any file outside `data/`.
- Do NOT run tests. Do NOT run npm. Do NOT run the browser.
- Do NOT git add, git commit, or git push. Leave changes in the working tree.
- Do NOT delete anything. All edits are additive or surgical replacements.
- Do NOT introduce new dependencies or modify `package.json`.

## Verification steps you should run (read-only)

Before writing, verify the sources:
1. `ls data/formula-probe-map.json data/formula-probe-supplement.js` — both should exist.
2. `wc -l C:/Users/rober/Downloads/Projects/tmux-trainer/ap-stats-cartridge.js` — should be 1793.
3. Grep `data/formula-probe-supplement.js` for `"U6-L4-QS1"` — exactly one match expected.

After writing, verify the outputs:
1. `ls data/ap-stats-cartridge.js data/formula-probe-map.js data/ap-stats-cartridge-VERSION.txt` — all three new files exist.
2. `wc -l data/ap-stats-cartridge.js` — should be within ±5 of 1793 (accounts for the appended window attachment).
3. Grep `data/formula-probe-supplement.js` for `width-ci` — should match both the existing entry's `formulaId` and the reasoning text's `(z*/ME)`.
4. Grep `data/formula-probe-supplement.js` for `tomato` — should be 0 matches (old prompt removed).
5. Grep `data/formula-probe-map.js` for `window.FORMULA_PROBE_MAP` — should match.

## Return format

When done, print a concise summary:
- Files created (paths)
- Files modified (paths + which entry/line range)
- Line counts
- Any warnings or deviations from this spec

Then STOP. Do not attempt any other tasks. Do not run tests. Do not commit.
