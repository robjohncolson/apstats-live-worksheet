Now I have all the information I need. Let me produce the complete plan.

---

# FRQ Decomposition Implementation Plan

---

## 1. Critical-Path Summary

The critical path is: Batch A (author JSON offline) → Batch A2 (audit JSON) → Batch B (pure logic block + test suite) → Batch C (DOM integration). Batch B can begin in parallel with A2 because it only depends on the locked-in JSON schema shape, not the actual content. Batch C is a strict serial dependency on Batch B — it pastes the logic block in and calls the helper functions by name.

---

## 2. Dependency Graph

```
[Batch A]  →  [Batch A2]  →  gate: CC reviews audit
                                      |
     [Batch B] ←──────────────────────┘
     (can start after A2 gate passes; 
      B only needs the schema spec, not A's content)
                  |
             [Batch C]  (strict serial: needs B's function names)
```

- A and B are conceptually independent but B must ship first so Batch C knows the exact exported function names to call. CC should dispatch A first (sequential, 1 agent), then gate on A2 audit, then dispatch B (the logic block is self-contained and knows the schema from the spec), then dispatch C which pastes B's block and wires the DOM.
- There is no safe parallelism between B and C. Dispatch them serially.

---

## 3. Batch A Plan + Prompt

### What Codex must read

1. All 9 FRQ prompts from `C:/Users/rober/Downloads/Projects/school/curriculum_render/data/curriculum.js` at lines 2129, 6638, 11272, 14384, 18734, 23712, 28505, 32553, 36325 — read only the `prompt` field of each.
2. `C:/Users/rober/Downloads/Projects/school/follow-alongs/data/formula-probe-map.json` — to find valid `questionIds[]` for each formula.
3. `C:/Users/rober/Downloads/Projects/school/follow-alongs/data/formula-probe-supplement.js` — to find supplement IDs (`U4-L9-QS1`, etc.) valid for the merged bank.
4. `C:/Users/rober/Downloads/Projects/school/follow-alongs/data/ap-stats-cartridge.js` — to confirm formula IDs (e.g., `"zscore"`, `"phat-sd"`, `"one-prop-ci"`, `"rv-mean"`) appear in `AP_STATS_CARTRIDGE.commands[i].id`.

### Per-FRQ authoring procedure

For each of the 9 gate FRQs, Codex produces 2–4 skills. Each skill must:
- Name a thing the rubric explicitly rewards (parse the `scoring.rubric[].criteria` in the FRQ solution object for hints).
- Map `supportingFormulas[]` to formula IDs actually used by the calculation steps in that rubric criterion.
- Map `supportingMcqIds[]` to MCQ IDs from `formula-probe-map.json` map values, or supplement IDs from `formula-probe-supplement.js` — prefer main bank; fall back to supplement for GAP formulas.
- Set a skill-level `penalty` that sums with other skills to ≤ 0.50. Suggested distribution: 2 skills → 0.10 + 0.10; 3 skills → 0.10 + 0.10 + 0.10; 4 skills → 0.05 + 0.10 + 0.10 + 0.10. Never assign penalty > 0.15 to a single skill.

### Pre-computed content guide per FRQ (to give Codex concrete starting points)

- **U1 (zscore, normal distribution comparison)**: skills: (1) compute z-score `["zscore"]`, MCQs `["U1-L10-Q02","U1-L10-Q03"]`; (2) compare tail probabilities using empirical-rule/normal table `["empirical-rule"]`, MCQs `["U1-L10-Q05","U1-L10-Q07"]`.
- **U2 (residuals, regression SE, normal residuals)**: skills: (1) compute residual `["residual","linreg"]`, MCQs `["U2-L7-Q01","U2-L6-Q03"]`; (2) interpret s (std dev of residuals) `["resid-s"]`, MCQs `["U9-L1-QS1"]`; (3) find percent from normal distribution of residuals `["zscore"]`, MCQs `["U1-L10-Q02"]`.
- **U3 (scope of inference, random assignment vs selection)**: no formulas central; skills are conceptual. skills: (1) identify population of generalization `[]`, MCQs `[]` (may leave empty — acceptable); (2) causal inference vs association `[]`, MCQs `[]`. Note: this FRQ has no formula dependency — `supportingFormulas` may be `[]` and `supportingMcqIds` may be `[]` for both skills. The `penalty` on empty-formula skills should be 0.05 at most since the student has no real "tool" to view.
- **U4 (expected value, probability distribution)**: skills: (1) compute E(X) `["rv-mean"]`, MCQs `["U4-L8-Q04","U4-L8-Q01"]`; (2) compute expected value of mixture (lincomb) `["lincomb-mean"]`, MCQs `["U4-L9-QS1"]`; (3) find threshold probability p `["rv-mean"]`, MCQs `["U4-L8-Q04"]`.
- **U5 (CLT, sampling distribution, estimator comparison)**: skills: (1) apply CLT / compute xbar-sd `["xbar-sd","xbar-mean"]`, MCQs `["U5-L7-Q04","U5-L7-Q05"]`; (2) compute z-score for sample mean `["zscore","xbar-sd"]`, MCQs `["U1-L10-Q02","U5-L7-Q04"]`; (3) interpret bias/variability of estimator `["xbar-mean"]`, MCQs `["U5-L7-Q09"]`.
- **U6 (one-proportion CI, interpret CI)**: skills: (1) check conditions `["random-condition","normal-condition","ten-pct-condition"]`, MCQs `["U6-L2-Q01","U6-L2-Q02"]`; (2) compute the interval `["one-prop-ci","phat-se"]`, MCQs `["U6-L2-Q05","U6-L5-Q01"]`; (3) interpret in context and use to evaluate claim `["p-value-interp"]`, MCQs `["U6-L2-Q03"]`.
- **U7 (one-sample t CI, scope)**: skills: (1) check conditions `["random-condition","normal-condition"]`, MCQs `["U6-L2-Q01"]`; (2) construct CI `["one-mean-ci","xbar-se","df-t"]`, MCQs `["U7-L2-Q06","U7-L5-Q01"]`; (3) generalize — scope of inference `[]`, MCQs `[]`.
- **U8 (chi-square for homogeneity)**: skills: (1) state hypotheses and identify test `["chi-sq-hyp","chi-sq-select"]`, MCQs `["U8-L2-Q05","U8-L5-Q02"]`; (2) check conditions and compute `["chi-sq-conditions","chi-sq","expected-twoway","df-twoway"]`, MCQs `["U8-L2-Q02","U8-L4-Q01","U8-L3-Q06"]`; (3) conclude in context `["chi-sq-conclude"]`, MCQs `["U8-L3-Q09"]`.
- **U9 (regression inference, t-test for slope)**: skills: (1) construct scatterplot and assess linearity `["linreg","slope-b"]`, MCQs `["U2-L6-Q03","U2-L8-Q06"]`; (2) compute LSRL `["linreg","slope-b","y-intercept"]`, MCQs `["U2-L6-Q03","U2-L8-Q01"]`; (3) state hypotheses and conclude `["slope-t","slope-ci"]`, MCQs `["U9-L5-Q02","U9-L5-Q04"]`.

---

### Batch A — Self-contained Codex prompt

````markdown
```
Task: Author data/frq-decompositions.json for the AP Statistics Study Guide v6 FRQ Decomposition feature.

This prompt arrives via file. Working directory: C:/Users/rober/Downloads/Projects/school/follow-alongs

## What you must produce

Create ONE file: C:/Users/rober/Downloads/Projects/school/follow-alongs/data/frq-decompositions.json

The file must contain decompositions for all 9 Progress Check FRQ gate questions. Do NOT modify any existing file.

## Files to read (read-only — do NOT modify)

1. C:/Users/rober/Downloads/Projects/school/curriculum_render/data/curriculum.js
   Read the `"prompt"` field and `"solution"` → `"scoring"` → `"rubric"` → `"criteria"` fields for each of these entry IDs:
   - "U1-PC-FRQ-Q02" (line 2129)
   - "U2-PC-FRQ-Q02" (line 6638)
   - "U3-PC-FRQ-Q01" (line 11272)
   - "U4-PC-FRQ-Q02" (line 14384)
   - "U5-PC-FRQ-Q02" (line 18734)
   - "U6-PC-FRQ-Q01" (line 23712)
   - "U7-PC-FRQ-Q02" (line 28505)
   - "U8-PC-FRQ-Q01" (line 32553)
   - "U9-PC-FRQ-Q01" (line 36325)
   Do NOT read the entire 37913-line file. Use line-offset reads to fetch only the relevant sections (each FRQ entry is roughly 40-80 lines).
   THIS FILE IS SACRED — read-only, never modify.

2. C:/Users/rober/Downloads/Projects/school/follow-alongs/data/formula-probe-map.json
   Read all 122 lines. The "map" object keys are formula IDs; the "questionIds" arrays are valid MCQ IDs from the main curriculum bank.

3. C:/Users/rober/Downloads/Projects/school/follow-alongs/data/formula-probe-supplement.js
   Read all 291 lines. The EMBEDDED_CURRICULUM_SUPPLEMENT array entries have "id" and "formulaId" fields. These IDs (e.g., "U4-L9-QS1", "U9-L1-QS1") are valid supplement MCQ IDs.

4. C:/Users/rober/Downloads/Projects/school/follow-alongs/data/ap-stats-cartridge.js
   Read only the first 50 lines to confirm the commands[] structure. Formula IDs are in commands[i].id (e.g., "mean", "zscore", "rv-mean", "one-prop-ci").

## Required JSON shape

```json
{
  "U1-PC-FRQ-Q02": {
    "unit": 1,
    "topic": "string — 10-20 words describing what this FRQ rewards",
    "skills": [
      {
        "id": "u1-frq-zscore",
        "name": "string — 3-8 words",
        "whyItMatters": "string — 1-2 sentences: what scoring the FRQ depends on this skill",
        "supportingFormulas": ["zscore"],
        "supportingMcqIds": ["U1-L10-Q02", "U1-L10-Q03"],
        "penalty": 0.10
      }
    ],
    "maxPenalty": 0.50
  }
}
```

All 9 gate IDs must appear as top-level keys: "U1-PC-FRQ-Q02", "U2-PC-FRQ-Q02", "U3-PC-FRQ-Q01", "U4-PC-FRQ-Q02", "U5-PC-FRQ-Q02", "U6-PC-FRQ-Q01", "U7-PC-FRQ-Q02", "U8-PC-FRQ-Q01", "U9-PC-FRQ-Q01".

## Per-FRQ authoring instructions

For each FRQ, read the prompt text and rubric criteria from curriculum.js, then produce 2–4 skills that match what the AP rubric explicitly rewards.

Rules:
- supportingFormulas[] must only contain formula IDs that actually appear as `id` values in ap-stats-cartridge.js commands[]. 
  Valid formula IDs include but are not limited to: "mean", "std-dev", "linreg", "linreg-mean", "corr-r", "slope-b", "residual", "r-squared", "y-intercept", "variance", "zscore", "iqr", "empirical-rule", "add-rule", "cond-prob", "complement", "mult-rule", "mult-independent", "rv-mean", "rv-sd", "binom-pmf", "binom-mean", "binom-sd", "lincomb-mean", "lincomb-var", "lintransform", "phat-mean", "phat-sd", "phat-se", "xbar-mean", "xbar-sd", "xbar-se", "diff-p-sd", "diff-p-se", "pooled-se", "one-prop-z", "one-prop-ci", "two-prop-z", "two-prop-ci", "large-counts", "diff-x-sd", "diff-x-se", "one-mean-t", "two-mean-t", "paired-t", "one-mean-ci", "two-mean-ci", "chi-sq", "expected-twoway", "expected-gof", "df-gof", "df-twoway", "chi-sq-select", "chi-sq-hyp", "chi-sq-conditions", "chi-sq-conclude", "chi-sq-output", "std-resid-chi", "slope-mean", "slope-sd", "slope-se", "resid-s", "slope-t", "slope-ci", "z-test-stat", "ci-formula", "power", "margin-error", "width-ci", "df-t", "type-i-error", "type-ii-error", "ten-pct-condition", "random-condition", "normal-condition", "p-value-interp", "log-transform".
  It is OK to leave supportingFormulas as [] for conceptual skills (e.g., scope of inference in U3).

- supportingMcqIds[] must contain IDs that appear EITHER in formula-probe-map.json (under any formula's "questionIds" array) OR as an "id" field in EMBEDDED_CURRICULUM_SUPPLEMENT in formula-probe-supplement.js.
  Never invent question IDs. Leave [] if no valid ID maps to this skill.

- penalty values per skill: use 0.05 or 0.10. Never set a single skill penalty above 0.15.
  The sum of all skill penalties for one FRQ must be ≤ 0.50.
  2 skills: use 0.10 + 0.10 = 0.20 total (well under cap).
  3 skills: use 0.10 + 0.10 + 0.10 = 0.30 total.
  4 skills: use 0.10 + 0.10 + 0.10 + 0.10 = 0.40 total, or some at 0.05.
  Always set maxPenalty to exactly 0.50 for every FRQ.

- skill id format: "uN-frq-<slug>" where N is the unit number and slug is 2-5 lowercase words joined by hyphens.

## Skill guidance per FRQ (use rubric criteria from curriculum.js to verify these fit)

U1-PC-FRQ-Q02 (z-scores, normal comparison):
  topic: "Calculating z-scores and comparing tail probabilities across two normal distributions"
  Skills:
  - id: "u1-frq-zscore", name: "Compute z-scores for both groups", formulas: ["zscore"], mcqs: ["U1-L10-Q02","U1-L10-Q03"], penalty: 0.10
  - id: "u1-frq-normal-tail", name: "Compare tail probabilities from z-scores", formulas: ["empirical-rule"], mcqs: ["U1-L10-Q05","U1-L10-Q07"], penalty: 0.10

U2-PC-FRQ-Q02 (residuals, s, normal residuals):
  topic: "Computing residuals, interpreting the standard deviation of residuals, and using normal distribution of residuals"
  Skills:
  - id: "u2-frq-residual", name: "Calculate and interpret a residual", formulas: ["residual","linreg"], mcqs: ["U2-L7-Q01","U2-L6-Q03"], penalty: 0.10
  - id: "u2-frq-resid-sd", name: "Interpret the standard deviation of residuals", formulas: ["resid-s"], mcqs: ["U9-L1-QS1"], penalty: 0.10
  - id: "u2-frq-normal-resid", name: "Find percentile using normal model for residuals", formulas: ["zscore"], mcqs: ["U1-L10-Q02","U1-L10-Q03"], penalty: 0.10

U3-PC-FRQ-Q01 (scope of inference, random assignment vs selection):
  topic: "Distinguishing population of generalization and cause-and-effect conclusions by study design"
  Skills:
  - id: "u3-frq-generalize", name: "Identify population of generalization for each method", formulas: [], mcqs: [], penalty: 0.10
  - id: "u3-frq-causation", name: "Explain why random assignment allows causal conclusions", formulas: [], mcqs: [], penalty: 0.10

U4-PC-FRQ-Q02 (expected value, probability distribution, mixture EV):
  topic: "Calculating expected value of a discrete random variable and comparing strategies using expected values"
  Skills:
  - id: "u4-frq-ev", name: "Compute E(X) from probability distribution", formulas: ["rv-mean"], mcqs: ["U4-L8-Q04","U4-L8-Q01"], penalty: 0.10
  - id: "u4-frq-mixture-ev", name: "Compute expected value of a mixed outcome strategy", formulas: ["lincomb-mean"], mcqs: ["U4-L9-QS1"], penalty: 0.10
  - id: "u4-frq-threshold-p", name: "Find probability threshold that changes strategy", formulas: ["rv-mean"], mcqs: ["U4-L8-Q04"], penalty: 0.10

U5-PC-FRQ-Q02 (CLT, sampling dist of mean, estimator bias):
  topic: "Applying Central Limit Theorem to find probabilities for sample means and evaluating estimator properties"
  Skills:
  - id: "u5-frq-clt", name: "Identify and apply CLT for sampling distribution of x-bar", formulas: ["xbar-mean","xbar-sd"], mcqs: ["U5-L7-Q04","U5-L7-Q05"], penalty: 0.10
  - id: "u5-frq-z-for-xbar", name: "Compute probability for sample mean using z-score", formulas: ["zscore","xbar-sd"], mcqs: ["U1-L10-Q02","U5-L7-Q04"], penalty: 0.10
  - id: "u5-frq-bias", name: "Evaluate bias and variability of an estimator", formulas: ["xbar-mean"], mcqs: ["U5-L7-Q09","U5-L7-Q05"], penalty: 0.10

U6-PC-FRQ-Q01 (one-proportion CI, interpret, evaluate claim):
  topic: "Constructing and interpreting a one-proportion confidence interval and evaluating a specific claim"
  Skills:
  - id: "u6-frq-conditions", name: "Check conditions for one-proportion z interval", formulas: ["random-condition","normal-condition","ten-pct-condition"], mcqs: ["U6-L2-Q01","U6-L2-Q02"], penalty: 0.10
  - id: "u6-frq-construct-ci", name: "Construct the confidence interval", formulas: ["one-prop-ci","phat-se"], mcqs: ["U6-L2-Q05","U6-L5-Q01"], penalty: 0.10
  - id: "u6-frq-interpret-ci", name: "Interpret the interval and evaluate the claim", formulas: ["ci-formula"], mcqs: ["U6-L2-Q03"], penalty: 0.10

U7-PC-FRQ-Q02 (one-sample t CI, scope):
  topic: "Constructing a one-sample t confidence interval for a mean and evaluating scope of generalization"
  Skills:
  - id: "u7-frq-conditions", name: "Check conditions for one-sample t interval", formulas: ["random-condition","normal-condition"], mcqs: ["U6-L2-Q01"], penalty: 0.10
  - id: "u7-frq-construct-ci", name: "Construct and interpret the t interval", formulas: ["one-mean-ci","xbar-se","df-t"], mcqs: ["U7-L2-Q06","U7-L5-Q01"], penalty: 0.10
  - id: "u7-frq-scope", name: "Evaluate whether results generalize beyond the sample", formulas: [], mcqs: [], penalty: 0.05

U8-PC-FRQ-Q01 (chi-square for homogeneity):
  topic: "Performing a chi-square test for homogeneity including hypotheses, conditions, calculation, and conclusion"
  Skills:
  - id: "u8-frq-hypotheses", name: "State hypotheses and select chi-square test", formulas: ["chi-sq-hyp","chi-sq-select"], mcqs: ["U8-L2-Q05","U8-L5-Q02"], penalty: 0.10
  - id: "u8-frq-conditions-calc", name: "Check conditions and compute chi-square statistic", formulas: ["chi-sq-conditions","chi-sq","expected-twoway","df-twoway"], mcqs: ["U8-L2-Q02","U8-L4-Q01","U8-L3-Q06"], penalty: 0.10
  - id: "u8-frq-conclude", name: "State conclusion in context with p-value", formulas: ["chi-sq-conclude"], mcqs: ["U8-L3-Q09"], penalty: 0.10

U9-PC-FRQ-Q01 (regression inference, t-test for slope):
  topic: "Constructing a regression line, interpreting output, and conducting a t-test for the slope"
  Skills:
  - id: "u9-frq-scatterplot", name: "Construct scatterplot and assess linearity", formulas: ["linreg","residual"], mcqs: ["U2-L6-Q03","U2-L7-Q01"], penalty: 0.10
  - id: "u9-frq-lsrl", name: "Compute the least-squares regression line", formulas: ["linreg","slope-b","y-intercept"], mcqs: ["U2-L6-Q03","U2-L8-Q01"], penalty: 0.10
  - id: "u9-frq-slope-test", name: "Conduct t-test for the slope and conclude", formulas: ["slope-t","slope-ci"], mcqs: ["U9-L5-Q02","U9-L5-Q04"], penalty: 0.10

## Acceptance criteria (self-check before writing the file)

1. All 9 gate IDs present as top-level keys.
2. Every skill has exactly 6 fields: id, name, whyItMatters, supportingFormulas, supportingMcqIds, penalty.
3. Every formula ID in any supportingFormulas[] is in the valid list above.
4. Every MCQ ID in any supportingMcqIds[] appears either in formula-probe-map.json questionIds arrays OR as an id in EMBEDDED_CURRICULUM_SUPPLEMENT. Remove any ID you cannot verify.
5. For each FRQ, sum(skill.penalty) ≤ 0.50 and maxPenalty === 0.50.
6. No skill has penalty > 0.15.
7. Valid JSON (no trailing commas, no comments).

## Output

Write exactly one file: C:/Users/rober/Downloads/Projects/school/follow-alongs/data/frq-decompositions.json

Do not modify any other file. Do not create any other file. Return a short summary of authoring decisions made.
```
````

---

## 4. Batch A2 Plan + Prompt

### Purpose

A single Codex agent reads the JSON Batch A wrote, verifies all 9 entries against the cartridge and MCQ banks, and produces `.frq-decomp-audit.md` in the working dir. CC reads this file before dispatching Batch B. CC should gate on no "FAIL" entries in the audit before proceeding.

### Batch A2 — Self-contained Codex prompt

````markdown
```
Task: Audit C:/Users/rober/Downloads/Projects/school/follow-alongs/data/frq-decompositions.json and produce a short audit report.

Working directory: C:/Users/rober/Downloads/Projects/school/follow-alongs

## What you must produce

Write ONE file: C:/Users/rober/Downloads/Projects/school/follow-alongs/.frq-decomp-audit.md

## Files to read

1. C:/Users/rober/Downloads/Projects/school/follow-alongs/data/frq-decompositions.json — the subject of this audit.
2. C:/Users/rober/Downloads/Projects/school/follow-alongs/data/ap-stats-cartridge.js — extract all commands[i].id values. These are the valid formula IDs.
3. C:/Users/rober/Downloads/Projects/school/follow-alongs/data/formula-probe-map.json — extract all questionIds from the "map" values. These are valid main-bank MCQ IDs.
4. C:/Users/rober/Downloads/Projects/school/follow-alongs/data/formula-probe-supplement.js — extract all "id" values from EMBEDDED_CURRICULUM_SUPPLEMENT. These are valid supplement MCQ IDs.

## Audit checks to perform

For each of the 9 FRQ entries:

A. STRUCTURE: Confirm the entry has: unit (number 1-9), topic (string, non-empty), skills (array, 2-4 items), maxPenalty (number === 0.50).
   Each skill must have: id (string), name (string), whyItMatters (string, non-empty), supportingFormulas (array), supportingMcqIds (array), penalty (number 0.05-0.15).

B. FORMULA RESOLUTION: For every formula ID in supportingFormulas[], confirm it exists in ap-stats-cartridge.js commands[i].id. List any that do not resolve as FAIL.

C. MCQ RESOLUTION: For every MCQ ID in supportingMcqIds[], confirm it exists in either the formula-probe-map.json questionIds arrays OR EMBEDDED_CURRICULUM_SUPPLEMENT ids. List any that do not resolve as FAIL.

D. PENALTY ARITHMETIC: Compute sum(skill.penalty) for each FRQ. Flag as WARN if sum > 0.40 (close to cap). Flag as FAIL if sum > 0.50.

E. SKILL COMPLETENESS: Flag as WARN if a skill has supportingFormulas=[] AND supportingMcqIds=[] (no tools for the student to access). This is acceptable only for conceptual skills like scope of inference in U3. Note whether the rationale in whyItMatters justifies the absence.

F. COVERAGE GAPS: For each FRQ, check whether the skills collectively cover all rubric criterion types (e.g., for U8 chi-square: hypotheses, conditions, calculation, conclusion). Flag as WARN if an obvious skill category is missing.

## Report format (.frq-decomp-audit.md)

```markdown
# FRQ Decomposition Audit — v6 Batch A

Generated: [date]

## Summary
- Total FRQs: 9
- PASS: N  FAIL: N  WARN: N

## Per-FRQ Results

### U1-PC-FRQ-Q02
Status: PASS/FAIL/WARN
Skills: [list names]
Penalty total: X.XX / 0.50
Issues: [none | list of FAIL/WARN items]

[repeat for all 9]

## Unresolvable IDs
[list any formula or MCQ IDs that failed resolution, or "none"]

## Recommendations
[optional: 1-3 bullet points for CC/Codex to fix before Batch C]
```

## Hard rules

- Do NOT modify frq-decompositions.json or any other existing file.
- Do NOT write any file other than .frq-decomp-audit.md.
- All FAIL items must be listed explicitly with the offending ID and which FRQ it came from.
- If frq-decompositions.json does not exist, write the audit file with a single FAIL: "frq-decompositions.json not found" and stop.
```
````

---

## 5. Batch B Plan + Prompt

### Helper function signatures and semantics

All four functions go into a new file `.v6-frq-decomp-block.js`, wrapped in the same IIFE pattern as `.v5-ladder-block.js` (`(function(window){ ... })(typeof window !== 'undefined' ? window : globalThis);`), publishing `window.__studyGuideV6__`.

**`getFrqDecomposition(questionId)`**
- Signature: `(questionId: string) => DecompEntry | null`
- Reads `window.FRQ_DECOMPOSITIONS` (the parsed JSON, set by a `<script src="data/frq-decompositions.json">` tag or by the HTML via a fetch at init — see Batch C for loading mechanism).
- Returns `null` if `window.FRQ_DECOMPOSITIONS` is falsy or `questionId` not found.

**`recordHelperUsed(unit, kind, payload, state)`**
- Signature: `(unit: number, kind: 'formula'|'mcq-correct'|'mcq-wrong', payload: string, state: object) => void`
- `payload` is the formulaId (for kind `'formula'`) or questionId (for MCQ kinds).
- Mutates `state.units[unit].frqHelpers`. Must initialize `frqHelpers` if absent.
- Dedup rule: `'formula'` — if payload already in `frqHelpers.formulasViewed`, no-op. `'mcq-correct'` or `'mcq-wrong'` — if payload already in `frqHelpers.mcqDrills` (as any value), no-op (first-use wins).

**`computeEffectivePenalty(frqHelpers, decomposition)`**
- Signature: `(frqHelpers: object, decomposition: DecompEntry | null) => number`
- Returns `0` if `frqHelpers` is null/undefined or `decomposition` is null.
- Formula penalty: each unique formulaId in `frqHelpers.formulasViewed` that appears in any skill's `supportingFormulas[]` → +0.05. Cap contribution at one count per unique ID even if it appears in multiple skills.
- MCQ penalty: each key in `frqHelpers.mcqDrills` → if value is `'correct'` → +0.10; if `'wrong'` → +0.15.
- Cap result at `decomposition.maxPenalty` (0.50).
- Floor at 0.

**`computeEffectiveScore(rawScore, penalty)`**
- Signature: `(rawScore: 'E'|'P'|'I'|'paper', penalty: number) => {raw: string, rawNumeric: number, effective: number, penaltyPct: number, breakdown: string[]}`
- Map: `'E' → 1.0`, `'P' → 0.6`, `'I' → 0.2`, `'paper' → null`.
- Paper mode: returns `{raw:'paper', rawNumeric:null, effective:null, penaltyPct:0, breakdown:[]}` — no penalty applied.
- Otherwise: `effective = rawNumeric × (1 - penalty)`, rounded to 2 decimal places.
- `breakdown[]` = human-readable strings, e.g. `"Formula card viewed (zscore): −5%"`, `"Correct drill (U1-L10-Q02): −10%"`, `"Wrong drill (U6-L2-Q05): −15%"`.
- `breakdown` is built from `frqHelpers` data which is NOT passed to this function — this function only computes the math. The breakdown strings must be built in the caller (Batch C's render function) from `frqHelpers`. `computeEffectiveScore` takes just `rawScore` and `penalty` (already computed).

### File structure

`.v6-frq-decomp-block.js` — IIFE wrapping all four functions, publishing `window.__studyGuideV6__`. Batch C will paste its contents into `study_guide_diagnostic.html` (same pattern as v5's inline paste) and destructure: `const { getFrqDecomposition, recordHelperUsed, computeEffectivePenalty, computeEffectiveScore } = window.__studyGuideV6__;`.

### Test file

`tests/study-guide-v6-frq-decomposition.test.js` — uses the synthetic-window pattern from `study-guide-v5-ladder.test.js`.

### Batch B — Self-contained Codex prompt

````markdown
```
Task: Write .v6-frq-decomp-block.js (pure-logic IIFE) and tests/study-guide-v6-frq-decomposition.test.js (vitest suite using synthetic-window pattern).

Working directory: C:/Users/rober/Downloads/Projects/school/follow-alongs

This prompt arrives via file. Do NOT modify any existing files.

## Context

The v6 FRQ Decomposition feature adds helper-tracking logic to the AP Statistics Study Guide. Batch A has (or will have) written data/frq-decompositions.json. Batch C will wire the DOM. Your job is ONLY the pure logic functions and tests.

Read these files to understand the existing patterns:
- C:/Users/rober/Downloads/Projects/school/follow-alongs/.v5-ladder-block.js — the IIFE wrapper pattern to copy
- C:/Users/rober/Downloads/Projects/school/follow-alongs/tests/study-guide-v5-ladder.test.js — the synthetic-window test pattern to copy

## Deliverable 1: .v6-frq-decomp-block.js

File must be an IIFE: `(function(window){ 'use strict'; ... })(typeof window !== 'undefined' ? window : globalThis);`

It publishes `window.__studyGuideV6__` with exactly these four exports:

### getFrqDecomposition(questionId)

```js
function getFrqDecomposition(questionId) {
  // Returns window.FRQ_DECOMPOSITIONS[questionId] if it exists, else null.
  // Returns null if window.FRQ_DECOMPOSITIONS is falsy.
  // Returns null if questionId is not a string or is empty.
}
```

### recordHelperUsed(unit, kind, payload, state)

Parameters:
- unit: integer 1-9
- kind: 'formula' | 'mcq-correct' | 'mcq-wrong'
- payload: string (formulaId for 'formula'; questionId for MCQ kinds)
- state: the global state object

Behavior:
1. Ensure state.units[unit].frqHelpers exists. If not, initialize to:
   { formulasViewed: [], mcqDrills: {}, confirmed: false, activeDrillQuestionId: null }
2. kind === 'formula': if payload is already in frqHelpers.formulasViewed, no-op. Otherwise push payload.
3. kind === 'mcq-correct': if payload is already a key in frqHelpers.mcqDrills, no-op. Otherwise set frqHelpers.mcqDrills[payload] = 'correct'.
4. kind === 'mcq-wrong': if payload is already a key in frqHelpers.mcqDrills, no-op. Otherwise set frqHelpers.mcqDrills[payload] = 'wrong'.
5. Returns undefined. Mutates state in-place.

### computeEffectivePenalty(frqHelpers, decomposition)

Parameters:
- frqHelpers: object with formulasViewed[], mcqDrills{}, or null/undefined
- decomposition: DecompEntry from getFrqDecomposition, or null

Returns: number in [0, 0.50]

Behavior:
1. If frqHelpers is falsy or decomposition is falsy, return 0.
2. For each unique formulaId in frqHelpers.formulasViewed: +0.05 if that formulaId appears in ANY skill's supportingFormulas[] in decomposition.skills. Use a Set to deduplicate formulaIds even if they appear in multiple skills.
3. For each key in frqHelpers.mcqDrills: if value === 'correct' → +0.10; if value === 'wrong' → +0.15.
4. Cap result at decomposition.maxPenalty (use 0.50 if maxPenalty is missing or not a number).
5. Floor at 0.
6. Return as a number with normal floating-point precision (no rounding at this layer).

### computeEffectiveScore(rawScore, penalty)

Parameters:
- rawScore: 'E' | 'P' | 'I' | 'paper'
- penalty: number in [0, 0.50]

Returns: object:
```js
{
  raw: string,       // the raw score letter: 'E', 'P', 'I', or 'paper'
  rawNumeric: number | null,  // E→1.0, P→0.6, I→0.2, paper→null
  effective: number | null,   // rawNumeric * (1 - penalty), rounded to 2 decimal places. null if paper.
  penaltyPct: number,         // penalty * 100, rounded to nearest integer. 0 if paper.
  breakdown: []               // always empty array — caller builds breakdown text
}
```

Paper mode: rawNumeric=null, effective=null, penaltyPct=0, breakdown=[].
Unknown rawScore: treat as 'I'.
Rounding: Math.round(rawNumeric * (1 - penalty) * 100) / 100.

### Export

```js
window.__studyGuideV6__ = {
  getFrqDecomposition,
  recordHelperUsed,
  computeEffectivePenalty,
  computeEffectiveScore
};
```

## Deliverable 2: tests/study-guide-v6-frq-decomposition.test.js

Use ESM imports (the package.json has "type":"module"):
```js
import { describe, it, expect, beforeEach } from 'vitest';
import { readFileSync, existsSync } from 'node:fs';
import { resolve } from 'node:path';
```

Synthetic-window pattern (copy from study-guide-v5-ladder.test.js):
```js
const V6_PATH = resolve(__dirname, '../.v6-frq-decomp-block.js');

function loadV6(fakeWindow) {
  const src = readFileSync(V6_PATH, 'utf8');
  new Function('window', src)(fakeWindow);
  return fakeWindow.__studyGuideV6__;
}

function makeFakeWindow(decomps = {}) {
  return {
    FRQ_DECOMPOSITIONS: decomps
  };
}
```

### Required tests (write all of these, plus any you deem necessary):

**getFrqDecomposition:**
1. Returns null when FRQ_DECOMPOSITIONS is undefined
2. Returns null when FRQ_DECOMPOSITIONS is an empty object and questionId not found
3. Returns the entry when the questionId matches
4. Returns null when questionId is empty string
5. Returns null when questionId is not a string (pass a number)

**recordHelperUsed:**
6. Initializes frqHelpers if absent from unit state
7. Formula dedup: calling with same formulaId twice leaves formulasViewed with one entry
8. MCQ-correct dedup: calling mcq-correct twice for same questionId leaves value 'correct' and does not flip to second call's value
9. MCQ-wrong does NOT overwrite an already-recorded 'correct' for same questionId (first-use wins)
10. MCQ-correct does NOT overwrite an already-recorded 'wrong' for same questionId (first-use wins)
11. Multiple different formulaIds accumulate in formulasViewed array
12. confirmed field is initialized false and not mutated by recordHelperUsed

**computeEffectivePenalty:**
13. Returns 0 when frqHelpers is null
14. Returns 0 when decomposition is null
15. Returns 0 when formulasViewed is empty and mcqDrills is empty
16. Formula that IS in a skill's supportingFormulas adds 0.05
17. Formula that is NOT in any skill's supportingFormulas adds 0 (viewer penalty only applies to formulas that are actually in the decomposition)
18. Same formulaId in two different skills' supportingFormulas counts only once (0.05 total not 0.10)
19. Correct MCQ drill adds 0.10
20. Wrong MCQ drill adds 0.15
21. Penalties from multiple helpers accumulate correctly
22. Total is capped at maxPenalty (0.50): test case where raw sum would be 0.65 — result must be 0.50

**computeEffectiveScore:**
23. E + 0 penalty → effective = 1.0
24. E + 0.10 penalty → effective = 0.90
25. P + 0.20 penalty → effective = Math.round(0.6 * 0.8 * 100) / 100 = 0.48
26. I + 0.30 penalty → effective = Math.round(0.2 * 0.7 * 100) / 100 = 0.14
27. paper → effective = null, rawNumeric = null, penaltyPct = 0
28. Unknown score (e.g. 'X') → treats as 'I'
29. penaltyPct rounds to integer: 0.333... penalty → penaltyPct = 33
30. breakdown is always [] (empty array)

## Must not

- Touch study_guide_diagnostic.html
- Touch any existing .js file
- Modify formula-probe-map.json, ap-stats-cartridge.js, curriculum.js, or any data file
- Use require() — this is an ESM project ("type":"module" in package.json)
- Add Node require() calls to the test file; use only readFileSync + new Function pattern

## Output

Two files: .v6-frq-decomp-block.js and tests/study-guide-v6-frq-decomposition.test.js
Short summary of decisions made.
```
````

---

## 6. Batch C Plan + Prompt

### State schema migration

In `normalizeState` (line 1556), the version check at line 1559 currently accepts `2, 3, 4, 5`. Codex must add `6` to that list. The migration branch for `schemaVersion === 6` carries everything from v5 forward and adds `frqHelpers` to each unit. For v5 → v6, `touchedFormulas` and `dailyDose` carry forward unchanged — only `frqHelpers` is added. For v2/v3/v4, the existing "reset to fresh" path gains `frqHelpers: defaultFrqHelpers()`. Bump `STORAGE_KEY` to `'apStatsStudyGuideDiagnostic.v6'`, `SCHEMA_VERSION` to `6`, `STATE_ID` to `'sg-state-v6'`.

`defaultFrqHelpers()` returns: `{ formulasViewed: [], mcqDrills: {}, confirmed: false, activeDrillQuestionId: null }`.

In `makeDefaultState` (line 1461), each `units[unit]` object gains `frqHelpers: defaultFrqHelpers()`. In `normalizeState`, the per-unit loop gains: `frqHelpers: normalizeFrqHelpers(value.frqHelpers)`.

`normalizeFrqHelpers(raw)` returns a validated frqHelpers object: `formulasViewed` as filtered array of strings, `mcqDrills` as object with string keys and `'correct'|'wrong'` values, `confirmed` as boolean, `activeDrillQuestionId` as string or null.

### Paste location

The v6 block paste goes at line 1423 — after the v5 IIFE closing `})(...)` but before the `const { LADDER, ... } = window.__studyGuideV5__;` destructure line. Add a second destructure line immediately after:
```js
const { getFrqDecomposition, recordHelperUsed, computeEffectivePenalty, computeEffectiveScore } = window.__studyGuideV6__;
```

The JSON must be loaded. Because the file is a plain JSON file (not a JS module), the cleanest approach is to add a `<script>` tag that sets `window.FRQ_DECOMPOSITIONS = {...}` — but that requires either converting the JSON to JS or loading via fetch. The recommended approach for Batch C: add a `<script>` tag in the `<head>` section as `<script src="data/frq-decompositions.js"></script>` and instruct Batch C to write a small wrapper file `data/frq-decompositions.js` that does `window.FRQ_DECOMPOSITIONS = { ... }` with the JSON content. The alternative is a `fetch()` call in `init()` — but that creates async initialization that complicates the existing sync startup. The wrapper JS file is simpler and consistent with how `ap-stats-cartridge.js` works.

Wait — re-reading the hard rules: Batch C modifies `study_guide_diagnostic.html` only, and presumably Batch A wrote `data/frq-decompositions.json`. So Batch C should add a `fetch` call in `init()` to load the JSON asynchronously before `refreshFocusedLayout()`, OR convert the JSON loading at startup. Actually the simplest approach given the existing architecture: in `init()`, add a synchronous `fetch` that sets `window.FRQ_DECOMPOSITIONS`. But fetch is async. Better: write a `<script>` tag in the HTML head that inline-embeds the JSON content as `window.FRQ_DECOMPOSITIONS = <JSON content here>`. This requires Batch C to read the JSON and embed it inline — which Codex can do since both files are in scope.

### renderActiveProbe FRQ branch additions (lines 2233-2289)

After the `frq-answer` textarea and before the `actions` div, insert a `<div class="sg-frq-helpers">` containing:
1. A `<p class="sg-frq-helpers-intro">` showing "Helpers may reduce your score." only if `decomp !== null`.
2. For each skill in `decomp.skills`: a `<button class="sg-frq-helper-formula-btn">` per formula in `skill.supportingFormulas` and a `<button class="sg-frq-helper-drill-btn">` per ID in `skill.supportingMcqIds`.

On helper button click: if `!data.frqHelpers.confirmed`, show the modal (see below). Otherwise call `recordHelperUsed(unit, kind, payload, state)`, `renderRemediation()`, `saveSoon()`.

### Confirmation modal

When a helper is first used per unit (per session — `data.frqHelpers.confirmed === false`), show a modal: title "Using helpers will reduce your score.", body "Continue?", two buttons: "Yes, show helper" and "Cancel". On confirm: set `data.frqHelpers.confirmed = true`, call `recordHelperUsed`, `renderRemediation()`, `saveSoon()`. The modal uses the existing `.sg-modal-backdrop` / `.sg-modal` CSS classes (lines 102-104). The backdrop element gets class `sg-frq-helper-modal` added for test targeting.

### renderRemediation FRQ branch (lines 2296-2361)

At the top of `renderRemediation()`, after deriving `questionId`, add:
```js
const isFrqGate = /^U\d-PC-FRQ/.test(questionId);
```

If `isFrqGate`: skip the existing `tagEntry`/`lessonInfo` logic. Instead:
1. Call `const decomp = getFrqDecomposition(questionId)`.
2. If `decomp === null`: render the existing "Lesson ?" fallback (graceful degradation — this is a safety net, not expected in production).
3. If `decomp !== null`:
   - Render `make('div', 'sg-remediation-kicker', 'Unit N · FRQ Gate')`.
   - Render `make('h3', 'sg-remediation-title', decomp.topic)`.
   - If `data.frqHelpers.activeDrillQuestionId !== null`: render the active helper MCQ in place of the skill list. Find the question via `findQuestion(data.frqHelpers.activeDrillQuestionId)`. Render it as a mini MCQ (prompt + choices + check button). On check: call `recordHelperUsed(unit, correct ? 'mcq-correct' : 'mcq-wrong', drillQuestionId, state)`, clear `activeDrillQuestionId`, `renderRemediation()`, `renderActiveProbe()`.
   - Otherwise: render each skill as a list item showing `skill.name` + `skill.whyItMatters`.
4. Then render existing `focusSynthesis` block if present.
5. No curriculum link for FRQ gates (there is no lesson to link to).

### renderGrade update (line 2369)

`renderGrade(grade)` gains an optional second parameter `effectiveScoreResult`:
```js
function renderGrade(grade, effectiveScoreResult)
```

If `effectiveScoreResult` is provided and `grade.score !== 'paper'`:
- Add a `<div class="frq-grade-effective">` inside the panel after `copy`, showing:
  - `Raw: ${grade.score} → ${(rawNumeric * 100).toFixed(0)}%`
  - `Penalty: ${penaltyPct}%`
  - `Effective score: ${(effective * 100).toFixed(0)}%`

The call site at line 2275 changes to:
```js
const decomp = getFrqDecomposition(questionId);
const frqH = data.frqHelpers;
const penalty = computeEffectivePenalty(frqH, decomp);
const eff = computeEffectiveScore(data.frqGrade.score, penalty);
if (data.frqGrade) card.appendChild(renderGrade(data.frqGrade, eff));
```

### CSS additions

Minimal CSS to add inside the existing `<style>` block (near line 195-210):
```css
.sg-frq-helpers{display:grid;gap:8px;padding:10px 0;border-top:1px solid var(--sg-border)}
.sg-frq-helper-intro{margin:0;font-size:.85rem;color:var(--sg-text-dim)}
.sg-frq-helper-formula-btn,.sg-frq-helper-drill-btn{padding:6px 12px;border-radius:999px;border:1px solid var(--sg-border);background:var(--sg-bg-card);color:var(--sg-text);font:inherit;cursor:pointer;font-size:.85rem}
.sg-frq-helper-formula-btn:hover,.sg-frq-helper-drill-btn:hover{border-color:var(--sg-accent)}
.sg-frq-helper-modal .sg-modal{gap:12px}
.frq-grade-effective{font-size:.9rem;color:var(--sg-text-dim);display:grid;gap:4px}
```

### Test updates in tests/study-guide.test.js

The `study-guide.test.js` structural tests currently check for `'apStatsStudyGuideDiagnostic.v3'` (line 125) — these are existing string assertions. The new v6 assertions to add at the end of the `study_guide_diagnostic.html — v3 structure` describe block:
- HTML contains `'apStatsStudyGuideDiagnostic.v6'`
- HTML contains `'sg-state-v6'`
- HTML contains `'getFrqDecomposition'`
- HTML contains `'computeEffectiveScore'`
- HTML contains `'frqHelpers'`
- HTML contains `'sg-frq-helper-modal'`
- HTML contains `'sg-frq-helpers'`

Note: the test on line 125 (`'apStatsStudyGuideDiagnostic.v3'`) will break after Batch C bumps to v6 — Codex must update that assertion to `v6`.

### Import state chain update

In `importStateFromFile` (lines 2490-2517), the regex chain at line 2492-2496 must be extended to also match `'sg-state-v6'` (add `matchV6` at the top of the chain).

### Batch C — Self-contained Codex prompt

````markdown
```
Task: Wire the FRQ Decomposition feature into study_guide_diagnostic.html (state schema v5→v6, helper UI, renderRemediation FRQ branch, renderGrade effective score display) and update tests/study-guide.test.js.

Working directory: C:/Users/rober/Downloads/Projects/school/follow-alongs

This prompt arrives via file. The following files already exist and must NOT be modified:
- .v5-ladder-block.js
- .v6-frq-decomp-block.js  (written by Batch B)
- tests/study-guide-v5-ladder.test.js
- tests/study-guide-v6-frq-decomposition.test.js  (written by Batch B)
- data/frq-decompositions.json  (written by Batch A)
- data/ap-stats-cartridge.js
- data/formula-probe-map.json
- data/formula-probe-supplement.js
- curriculum_render/data/curriculum.js  (SACRED — never touch)

## Files you MUST modify (only these two)

1. C:/Users/rober/Downloads/Projects/school/follow-alongs/study_guide_diagnostic.html  (2598 lines currently)
2. C:/Users/rober/Downloads/Projects/school/follow-alongs/tests/study-guide.test.js

## File you MUST create (only this one)

3. C:/Users/rober/Downloads/Projects/school/follow-alongs/data/frq-decompositions.js
   This is a small JS wrapper that embeds the JSON so it loads as a synchronous script.
   Read data/frq-decompositions.json and write:
   ```js
   window.FRQ_DECOMPOSITIONS = <contents of frq-decompositions.json>;
   ```
   Make this a literal assignment, not a module export.

## Step 1: Read the existing code

Before making ANY edits, read:
- study_guide_diagnostic.html lines 1-200 (CSS block)
- study_guide_diagnostic.html lines 288-300 (STORAGE_KEY, SCHEMA_VERSION, STATE_ID, UNIT_COUNT, bare-{ block open)
- study_guide_diagnostic.html lines 1461-1470 (makeDefaultState)
- study_guide_diagnostic.html lines 1556-1595 (normalizeState)
- study_guide_diagnostic.html lines 1420-1428 (v5 IIFE close + destructure line 1424)
- study_guide_diagnostic.html lines 2233-2295 (FRQ branch of renderActiveProbe)
- study_guide_diagnostic.html lines 2296-2361 (renderRemediation)
- study_guide_diagnostic.html lines 2369-2379 (renderGrade)
- study_guide_diagnostic.html lines 2485-2517 (importStateFromFile)
- .v6-frq-decomp-block.js (all lines — this is what you will paste inline)
- data/frq-decompositions.json (all lines — to embed in frq-decompositions.js)

## Step 2: Create data/frq-decompositions.js

Write this file. Content:
```
window.FRQ_DECOMPOSITIONS = <paste exact JSON content from data/frq-decompositions.json here>;
```

## Step 3: Edits to study_guide_diagnostic.html

### 3a. Add script tag in head (after existing formula-probe-supplement.js script tag, around line 287)

Add:
```html
<script src="data/frq-decompositions.js"></script>
```

### 3b. Bump version constants (lines 290-292)

Change:
- `STORAGE_KEY = 'apStatsStudyGuideDiagnostic.v5'` → `'apStatsStudyGuideDiagnostic.v6'`
- `SCHEMA_VERSION = 5` → `6`
- `STATE_ID = 'sg-state-v5'` → `'sg-state-v6'`

IMPORTANT: The bare `{` at line 294 opens a block that closes at line 783. Do NOT remove it. All your edits inside the script tag must be syntactically valid within that block.

### 3c. Paste v6 block + destructure (after line 1423, which ends the v5 IIFE)

After the line:
```js
      window.__studyGuideV5__ = api;
      })(typeof window !== 'undefined' ? window : globalThis);
```
And before the existing destructure line 1424 (`const { LADDER, ...`), insert:
1. The complete contents of .v6-frq-decomp-block.js (pasted verbatim)
2. Then:
```js
const { getFrqDecomposition, recordHelperUsed, computeEffectivePenalty, computeEffectiveScore } = window.__studyGuideV6__;
```

### 3d. Add defaultFrqHelpers function (after the normalizeActiveProbe function, around line 1505)

```js
function defaultFrqHelpers() {
  return { formulasViewed: [], mcqDrills: {}, confirmed: false, activeDrillQuestionId: null };
}

function normalizeFrqHelpers(raw) {
  if (!raw || typeof raw !== 'object') return defaultFrqHelpers();
  return {
    formulasViewed: Array.isArray(raw.formulasViewed) ? raw.formulasViewed.filter(v => typeof v === 'string') : [],
    mcqDrills: raw.mcqDrills && typeof raw.mcqDrills === 'object'
      ? Object.fromEntries(Object.entries(raw.mcqDrills).filter(([,v]) => v === 'correct' || v === 'wrong'))
      : {},
    confirmed: typeof raw.confirmed === 'boolean' ? raw.confirmed : false,
    activeDrillQuestionId: typeof raw.activeDrillQuestionId === 'string' ? raw.activeDrillQuestionId : null
  };
}
```

### 3e. Update makeDefaultState (line 1464)

In `makeDefaultState`, the units loop assigns each unit object. Add `frqHelpers: defaultFrqHelpers()` to the per-unit object. Current line 1464:
```js
units[unit] = { mode:'both', mcqAnswers:{}, mcqResults:{}, frqAnswer:'', frqGrade:null, focusSynthesis:null, collapsed:true, frqOnPaper:false, masteryState:{}, priority:null };
```
Change to:
```js
units[unit] = { mode:'both', mcqAnswers:{}, mcqResults:{}, frqAnswer:'', frqGrade:null, focusSynthesis:null, collapsed:true, frqOnPaper:false, masteryState:{}, priority:null, frqHelpers: defaultFrqHelpers() };
```

Also update `getUnit` (line 1604) the same way — add `frqHelpers: defaultFrqHelpers()` to the default object there.

### 3f. Update normalizeState (line 1556-1595)

Line 1559 currently:
```js
if (raw.schemaVersion !== 2 && raw.schemaVersion !== 3 && raw.schemaVersion !== 4 && raw.schemaVersion !== 5) return next;
```
Change to also accept `6`:
```js
if (raw.schemaVersion !== 2 && raw.schemaVersion !== 3 && raw.schemaVersion !== 4 && raw.schemaVersion !== 5 && raw.schemaVersion !== 6) return next;
```

In the per-unit loop (lines 1579-1592), after `priority: ...`, add:
```js
frqHelpers: normalizeFrqHelpers(value.frqHelpers),
```

For `doseLadder`: currently only copied if `raw.schemaVersion === 5`. Change to `raw.schemaVersion === 5 || raw.schemaVersion === 6`.
For `dailyDose`: same — add `|| raw.schemaVersion === 6`.

### 3g. Update importStateFromFile (lines 2490-2497)

Add a matchV6 regex before matchV5:
```js
const matchV6 = html.match(/<script[^>]*data-state-id=["']sg-state-v6["'][^>]*>([\s\S]*?)<\/script>/i);
const matchV5 = ...existing...
const match = matchV6 || matchV5 || matchV4 || matchV3 || matchV2;
```

### 3h. Update init() fallback chain (lines 2554-2578)

Add v6 localStorage read at the top of the chain before v5:
```js
const rawV6 = localStorage.getItem(STORAGE_KEY); // STORAGE_KEY is now v6
```
(STORAGE_KEY is already bumped to v6, so the existing v5 read `localStorage.getItem(STORAGE_KEY)` at line 2555 now reads the v6 key. The v5 fallback reads `'apStatsStudyGuideDiagnostic.v5'` — update the string literal from `'.v4'` etc. Specifically, the fallback chain needs:
- Try `apStatsStudyGuideDiagnostic.v6` (= STORAGE_KEY, already done)
- Fallback to `apStatsStudyGuideDiagnostic.v5` (currently at line 2559 as `.v4` — the string at line 2559 reads `.v4`, which was added by the v5 batch — update to `.v5`)

Actually, read the current init() chain carefully at lines 2554-2578 before making this edit. Map the exact string literals that exist and shift them forward by one version.

### 3i. Add CSS (inside the existing style block, near line 195)

After the `.frq-grade-paper` styles (~line 207), add:
```css
.sg-frq-helpers{display:grid;gap:8px;padding:10px 0;border-top:1px solid var(--sg-border);margin-top:6px}
.sg-frq-helpers-intro{margin:0;font-size:.85rem;color:var(--sg-text-dim)}
.sg-frq-skill-name{font-weight:700;font-size:.9rem;color:var(--sg-text)}
.sg-frq-helper-formula-btn,.sg-frq-helper-drill-btn{padding:6px 12px;border-radius:999px;border:1px solid var(--sg-border);background:var(--sg-bg-card);color:var(--sg-text);font:inherit;cursor:pointer;font-size:.85rem}
.sg-frq-helper-formula-btn:hover,.sg-frq-helper-drill-btn:hover{border-color:var(--sg-accent)}
.sg-frq-helper-modal .sg-modal{gap:12px}
.frq-grade-effective{font-size:.9rem;color:var(--sg-text-dim);display:grid;gap:4px;padding-top:6px;border-top:1px solid var(--sg-border)}
.sg-helper-drill-card{display:grid;gap:12px;padding:12px;border:1px solid var(--sg-border);border-radius:8px;background:var(--sg-bg-card)}
```

### 3j. Update renderActiveProbe — FRQ branch (around line 2233)

Inside the `else` branch (the FRQ branch), after the `card.append(answer, actions)` line (currently around line 2273), and before the `if (data.frqGrade)` check, add the helper panel:

```js
// --- FRQ helper panel (v6) ---
const decomp = getFrqDecomposition(questionId);
if (decomp) {
  const helperPanel = make('div', 'sg-frq-helpers');
  helperPanel.appendChild(make('p', 'sg-frq-helpers-intro', 'Need a hint? Using helpers reduces your score.'));
  decomp.skills.forEach(skill => {
    if (!skill.supportingFormulas.length && !skill.supportingMcqIds.length) return;
    const skillLabel = make('div', 'sg-frq-skill-name', skill.name);
    helperPanel.appendChild(skillLabel);
    const btnRow = make('div', 'sg-next-row');
    skill.supportingFormulas.forEach(formulaId => {
      const entry = getFormulaEntry(formulaId);
      const btn = make('button', 'sg-frq-helper-formula-btn', `Formula: ${entry ? entry.action : formulaId}`);
      btn.type = 'button';
      btn.addEventListener('click', () => handleHelperClick('formula', formulaId, unit, data, questionId));
      btnRow.appendChild(btn);
    });
    skill.supportingMcqIds.forEach(mcqId => {
      const btn = make('button', 'sg-frq-helper-drill-btn', `Practice drill`);
      btn.type = 'button';
      btn.dataset.mcqId = mcqId;
      btn.addEventListener('click', () => handleHelperClick('drill', mcqId, unit, data, questionId));
      btnRow.appendChild(btn);
    });
    helperPanel.appendChild(btnRow);
  });
  card.appendChild(helperPanel);
}
// --- end helper panel ---
```

Add `handleHelperClick` function (outside renderActiveProbe, near renderGrade):

```js
function handleHelperClick(kind, id, unit, data, questionId) {
  if (!data.frqHelpers) data.frqHelpers = defaultFrqHelpers();
  if (!data.frqHelpers.confirmed) {
    // Show confirmation modal
    showFrqHelperModal(() => {
      data.frqHelpers.confirmed = true;
      saveSoon();
      doHelperAction(kind, id, unit, data, questionId);
    });
    return;
  }
  doHelperAction(kind, id, unit, data, questionId);
}

function doHelperAction(kind, id, unit, data, questionId) {
  if (kind === 'formula') {
    recordHelperUsed(unit, 'formula', id, state);
    // Show formula card in the right pane — set activeDrillQuestionId to null so skills show
    renderRemediation();
    saveSoon();
  } else if (kind === 'drill') {
    data.frqHelpers.activeDrillQuestionId = id;
    renderRemediation();
    saveSoon();
  }
}

function showFrqHelperModal(onConfirm) {
  const backdrop = make('div', 'sg-modal-backdrop sg-frq-helper-modal');
  const modal = make('div', 'sg-modal');
  modal.setAttribute('role', 'dialog');
  modal.setAttribute('aria-modal', 'true');
  modal.tabIndex = -1;
  modal.appendChild(make('h2', '', 'Using helpers will reduce your score.'));
  modal.appendChild(make('p', 'sg-remediation-body', 'Continue?'));
  const btnRow = make('div', 'sg-next-row');
  const confirmBtn = make('button', '', 'Yes, show helper');
  confirmBtn.type = 'button';
  confirmBtn.addEventListener('click', () => {
    backdrop.remove();
    document.removeEventListener('keydown', escHandler);
    onConfirm();
  });
  const cancelBtn = make('button', '', 'Cancel');
  cancelBtn.type = 'button';
  cancelBtn.addEventListener('click', () => {
    backdrop.remove();
    document.removeEventListener('keydown', escHandler);
  });
  btnRow.appendChild(confirmBtn);
  btnRow.appendChild(cancelBtn);
  modal.appendChild(btnRow);
  backdrop.appendChild(modal);
  const escHandler = e => { if (e.key === 'Escape') { backdrop.remove(); document.removeEventListener('keydown', escHandler); } };
  document.addEventListener('keydown', escHandler);
  backdrop.addEventListener('click', e => { if (e.target === backdrop) { backdrop.remove(); document.removeEventListener('keydown', escHandler); } });
  document.body.appendChild(backdrop);
  modal.focus();
}
```

### 3k. Update renderGrade (line 2369)

Change signature to `function renderGrade(grade, effectiveScoreResult)`.

After building the panel (after the missing-ideas list at line 2377), add:
```js
if (effectiveScoreResult && effectiveScoreResult.raw !== 'paper' && effectiveScoreResult.effective !== null) {
  const effDiv = make('div', 'frq-grade-effective');
  effDiv.appendChild(make('div', '', `Raw: ${effectiveScoreResult.raw} (${Math.round(effectiveScoreResult.rawNumeric * 100)}%)`));
  effDiv.appendChild(make('div', '', `Penalty: ${effectiveScoreResult.penaltyPct}%`));
  effDiv.appendChild(make('div', '', `Effective score: ${Math.round(effectiveScoreResult.effective * 100)}%`));
  panel.appendChild(effDiv);
}
```

Update the call site at line 2275:
```js
if (data.frqGrade) {
  const decompForGrade = getFrqDecomposition(questionId);
  const frqH = data.frqHelpers || defaultFrqHelpers();
  const penaltyVal = computeEffectivePenalty(frqH, decompForGrade);
  const effScore = computeEffectiveScore(data.frqGrade.score, penaltyVal);
  card.appendChild(renderGrade(data.frqGrade, effScore));
}
```

### 3l. Update renderRemediation — FRQ gate branch (line 2296)

At the top of `renderRemediation()`, after deriving `questionId` (around line 2304), add:
```js
const isFrqGate = /^U\d-PC-FRQ/.test(String(questionId || ''));
```

Then replace the card-building block with a conditional:

```js
if (isFrqGate) {
  const data = getUnit(unit);
  const decomp = getFrqDecomposition(questionId);
  const card = make('div', 'sg-remediation-card');
  card.appendChild(make('div', 'sg-remediation-kicker', `Unit ${unit} · FRQ Gate`));
  if (!decomp) {
    // Graceful fallback if decomposition not found
    card.appendChild(make('h3', 'sg-remediation-title', 'FRQ Gate'));
    card.appendChild(make('p', 'sg-remediation-body', 'Skill details are unavailable for this gate.'));
  } else {
    card.appendChild(make('h3', 'sg-remediation-title', decomp.topic));
    const activeDrillId = data.frqHelpers && data.frqHelpers.activeDrillQuestionId;
    if (activeDrillId) {
      // Render the helper MCQ drill
      const drillQ = findQuestion(activeDrillId);
      if (drillQ) {
        const drillCard = make('div', 'sg-helper-drill-card');
        drillCard.appendChild(make('p', 'sg-probe-prompt', drillQ.prompt || ''));
        const drillChoices = make('ul', 'choice-list');
        const choiceData = drillQ.attachments && Array.isArray(drillQ.attachments.choices) ? drillQ.attachments.choices : [];
        const drillFeedback = make('div', 'probe-feedback');
        let drillAnswered = false;
        choiceData.forEach(choice => {
          const item = make('li', 'choice-item');
          const label = make('label', 'choice-row');
          const input = document.createElement('input');
          input.type = 'radio';
          input.name = 'sg-drill-choice';
          input.value = choice.key;
          input.addEventListener('change', () => {});
          label.append(input, make('span', 'choice-key', choice.key + ')'), make('span', 'choice-value', choice.value));
          item.appendChild(label);
          drillChoices.appendChild(item);
        });
        const drillCheck = make('button', '', 'Check');
        drillCheck.type = 'button';
        drillCheck.addEventListener('click', () => {
          if (drillAnswered) return;
          const sel = drillChoices.querySelector('input[name="sg-drill-choice"]:checked');
          if (!sel) { showToast('Choose an answer.'); return; }
          const correct = letter(sel.value) === letter(drillQ.answerKey);
          drillAnswered = true;
          recordHelperUsed(unit, correct ? 'mcq-correct' : 'mcq-wrong', activeDrillId, state);
          drillFeedback.textContent = correct ? 'Correct!' : `Incorrect. Correct answer: ${drillQ.answerKey}.`;
          Array.from(drillChoices.querySelectorAll('input')).forEach(i => { i.disabled = true; });
          drillCheck.disabled = true;
          const closeBtn = make('button', '', 'Close drill');
          closeBtn.type = 'button';
          closeBtn.addEventListener('click', () => {
            data.frqHelpers.activeDrillQuestionId = null;
            saveSoon();
            renderRemediation();
            renderActiveProbe();
          });
          drillCard.appendChild(closeBtn);
          saveSoon();
        });
        drillCard.appendChild(drillChoices);
        drillCard.appendChild(drillCheck);
        drillCard.appendChild(drillFeedback);
        card.appendChild(drillCard);
      } else {
        data.frqHelpers.activeDrillQuestionId = null;
      }
    } else {
      // Render skill list
      const skillList = make('ul', 'sg-remediation-skills');
      decomp.skills.forEach(skill => {
        const li = document.createElement('li');
        li.className = 'sg-remediation-body';
        li.innerHTML = `<strong>${esc(skill.name)}</strong> — ${esc(skill.whyItMatters)}`;
        skillList.appendChild(li);
      });
      card.appendChild(skillList);
    }
  }
  // Add focus synthesis if present (same as non-FRQ path)
  const focus = getUnit(unit).focusSynthesis;
  if (focus && focus.overallSummary) {
    card.appendChild(make('p', 'sg-remediation-body', focus.overallSummary));
  }
  ui.remediation.innerHTML = '';
  ui.remediation.appendChild(card);
  typeset(ui.remediation);
  return;
}
// ... existing non-FRQ remediation path continues unchanged ...
```

## Step 4: Update tests/study-guide.test.js

Read the existing test file. Find the describe block `'study_guide_diagnostic.html — v3 structure'`. The test at line 125 checks for `'apStatsStudyGuideDiagnostic.v3'` — that test is about the existing localStorage key in the source, which will now be `v6`. Update that assertion to check for `'apStatsStudyGuideDiagnostic.v6'`. Also update the `'sg-state-v3'` assertion to `'sg-state-v6'`.

Add new `it()` blocks to the existing describe:
```js
it('declares v6 storage schema', () => {
  const src = readFileSync(HTML_PATH, 'utf8');
  expect(src).toContain('apStatsStudyGuideDiagnostic.v6');
  expect(src).toContain('sg-state-v6');
});

it('exposes v6 FRQ decomposition helpers', () => {
  const src = readFileSync(HTML_PATH, 'utf8');
  expect(src).toContain('getFrqDecomposition');
  expect(src).toContain('computeEffectiveScore');
  expect(src).toContain('computeEffectivePenalty');
  expect(src).toContain('recordHelperUsed');
});

it('includes frqHelpers state field', () => {
  const src = readFileSync(HTML_PATH, 'utf8');
  expect(src).toContain('frqHelpers');
  expect(src).toContain('defaultFrqHelpers');
  expect(src).toContain('normalizeFrqHelpers');
});

it('has FRQ helper modal class', () => {
  const src = readFileSync(HTML_PATH, 'utf8');
  expect(src).toContain('sg-frq-helper-modal');
  expect(src).toContain('sg-frq-helpers');
});

it('loads frq-decompositions.js', () => {
  const src = readFileSync(HTML_PATH, 'utf8');
  expect(src).toContain('data/frq-decompositions.js');
});
```

## Hard rules

1. Do NOT touch .v5-ladder-block.js, .v6-frq-decomp-block.js, or any test file other than study-guide.test.js.
2. Do NOT modify curriculum_render/data/curriculum.js (SACRED).
3. The bare `{` block at line 294 must remain — it wraps v4+v5 code and closes at line 783. Your inline paste of the v6 block at line 1424 is OUTSIDE this bare-{ block (after line 783), so there is no conflict. Verify this by checking that the v5 IIFE closes before line 1424.
4. Paper-mode FRQs: frqHelpers tracking still occurs (students may use helpers before going to paper mode) but computeEffectiveScore returns null effective for 'paper' — do not render the effective score div for paper graded FRQs.
5. Use the existing `make()`, `esc()`, `typeset()`, `showToast()`, `saveSoon()`, `findQuestion()`, `letter()`, and `getFormulaEntry()` helpers — do not redefine them.
6. All new DOM elements must use the `make()` helper, not innerHTML, except where explicitly noted above (li.innerHTML for the skill text only).

## Acceptance criteria

- HTML file contains 'apStatsStudyGuideDiagnostic.v6', 'sg-state-v6', 'getFrqDecomposition', 'recordHelperUsed', 'computeEffectivePenalty', 'computeEffectiveScore', 'frqHelpers', 'defaultFrqHelpers', 'sg-frq-helper-modal', 'sg-frq-helpers', 'data/frq-decompositions.js'.
- normalizeState accepts schemaVersion 6.
- makeDefaultState and getUnit include frqHelpers.
- renderRemediation has the isFrqGate branch.
- renderGrade accepts a second parameter.
- All 5 new it() blocks in study-guide.test.js pass (run: npx vitest run tests/study-guide.test.js).
- Existing vitest suite still passes (do not break prior tests).
```
````

---

## 7. Risks and Pitfalls

**1. Block-scope oddity at line 294.** The bare `{` at line 294 encloses all v4 code and closes at line 783. The v5 IIFE (`(function(window){...})(...)`) lives at lines 784-1423 and is itself INSIDE the `<script>` IIFE but OUTSIDE the bare-{ block. The v6 paste must go at line 1424 or later — outside the bare-{ block. Batch C must read the structure carefully (the v5 IIFE end is at line 1423 exactly) and paste after that line. If Codex accidentally pastes inside the bare-{ block, JavaScript will throw a ReferenceError because `'use strict'` is active there.

**2. normalizeState v5→v6 migration: doseLadder and dailyDose gating.** Currently (line 1571-1576), `doseLadder` is copied only if `schemaVersion === 5`, and `dailyDose` the same. After Batch C, both conditions must be `=== 5 || === 6`. If Codex forgets to update this, v6 users will lose their dose ladder on every page reload. This is easy to miss because it is a non-obvious conditional far from the version bump.

**3. Penalty cap interaction with paper-mode pass-through.** `computeEffectiveScore('paper', penalty)` must return `effective: null` — not `1.0 * (1 - penalty)`. The `renderGrade` call site must check `effScore.effective !== null` before rendering the effective score div. If the check is missing, paper-mode FRQs will show a nonsensical effective score line.

**4. `activeProbe.questionId` not matching a known gate.** If a student navigates to a formula probe (MCQ), `questionId` will be something like `'U1-L10-Q02'` — `isFrqGate` will be false, and the existing remediation path runs. But if a student somehow arrives at an FRQ gate whose ID is not in `frq-decompositions.json` (e.g., a legacy ID that doesn't match exactly), `getFrqDecomposition` returns null and Batch C's code must fall through to the "Skill details are unavailable" graceful fallback. Codex must not accidentally render broken HTML in this case.

**5. `frqHelpers` persisting across probe changes.** Per the spec, `frqHelpers` is per-unit and survives probe changes. This means if a student views an FRQ, triggers the confirmation modal, and then navigates away to an MCQ and comes back to the same FRQ, `confirmed` is still `true` and they see no second modal. This is intentional per Fork 5, but Codex must not accidentally wire `advanceDailyQueue` to reset `frqHelpers`. Verify that the `advanceDailyQueue` / `setActiveProbe` code paths do not touch `data.frqHelpers`.

---

## 8. Open Questions for CC

1. **Loading frq-decompositions data.** The plan above recommends creating `data/frq-decompositions.js` as a sync script wrapper. An alternative is a `fetch()` in `init()`. Which is preferred? The sync wrapper is simpler and consistent with how `ap-stats-cartridge.js` works, but it creates a second artifact derived from Batch A's JSON. If CC wants to keep a single canonical JSON file, the fetch path is cleaner but requires Batch C to handle async init.

2. **Formula card "view" UX.** The spec says formula card view = −5% penalty, but does not specify what "viewing" means in the UI — is it rendering the formula's LaTeX content inline in the probe view (like a tooltip), or navigating the student to the Formula Defense cartridge entry? The plan above renders the formula name only (via `formulaName(formulaId)`). If CC wants a full LaTeX render of the formula in-pane, Batch C needs additional CSS and MathJax typeset calls.

3. **Supplement MCQ IDs for U3 and U7 scope-of-inference skills.** Both FRQs have skills with `supportingMcqIds: []` because no existing MCQ directly probes "scope of inference from random assignment." Is CC comfortable shipping these as helper-less skills (no drill button rendered), or should a new supplement entry be authored for them in `data/formula-probe-supplement.js`? If so, that would be a Batch A2 fix item.

4. **Batch B/C parallelism decision.** Can CC dispatch B and C in parallel once A2 passes? The plan above says serialize (B then C) because C must know B's exact function names before writing the paste. If CC has a way to lock in function names from this planning doc, B and C could fan out. Otherwise serialize.

5. **The existing `tests/study-guide.test.js` test at line 125 asserts `'apStatsStudyGuideDiagnostic.v3'`.** After Batch C bumps to v6, that assertion will break. The Batch C prompt above tells Codex to update it to v6. But if CC has other CI checks that depend on the v3 string being present (e.g., it's testing that the old export format is still parseable), this update needs thought before dispatching.

---

### Critical Files for Implementation

- `C:/Users/rober/Downloads/Projects/school/follow-alongs/study_guide_diagnostic.html`
- `C:/Users/rober/Downloads/Projects/school/follow-alongs/.v6-frq-decomp-block.js`
- `C:/Users/rober/Downloads/Projects/school/follow-alongs/data/frq-decompositions.json`
- `C:/Users/rober/Downloads/Projects/school/follow-alongs/tests/study-guide-v6-frq-decomposition.test.js`
- `C:/Users/rober/Downloads/Projects/school/follow-alongs/tests/study-guide.test.js`