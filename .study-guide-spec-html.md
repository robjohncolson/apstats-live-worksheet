# URGENT: Write ONE file — study_guide_diagnostic.html

**You are Codex, invoked as a subagent by Claude Code.** You previously timed out at 10 minutes on the larger 3-file version of this task. The two smaller files (`ai-grading-prompts-study-guide.js` and `tests/study-guide.test.js`) are ALREADY WRITTEN. You only need to write ONE file this time.

## Rules

- DO NOT read `u6_lesson1-2_live.html`, `ai-grading-prompts-u6-l1-2.js`, or `curriculum.js`. This spec is complete.
- DO NOT run `npm test`, `npx vitest`, or any other verification command.
- DO NOT make git commits.
- Write `study_guide_diagnostic.html` at the repo root and exit.
- Start writing the file on your very first tool call. Do not explore.

## Goal

Build a single-file HTML diagnostic worksheet for AP Statistics students. Nine "gate" questions (one per unit, free-response, AI-graded via the existing Railway `/api/ai/grade` endpoint). Each gate that scores `I` opens a drill-down with lesson probes. Each probe the student gets wrong reveals resource links.

## What's already in place

I (Claude Code) have already written these two sibling files. Trust them, do not read or edit them:

**`ai-grading-prompts-study-guide.js`** exposes (on `window`):
- `LESSON_CONTEXT_SG` — a string context for the grader.
- `UNIT_TITLES_SG` — `{1: 'Unit 1: ...', ..., 9: 'Unit 9: ...'}`.
- `UNIT_TOPICS_SG` — `{1: 'short topic label', ...}`.
- `GATE_IDS_SG` — `{1: 'U1-PC-FRQ-Q02', 2: 'U2-PC-FRQ-Q02', 3: 'U3-PC-FRQ-Q01', 4: 'U4-PC-FRQ-Q02', 5: 'U5-PC-FRQ-Q02', 6: 'U6-PC-FRQ-Q01', 7: 'U7-PC-FRQ-Q02', 8: 'U8-PC-FRQ-Q01', 9: 'U9-PC-FRQ-Q01'}`.
- `buildReflectionPromptSG(unitNumber, fullQuestionText, studentAnswer)` → returns the prompt string to send to DeepSeek.
- `getRubricSG(unitNumber)` → `{unitNumber, unitTitle, unitTopic, gateId}`.
- `getGateIdForUnitSG(unitNumber)` → gate question id string.
- `stripFrqBoilerplateSG(promptText)` → strips the `"Show all your work..."` prefix from a College Board FRQ prompt.

**`tests/study-guide.test.js`** will assert (don't worry, don't run tests, but these are the assertions so you know what shape must exist in your HTML file):
- `<script src="../railway_config.js">`
- `<script src="../railway_client.js">`
- `<script src="../curriculum_render/data/curriculum.js">`
- `<script src="ai-grading-prompts-study-guide.js">`
- All 9 gate IDs from `GATE_IDS_SG` appear as string literals
- `id="grade-btn"` (the Grade My Diagnostic button)
- `id="gates-container"` (container div)
- `id="summary-panel"` (summary div)
- The localStorage key `apStatsStudyGuideDiagnostic.v1`
- The string `/api/ai/grade`

Your file MUST contain all of these patterns.

## Critical reference snippets (inlined — don't read the source files)

### Grading fetch pattern (copy this structure verbatim into your JS)
```js
async function gradeGate(unitNumber, questionId, answer) {
  const fullPrompt = findGateQuestionText(questionId); // look up from EMBEDDED_CURRICULUM
  const prompt = window.buildReflectionPromptSG(unitNumber, fullPrompt, answer);

  const response = await fetch(`${window.RAILWAY_SERVER_URL}/api/ai/grade`, {
    method: 'POST',
    headers: { 'Content-Type': 'application/json' },
    body: JSON.stringify({
      scenario: {
        topic: 'AP Statistics Diagnostic Study Guide',
        questionId: questionId,
        lessonContext: window.LESSON_CONTEXT_SG,
      },
      answers: { answer: answer },
      prompt: prompt,
    }),
  });

  if (!response.ok) throw new Error('Grading request failed');
  const result = await response.json();

  // Enrichment pass: P with no missing elements → upgrade to E
  if (result.score === 'P' && result.missing && result.missing.length === 0) {
    result.score = 'E';
    result.feedback = 'Nice work — you covered the key ideas.';
  }
  return result;
}
```

### Railway answer submission (for class-wide tracking, optional and best-effort)
```js
async function submitGateAnswer(unitNumber, answer, result) {
  if (!window.railwayClient || !window.railwayClient.submitAnswer) return;
  try {
    await window.railwayClient.submitAnswer(
      `SG-U${unitNumber}-GATE`,
      answer,
      { score: result.score }
    );
  } catch (err) {
    console.warn('Railway submission failed (non-fatal):', err);
  }
}
```

## The 9 gate questions (use these exact IDs)

Look these up in `EMBEDDED_CURRICULUM` at runtime (the curriculum.js script assigns `const EMBEDDED_CURRICULUM = [...]` at top level, which is a lexical global readable by later scripts on the same page):

1. Unit 1 → `U1-PC-FRQ-Q02` (z-scores, two populations of birds)
2. Unit 2 → `U2-PC-FRQ-Q02` (residuals, LSR, interpretation)
3. Unit 3 → `U3-PC-FRQ-Q01` (experimental design vs observational study)
4. Unit 4 → `U4-PC-FRQ-Q02` (Miguel's golf, random variable, expected value)
5. Unit 5 → `U5-PC-FRQ-Q02` (CLT, house values, midrange estimator)
6. Unit 6 → `U6-PC-FRQ-Q01` (CI for p, television sport preferences)
7. Unit 7 → `U7-PC-FRQ-Q02` (CI for mean, penguin swimming speeds)
8. Unit 8 → `U8-PC-FRQ-Q01` (chi-square homogeneity, soda preferences)
9. Unit 9 → `U9-PC-FRQ-Q01` (regression t-test for slope, bar-cutting machine)

Each question in `EMBEDDED_CURRICULUM` has fields: `id, type, prompt, answerKey, attachments, solution, reasoning, requiresGraph, scoring`. For gate rendering, use:
- `prompt` — full text (after `stripFrqBoilerplateSG` to remove the boilerplate prefix)
- `attachments.table` — if present, render as an HTML `<table>` (2D array, first row = headers)
- `attachments.image` — if present, render as `<img src="../curriculum_render/{path}">` (broken image is acceptable — do not fetch/validate)
- `attachments.imageCaption` — caption under the image
- `requiresGraph: true` — show a dim footnote "*This question references a figure. If you cannot see it, ask your teacher or skip to the drill-down.*"

## Probe selection (runs in browser when a drill-down opens)

```js
function buildProbesForUnit(unitNumber) {
  const unitPrefix = 'U' + unitNumber + '-L';
  const mcByLesson = {};
  for (const q of EMBEDDED_CURRICULUM) {
    if (q.type !== 'multiple-choice') continue;
    if (!q.id.startsWith(unitPrefix)) continue;
    const m = q.id.match(/^U\d+-L(\d+)-Q\d+/);
    if (!m) continue;
    const lesson = parseInt(m[1], 10);
    (mcByLesson[lesson] ||= []).push(q);
  }
  const lessons = Object.keys(mcByLesson).map(Number).sort((a, b) => a - b);
  const probes = [];
  for (const lesson of lessons.slice(0, 6)) {
    const first = mcByLesson[lesson].slice().sort((a, b) => a.id.localeCompare(b.id))[0];
    probes.push({ lesson: lesson, question: first });
  }
  return probes;
}
```

Render each probe with:
- The prompt text
- The choices as radio buttons (`attachments.choices` is `[{key: 'A', value: '...'}, ...]`)
- `attachments.table` as an HTML table if present
- A "Check" button
- On check, compare selected value to `answerKey` (exact match). Store result in localStorage.
- If incorrect: append Resource Card (see below).
- If correct: green ✓, collapse.

## Resource Card format

On incorrect probe, render below the probe:
```html
<div class="resource-card">
  <div class="resource-card-header">📚 Review resources for Unit {u}, Lesson {l}</div>
  <ul>
    <li><a href="./u{u}_lesson{l}_live.html" target="_blank">📝 Follow-along worksheet</a></li>
    <li><a href="./u{u}_l{l}_blooket.csv" target="_blank">🃏 Blooket quiz CSV</a></li>
    <li><a href="../curriculum_render/index.html" target="_blank">❓ Curriculum quiz app (filter by unit {u}, lesson {l})</a></li>
  </ul>
</div>
```

For Units 1 and 2, at the top of the drill-down, show a one-line note: *"Follow-along worksheets aren't available for this unit yet — use the curriculum quiz app."* The per-probe resource card for U1/U2 lessons should still render all three links (the student will discover 404s on their own, which is acceptable UX).

## Grading threshold rules

| Result | Drill-down | Badge color | Copy |
|---|---|---|---|
| `E` | Hidden (header: "✓ Unit passed — click to expand for review") | green | "Strong — you nailed the unit-level synthesis." |
| `P` | Collapsed but visible (header: "Review these if you want to strengthen this unit") | yellow | Use `result.feedback` from API |
| `I` | **Auto-expanded** with "⚠ Focus here" header | red | Use `result.feedback` from API |

## Question IDs for Railway submission

- Gate: `SG-U{n}-GATE` (submit answer + score)
- Probe: `SG-U{n}-L{l}-Q{n}` (submit selected choice + correct/incorrect)

Wrap all `railwayClient.submitAnswer` calls in try/catch and swallow errors.

## localStorage persistence

Key: `apStatsStudyGuideDiagnostic.v1`. Shape:
```js
{
  savedAt: '2026-04-13T...',
  studentName: '...', studentPeriod: '...', studentDate: '...',
  answers: { 'sg-u1-answer': '...', ..., 'sg-u9-answer': '...' },
  grades: { 'sg-u1-answer': { score: 'I', feedback: '...', matched: [], missing: [] }, ... },
  probeAnswers: { 'U3-L4-Q01': 'B', ... },
  drillsOpened: [3, 5, 8]
}
```

Save on every `input` event (debounced ~300ms). Also save after any grade/probe/drill state change. Restore on `DOMContentLoaded` — rehydrate textareas, grade badges, probe selections, and drill visibility.

## Top-level HTML skeleton

```html
<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>AP Statistics — Diagnostic Study Guide</title>
  <style>
    /* Match the general aesthetic of the existing worksheets: navy-blue
       (#003366) accents, platinum-gray backgrounds (#f9f9f9), Segoe UI
       font stack. Use your judgment for spacing, borders, and badges.
       Embed CSS for: .gate-card, .gate-header, .gate-badge, .gate-badge-E,
       .gate-badge-P, .gate-badge-I, .drill-down, .probe, .probe.correct,
       .probe.incorrect, .resource-card, .grade-btn, .summary-panel, .summary-card */
  </style>
</head>
<body>
  <div class="header">
    <div class="header-left">AP Statistics</div>
    <div class="header-center">Diagnostic Study Guide</div>
    <div class="header-right">Know Where to Focus</div>
  </div>
  <h1>Diagnostic Study Guide</h1>
  <p class="subtitle">Nine questions, one per unit. We'll show you exactly where to review.</p>

  <div class="student-info">
    <label>Name: <input id="studentName"></label>
    <label>Period: <input id="studentPeriod"></label>
    <label>Date: <input id="studentDate" type="date"></label>
  </div>

  <div class="instructions-box">
    <strong>How this works:</strong>
    <ol>
      <li>Answer each unit's gate question as fully as you can.</li>
      <li>Click "Grade My Diagnostic" — all 9 go to the grader at once.</li>
      <li>Units you nail (<strong>E</strong>) collapse — done.</li>
      <li>Units you partially answered (<strong>P</strong>) offer optional review.</li>
      <li>Units you miss (<strong>I</strong>) auto-expand lesson probes and remediation resources.</li>
    </ol>
  </div>

  <div id="gates-container"></div>

  <div class="action-bar">
    <button id="grade-btn">🤖 Grade My Diagnostic</button>
    <button id="clear-btn">Clear All Answers</button>
  </div>

  <div id="summary-panel" hidden>
    <h2>Your Diagnostic Summary</h2>
    <div id="summary-grid"></div>
  </div>

  <script src="../railway_config.js"></script>
  <script src="../railway_client.js"></script>
  <script src="../curriculum_render/data/curriculum.js"></script>
  <script src="ai-grading-prompts-study-guide.js"></script>
  <script>
    // All study guide logic here:
    // 1. On DOMContentLoaded, render gates from GATE_IDS_SG + EMBEDDED_CURRICULUM.
    // 2. Wire grade button, clear button, input debounce.
    // 3. Restore from localStorage.
    // 4. Handle drill-down expand, probe check, resource card append.
    // 5. Render summary panel after grading completes.
    // 6. Best-effort Railway submission in try/catch.
  </script>
</body>
</html>
```

## Constraints

- Do not modify or touch `ai-grading-prompts-study-guide.js` or `tests/study-guide.test.js`. They are already written.
- Do not touch any other worksheet file.
- Target length: ~800-1200 lines for `study_guide_diagnostic.html`.
- Your file must contain every literal string the test file greps for (gate IDs, `id="grade-btn"`, `apStatsStudyGuideDiagnostic.v1`, etc.).

## When you're done

Write the file. Emit the result JSON. Exit. Do not verify.
