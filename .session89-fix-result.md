# Session 89 Fixes — Result

## Fix 2 (paper-mode disclosure gate)
- `study_guide_diagnostic.html` line ~4748: changed disclosure gate from `if (grade && grade.score)` to `if (grade && grade.score && grade.score !== 'paper')`
- Updated comment above gate to say "non-paper AI grading" so the new test can assert it
- `tests/study-guide.test.js`: updated the existing "renders disclosures when grade.score is truthy" test to check the new gate string; added a second test asserting the comment wording
- Test count after: 318/318 green

## Fix 5 (audit count recount)
- `.session89-frq-audit.md` lines 115-122: corrected OK count from 27 → 29 (31 skills - 2 WEAK = 29 OK)
- Removed the OPEN row from the alignment totals table; added a separate bold line "Total alignment rows: 31" and "OPEN items (separate from alignment totals): 2" to make it clear OPEN is not an alignment category
- No test changes needed (this file is not exercised by tests)
- Test count after: 318/318 green (no regression)

## Fix 3 (refreshSyncStatus on auth change)
- `study_guide_diagnostic.html` ~line 2405: added `refreshSyncStatus();` as the first statement of `refreshAuthPanelVisibility()`, so every auth state change (sign-in, sign-out, page load) immediately syncs the status text
- `tests/study-guide.test.js`: added test that extracts the `refreshAuthPanelVisibility` function body and asserts it contains `refreshSyncStatus()`
- Test count after: 319/319 green

## Fix 4 (JSON↔wrapper parity test)
- `tests/study-guide.test.js`: added new `describe('Fix 4 — frq-decompositions.js and .json parity', ...)` block with one test that loads both files, uses `new Function('window', jsSrc)(fakeWindow)` to extract `window.FRQ_DECOMPOSITIONS`, and deep-compares against `JSON.parse` of the JSON file
- Test passes immediately, confirming the session 87 regression was already closed by session 89 workstream B sync
- No production code changes (the files were already in parity)
- Test count after: 320/320 green

## Fix 1 (auth form listener rebind)
- `study_guide_diagnostic.html` ~line 2365: extracted `wireAuthFormListeners()` function (37 lines) placed immediately before `refreshAuthPanelVisibility()`. Function body:
  - Re-queries `ui.username`, `ui.password`, `ui.loginBtn`, `ui.createLink` via `getElementById`
  - Attaches `password` Enter-to-login `keydown` handler
  - Attaches `username` change handler (clears auth + switches profile)
  - Attaches `loginBtn` click handler
  - Attaches `createLink` click handler (opens create modal)
- `study_guide_diagnostic.html` restore branch of `refreshAuthPanelVisibility()` (~line 2455): removed the two inline `addEventListener` calls for `loginBtn` and `createLink` that were attached during element construction (would have caused double-firing); removed stale `ui.username/password/loginBtn/createLink` updates from that branch (now handled by `wireAuthFormListeners`); added `wireAuthFormListeners()` call after `renderUsernameOptions()`
- `study_guide_diagnostic.html` `init()` (~line 5048): replaced the ~25-line inline listener wiring block (password keydown, loginBtn click, username change, createLink click) with a single `wireAuthFormListeners()` call
- `tests/study-guide.test.js`: added `describe('Fix 1 — wireAuthFormListeners rebind after sign-out', ...)` with 4 tests:
  1. Function is defined
  2. Called from init (and old inline strings absent from init body)
  3. Called from restore branch of `refreshAuthPanelVisibility`
  4. Re-queries all 4 `getElementById` calls inside the function
- Test count after: 324/324 green

## Files modified / created
- `/C:/Users/rober/Downloads/Projects/school/follow-alongs/study_guide_diagnostic.html` — Fix 2 gate condition, Fix 3 refreshSyncStatus call, Fix 1 wireAuthFormListeners extraction
- `/C:/Users/rober/Downloads/Projects/school/follow-alongs/tests/study-guide.test.js` — Fix 2 updated + new tests, Fix 3 test, Fix 1 tests, Fix 4 parity test describe block
- `/C:/Users/rober/Downloads/Projects/school/follow-alongs/.session89-frq-audit.md` — Fix 5 summary recount

## Final test count
- `npx vitest run tests/study-guide.test.js tests/study-guide-v5-ladder.test.js tests/study-guide-v6-frq-decomposition.test.js` → **324/324 green** (up from 317 baseline; +7 new tests)

## Open questions
- none
