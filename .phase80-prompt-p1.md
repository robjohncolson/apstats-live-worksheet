Phase 80-1: Probe 0x028f0b directly (TI-84 CE ROM transpilation)

## Context
Phase 78-79 found that 0x028f02 (TEST mode label helper) splits into two phases:
- Phase A: `call 0x080244` → `call nz, 0x02398e` (draws mode-icon glyph at top-right)
- Phase B: `call 0x029374` → `push de ; call 0x0a1cac` (draws label string via known primitive)

All session 14 probes entering at 0x028f02 hit missing_block at step 859-1317 before reaching Phase B (the string call at 0x028f0b). We never confirmed that calling 0x0a1cac via this path renders legible text.

**Test**: probe 0x028f0b directly with the known TEST mode convention `(HL=0x029132 for "RADIAN", A=0x91)` and see if it renders the full "RADIAN" string. This validates 0x028f02's calling pattern end-to-end.

## Working directory
C:/Users/rober/Downloads/Projects/school/follow-alongs/TI-84_Plus_CE

## Deliverables

### 1. `probe-phase80-028f0b.mjs`
Pattern-match `probe-phase78-parents.mjs` exactly (same harness shape: boot → OS init → SetTextFgColor → probe with snapshot/restore). Probe list (6 entries):
- `028f0b_radian` — entry=0x028f0b, regs: { a: 0x91, _hl: 0x029132 }, note: 'RADIAN direct'
- `028f0b_degree` — entry=0x028f0b, regs: { a: 0x92, _hl: 0x029139 }, note: 'DEGREE direct'
- `028f0b_normal` — entry=0x028f0b, regs: { a: 0x4f, _hl: 0x0a0467 }, note: 'Normal token name'
- `028f0b_float` — entry=0x028f0b, regs: { a: 0x52, _hl: 0x0a0479 }, note: 'Float token name'
- `029374_radian` — entry=0x029374, regs: { a: 0x91, _hl: 0x029132 }, note: 'middle phase'
- `029374_degree` — entry=0x029374, regs: { a: 0x92, _hl: 0x029139 }, note: 'middle phase'

Run with `maxSteps: 50000, maxLoopIterations: 2000`. Capture VRAM stats (drawn/fg/bg/bbox), first 15 blocks, and ASCII preview if drawn > 100 (use helpers from probe-phase78-parents.mjs).

### 2. `phase80-1-028f0b-report.md`
- Results table: | probe | entry | drawn | fg | bg | bbox | steps | termination |
- Block trace for each probe
- ASCII previews for anything with drawn > 100
- Verdict section: did 0x028f0b render the labeled text string? Does (A=label_code, HL=string_addr) work end-to-end?

## Reference files
- `probe-phase78-parents.mjs` — template harness (ALREADY TESTED, use exact same boot/init/probe pattern)
- `phase77-manual-report.md` — Section 1 has the 0x028f02 disasm showing the two-phase structure
- `phase78-parents-report.md` — example report format

## Task type
IMPLEMENTATION — write the script, EXECUTE it via `node probe-phase80-028f0b.mjs`, produce the report. Do NOT stop after writing — actually run and capture results.

## Timeout
600 seconds. Should complete in ~90s (6 probes × ~10s each + 30s setup).
